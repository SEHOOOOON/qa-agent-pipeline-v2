# 프로젝트 안내: 시나리오와 처리 로직

현재 계약 기준: 2026-09-24. 최신 실행 결과·남은 과제는 [인계 문서](../PROJECT_HANDOFF.md), 설치·실행은 [README](../README.md)에 있습니다.

<a id="logic-map"></a>

## 먼저 보기: 어떤 판단을 왜 하는가

이 절은 **현재 구현을 읽는 지도**입니다. 검사를 추가하거나 제거한 계획이 아닙니다. 세부 업무 규칙은 아래 단계별 목록에서, 실제 조건문·예외·종료 지점·스키마·상태값은 [코드 위치별 색인](logic_inventory.json)에서 확인합니다. `CP` 번호는 자동 검사의 이름이며 모델 호출 횟수나 사람 승인 단계를 뜻하지 않습니다.

### 가장 중요한 보호 6가지

| 중요하게 지킬 것 | 쉬운 설명 | 담당 |
|---|---|---|
| 요청과 다른 시험을 만들지 않기 | 없는 기능·값을 덧붙이거나 해야 할 검사를 빠뜨리지 않음 | CP1·CP2, 공통 모델 근거 검토 |
| 모르는 것을 아는 것처럼 넘기지 않기 | 미정 조건·지원하지 못하는 자동화를 기록하고 진행 가능한 범위와 구분 | A1 진행 판단, A3 실행 자격, 최종 권고 |
| TC와 실제 코드가 같은 것을 검사하기 | 대상·값·조작 순서·확인 시점·기대결과 연결 유지 | CP2·CP3·컴파일 검사 |
| 시험 환경을 다음 시험에 오염시키지 않기 | 사전조건을 실제로 확인하고 변경 상태를 복원·확인 | CP3·시험 코드·결과 분류 |
| 실행하지 않은 일을 성공이라고 보고하지 않기 | 실패·미실행·제외·환경 점검을 구분하고 증거와 집계를 대조 | execute·Agent 4·CP4 |
| 사람 동의 없이 공식 기준을 바꾸지 않기 | 통과는 등록 자격일 뿐, SRS·공식 TC는 별도 사람 승인 | 승인 서버·현재 증거/해시 확인 |

**공통 토대:** 단계마다 같은 입력·TC·코드·증거인지 SHA-256과 계약 버전으로 확인합니다. 이 확인이 빠지면 위 여섯 보호가 다른 파일의 결과를 근거로 판단할 수 있습니다. 해시는 내용의 동일성을 확인하지 의미적 정답이나 외부 공격자에 대한 서명을 보장하지는 않습니다.

### 중요도와 규칙 종류를 섞지 않기

| 분류 | 의미 | 정리할 때의 원칙 |
|---|---|---|
| 핵심 보호 | 잘못된 시험·잘못된 등록을 막는 조건 | 근거 없이 제거하지 않음. 코드·모델·사람 중 실제 책임자를 명시 |
| 실행 지원 | 허용 행동·읽기 방식·현재 중앙 관제 지원 범위 | 제품 요구의 옳고 그름과 구분. 미지원은 제품 결함이 아님 |
| 운영 보호 | 타임아웃·파일 경로·중복 실행·보고 전송·오류 기록 | 정상 기능처럼 보이지 않아도 실패 시 필요 |
| 작성 지침 | 읽기 쉬운 상세 TC, 문장 분할, 설명 방식 | 프롬프트 안내와 실제 차단 조건을 구분. 모든 안내가 코드 검사인 것은 아님 |
| 과거 호환 | 이전 Run을 당시 계약대로 읽는 조건 | 새 실행의 규칙으로 설명하지 않음. 과거 증거를 지우며 단순화하지 않음 |

### PASS가 보여도 같은 뜻이 아니다

| 표시/결정 | 실제 의미 | 다음 처리 |
|---|---|---|
| CP1 통과 / 인계 CONTINUE | 분석 검사를 통과하고 다음 설계가 가능 | Agent 2. 부분 진행이면 미정 항목을 보존 |
| CP1 REVIEW + CONTINUE | 확정 범위는 진행 가능하지만 마지막 확인 사항이 남음 | 확정 범위 설계, 질문·미정 항목을 보고에 유지 |
| CP1 범위 REVIEW 또는 WAITING_FOR_USER | 검사 범위/입력을 확정할 수 없음 | PAUSE. 입력 보완 후 새 Run; 중단 지점 자동 재개 아님 |
| 공통 근거 검토 REVIEW | 내용의 근거/검사 연결을 확신할 수 없음 | 해당 단계/후보 인계 중단. 수정 가능한 문제와 함께 있어도 추측 재작성하지 않음 |
| CP2 REVIEW | 설계에 후속 자동 진행을 막는 확인 사항이 있음 | Agent 3로 자동 인계하지 않음 |
| CP3 PASS | 계획·코드 검사가 통과 | 제품 시험 결과는 별도 확인 |
| 제품 시험 PASS | 해당 시험의 기대값·실행 조건을 만족 | 공식 승인 아님. 남은 제외·미정 범위와 증거 확인 |
| 제품 기대값 불일치 | 기대값과 관찰값이 다름 | 유효한 실패 증거이면 Agent 4로 전달. 기대값을 바꿔 통과시키지 않음 |
| CP4 PASS | 보고의 출처·증거·집계가 일치 | 제품 실패가 있는 보고도 CP4는 통과 가능 |
| 최종 권고 PASS | 현재 실행 범위·증거·남은 확인 항목 기준으로 등록 검토 가능 | 사람의 별도 승인 필요 |
| HUMAN_REVIEW / HOLD | 사람 판단 필요 / 기술·증거 문제 등으로 보류 | 승인 자격과 구분. 조치 후 필요한 검증 수행 |
| 공식 승인 | 사람이 동의하고 서버 등록 조건까지 확인 | SRS·TC 반영 또는 SRS만 반영, 출처·판단 기록 보존 |

### 반복 처리의 경계

| 상황 | 처리와 범위 |
|---|---|
| A1·A2·A3 생성 결과의 자동 검사 FAIL | 각 단계/후보의 기존 1회 재작성 범위에서 수정. 수정본도 재검사 |
| 구조 검사 FAIL | 해당 시도의 추가 모델 근거 검토는 생략. 검토 생략을 PASS로 간주하지 않음 |
| 모델 근거 검토 UNSUPPORTED, 인용/대상 연결 오류 | FAIL 피드백. 남은 재작성 기회가 없으면 중단 |
| 모델 근거 검토 UNCERTAIN | REVIEW로 해당 인계 중단. 답을 추측하는 재작성 금지 |
| 모델 응답 누락·호출 오류 | ERROR 기록. 생성/검토 SDK 자동 재시도 0회. 통과로 대체하지 않음 |
| 컴파일 정적 검사 실패·제품 시험 실패 | 위 생성 재작성 분기와 별개. 이 실패 뒤 무제한 코드 수정/재실행 없음 |
| 한 A3 후보 미지원/검토 필요 | 후보 제외와 이유를 기록. 다른 독립 후보 및 후속 보고 가능. 내부 오류는 ERROR로 별도 집계 |
| 시험 시간 초과 | 프로세스 트리 종료·증거/오류 기록. 제품 성공으로 집계하지 않음 |
| API 총 호출 제한 | 생성과 검토를 모두 세어야 함. 현재 CLI의 보편적인 전역 예산 기능으로 오해하지 않음; 승인 실행 래퍼의 한도와 별개 |

### 전체 Checkpoint 규칙 목록

아래는 번호를 기준으로 묶은 업무 규칙입니다. 한 번호 안에 여러 조건문이 있고, 동일 번호가 성공/실패 경로에 반복될 수 있습니다. 현재/과거 적용 조건은 색인의 `guards`와 소스의 호출 옵션에서 확인합니다. **표의 항목 수는 검사 실행 횟수도 분기 테스트 커버리지도 아닙니다.**

<details>
<summary>Agent 1 · 요청을 어떤 시험 조건으로 넘기는가</summary>

구현: [qa_pipeline_agent1.py](../src/qa_pipeline_agent1.py), 실행·인계: [qa_pipeline_execution.py](../src/qa_pipeline_execution.py).

| 번호 | 확인하는 것 | 문제가 있으면 |
|---|---|---|
| CP1-001 | 입력 요청과 분석의 요청 ID | FAIL |
| CP1-002 | 대상 요구사항 ID의 입력/SRS 일치 | FAIL |
| CP1-003 | 지원 변경 유형 MODIFIED 유지 | FAIL |
| CP1-004 | 변경 전 기준이 요청과 SRS에 근거 | FAIL |
| CP1-005 | 변경 후 기준이 요청과 일치 | FAIL |
| CP1-006 | 요구사항 영향 분류의 ID·중복·대상/연관 범위·조건 연결 | FAIL |
| CP1-007 | 확정 조건 ID의 유일성·요청/SRS 원문 출처 | FAIL |
| CP1-008 | 인수 조건·변경 후 기준 누락 방지, 배경 범위의 동일 대상/전체 인용 | FAIL |
| CP1-009 | 확정 조건과 정보 부족 분리·미정 항목 보존 | 잘못 확정하면 FAIL; 남은 정보 부족은 REVIEW 가능 |
| CP1-010 | 전체/부분 진행·사용자 대기 결정과 질문의 일관성 | FAIL. 실제 인계는 decision과 handoff_status도 함께 판정 |
| CP1-011 | 대상 밖 검사 범위의 요청 근거와 연결 | 근거 오류 FAIL; 자동 확정 못하는 범위 REVIEW·PAUSE |
| CP1-012 | 제품 조건·시험 절차·제외·정보 부족의 원문 인계와 중복 역할 | FAIL |

`|`로 이어진 SRS 인용은 위치가 아니라 각 조각의 실제 출처로 확인합니다. 이것은 인용을 조합한 의미 전체가 옳다는 증명이 아닙니다. `PARTIAL_PROCEED`는 전체 요구사항 완료가 아닙니다.

</details>

<details>
<summary>Agent 2 · 기존 TC를 재사용할지, 무엇을 새로 설계할지</summary>

구현: [qa_pipeline_agent2.py](../src/qa_pipeline_agent2.py). 기본 불일치는 FAIL이며 CP2-011은 검토/진행 여부를 별도로 판단합니다.

| 번호 | 확인하는 것 |
|---|---|
| CP2-001 | 요청·Agent 1·설계의 요청 ID |
| CP2-002 | TC와 기대결과 ID·내용의 중복 |
| CP2-003 | 입력에 없는 Requirement/Condition 연결 금지 |
| CP2-004 | 모든 확정 조건의 신규 후보 또는 기존 TC 담당 연결 |
| CP2-005 | TC별 요구사항·조건·기대결과 추적 연결 |
| CP2-006 | 필요한 화면/내부 상태 이중 확인과 관찰 단계 정합성 |
| CP2-007 | 알림을 요구하는 조건의 알림 관찰 연결. 없는 알림 기능을 추가하라는 규칙 아님 |
| CP2-008 | 변경 검증 목적·중앙 관제 제어 경로·대상 연결 |
| CP2-009 | 제품 TC에 자동화 코드/실행 결과를 섞지 않기 |
| CP2-010 | 대상·온도 경계 등 실제 실행용 구조화 데이터 |
| CP2-011 | 해석이 남아 자동 진행 불가능한 검토 사항은 REVIEW; 제외·최종 확인 안내와 구분 |
| CP2-012 | 공통·도메인·기능별 QA 기준과 TC별 적용 근거 |
| CP2-013 | 다른 TC의 실행에 의존하지 않는 사전조건·독립성 |
| CP2-014 | 제외·정보 부족·절차 원문의 보존. 같은 목록의 연속 분할 허용; 누락·순서 변경 금지 |
| CP2-015 | 조건별 순차 실행·중간 초기화·복원의 그룹 구성 |
| CP2-016 | 신규 변경분과 관련 기존 회귀 선택 분리. 기존 TC를 불필요하게 복제하지 않기 |
| CP2-017 | 연결 조건·관찰 대상·명시 코드/수치의 근거. 과거 단어 의미 추정은 새 실행에서 제외 |
| CP2-018 | 실제 변경이 필요한 SRS만 개정 제안. 이미 반영된 동일 문장 재제안·필요 개정 누락 방지 |
| CP2-019 | 재사용 TC의 실제 카탈로그 동작과 조건별 코드/수치 근거 |
| CP2-020 | 기대결과의 확인 대상·유일한 판정 단계 연결. 새 실행은 특정 상세 문체를 강제하지 않음 |
| CP2-021 | **과거 문장 상세화 검사. 새 실행에서는 비활성**. 상세 작성 예시는 프롬프트에 유지 |
| CP2-022 | 조회/값 변경/변경 차단 유형과 복원 필요 여부 |
| CP2-023 | 복원 조작·확인 ER·관찰 기준·순서·시점의 구조화 계약 |

참고용 SRS 연결만으로 새 이중 검증을 강제하지 않습니다. 직접 상태 정합성을 요구하거나 REQUIRED 정책이면 계속 필요합니다. 기존 TC 선택만으로 해당 조건을 검증했다고 간주하지 않고 담당 조건 ID를 대조합니다. SRS 개정 불필요와 시험 불필요는 다른 판단입니다.

</details>

<details>
<summary>Agent 3 · TC를 실제로 실행할 수 있고, 같은 것을 검사하는가</summary>

구현: [qa_pipeline_agent3.py](../src/qa_pipeline_agent3.py), 시험 실행: [qa_pipeline_execution.py](../src/qa_pipeline_execution.py).

| 번호 | 확인하는 것 | 범위/처리 |
|---|---|---|
| CP3-000 | 현재 허용 행동/읽기 방식으로 구현 가능한가 | 지원 확장 필요는 REVIEW. 요구사항이 틀렸다는 뜻 아님 |
| CP3-001 | 승인된 TC ID·대상·중앙 관제 경로 | 불일치 FAIL |
| CP3-002 | Action ID·관찰된 선택자·허용 행동 | 불일치 FAIL |
| CP3-003 | 모든 ER과 Assertion의 정확히 1:1 연결 | 누락·중복 FAIL |
| CP3-003A | 조건별 확인 시점을 실제 조작 순서에 연결 | 빠른/잘못된 시점 FAIL |
| CP3-004 | UI/내부값 관찰 계층·읽기 구현·기대값 근거 | 불일치 FAIL. 단어 겹침으로 대상 의미를 증명하지 않음 |
| CP3-005 | TC의 모드·온도 값 보존 | 값 변경 FAIL |
| CP3-006 | 초기 상태를 되돌릴 복원 Action 연결 | 누락·다른 복원 FAIL |
| CP3-006A · 사전조건 | TC 사전조건별 실제 읽기·기대값·TEST 이전 확인 | 연결 부재 FAIL |
| CP3-006A · 조작 순서 | 준비·선택·설정·적용·중간 초기화의 TC 순서 | 누락·순서 변경 FAIL. 위 항목과 같은 ID를 사용 중 |
| CP3-006B | 복원 확인 대상·원래 관찰값 기준·복원 후 시점 | 누락/기준 변형 FAIL |
| CP3-006C | 조회/변경/차단 유형에 맞는 시험·복원 전략 | 정책 불일치 FAIL |
| CP3-007 | 생성 Python 문법 | 실패 시 시험하지 않음 |
| CP3-008 | 금지 실행·파일 변경·외부 호출·검사 우회 등 정적 확인 | 실패 시 시험하지 않음. OS 보안 격리 보장 아님 |
| CP3-009 | ER마다 생성 코드의 검사 연결 표시 존재 | 누락 FAIL. 표시 존재만으로 의미 전체를 증명하지 않음 |

READ_ONLY에서 마지막 조회가 실제 Assertion이면 확인용 가짜 클릭을 요구하지 않는 제한적 지원이 있습니다. 실제 조작 누락이나 중간 검사까지 허용하는 예외는 아닙니다. 모델은 계획을 만들며 Python은 허용 목록 컴파일러가 생성합니다.

</details>

<details>
<summary>공통 모델 검토 · 단어가 아니라 근거와 의미를 대조</summary>

구현: [qa_pipeline_grounding.py](../src/qa_pipeline_grounding.py). `AGENT1-GROUNDING`, `AGENT2-GROUNDING`, `AGENT3-GROUNDING`이 각 단계 CP 결과에 추가됩니다.

| 구분 | 검사/판단 | 결과 |
|---|---|---|
| 검토 대상 | 코드가 정한 전체 항목의 ID·순서·누락·중복 | 오류 FAIL |
| 원문 연결 | source_id 존재·인용문 실제 존재·SUPPORTED의 인용 필수 | 오류 FAIL. 인용이 있다는 사실만으로 의미는 증명되지 않음 |
| 의미 판단 | 요청에 없는 기능·기대값 추가, 원문과 다른 해석, A3 검사 누락 | 모델이 UNSUPPORTED면 FAIL |
| 기대결과 단위 | 한 대상·한 시점의 독립 사실인지 모델이 검토 | 여러 독립 사실이면 보존하며 분리하도록 FAIL |
| 불확실 | 모델이 UNCERTAIN으로 남긴 항목 | REVIEW 우선. 다른 수정 가능 오류도 함께 기록 |
| 검토 무결성 | 단계·입력·산출물 해시·계약·최종 검토 파일 해시 | 불일치/누락 인계 거부 |
| 실제 실행 | 구조 검사 이후 검토, 재작성본도 새 검토 | 검토 ERROR/REVIEW를 성공으로 대체하지 않음 |
| 미구현 기능 | 요청에는 있지만 현재 UI에는 없는 기능 | 근거 없는 생성과 구분. 자동화 지원/제품 시험 단계에서 처리 |

이 모델 검토는 오판할 수 있습니다. 대역 응답 테스트는 분기 연결을 확인할 뿐 실제 판단 정확성을 입증하지 않습니다. 이 절의 순수 코드 검사와 모델 판단, 프롬프트 작성 지침을 하나의 확정적 검사기로 설명하지 않습니다.

</details>

<details>
<summary>실행·복원·Agent 4 · 무슨 일이 실제로 일어났는가</summary>

구현: [qa_pipeline_execution.py](../src/qa_pipeline_execution.py), [qa_pipeline_reporting.py](../src/qa_pipeline_reporting.py), [qa_pipeline_orchestrator.py](../src/qa_pipeline_orchestrator.py).

| 결정 | 조건과 처리 |
|---|---|
| 후보 선택 | AUTO는 실행 가능한 신규 후보 모두. 명시 TC 선택 시 나머지를 미선택 사유와 함께 기록 |
| 기존 TC만 필요 | A3 새 계획 생성을 건너뛰고 관련 기존 TC 실행으로 진행. 신규 후보 없음 자체는 오류 아님 |
| 후보 전부 제외 | 제외 사유를 보존하고 환경 점검·선택 기존 TC·보고 가능. 요청 전체 성공 아님 |
| Inventory | 선택자·표시·활성·하네스 확인. 제품 동작 성공으로 취급하지 않음 |
| 사전조건 불일치 | 본 시험을 시작하지 않고 자동화/준비 문제로 보고 |
| 조회 TC | 복원 불필요. 불필요한 변경 Action을 추가하지 않음 |
| 값 변경 TC | 준비 전/시험 전 기준을 계약에 맞게 관찰, 필요한 상태 복원 후 비교 |
| 변경 차단 TC | 준비 상태가 유지됐는지 확인. 실제 변경됐으면 복원; 준비 과정의 변경도 정리 |
| 복원 실패 | 관찰 기록 보존, 같은 브라우저 환경에서 계속 시험하지 않음. 다른 TC는 분리 환경 |
| 코드 재사용 | 인계/현재 계획 검사 후 현재 컴파일 출력과 같으면 저장 시험 재사용, 다르면 별도 재컴파일·재시험 |
| 환경 점검 | TC-ENV-000 실패 시 기존 회귀 중단. 환경 건수를 제품 성공률에 합치지 않음 |
| 기존 회귀 | Agent 2가 선택한 기존 TC만 중복 없이 실행. 승인 TC의 코드/명세 해시도 확인 |
| 실패 분류 우선순위 | 환경 오류 → 사전조건 불일치 → 복원 실패 → 제품 기대값 불일치 → 기술 오류/시간 초과 → 미실행. 정상 PASS는 문제 분류 없음 |
| 제품 불일치+복원 실패 | 복원 문제로 보류하되 제품 불일치 관찰도 함께 보존 |
| 최종 권고 | CP4 실패/자동화·환경·증거 부족·미실행은 HOLD. 제품 불일치는 HUMAN_REVIEW. 그 외 PASS 후보도 제외·정보 부족이 남으면 HUMAN_REVIEW |

| 번호 | 보고 전에 확인하는 것 |
|---|---|
| CP4-001 | Run ID 일치 |
| CP4-002 | 검증 실행·Agent 3·후보·제품·기존 코드의 해시 연결 |
| CP4-003 | 실행 결과 TC 중복 없음 |
| CP4-004 | 실행 출처·원래 결과·중립 상태·종료 코드 등 상태 계약 |
| CP4-005 | 선택된 회귀와 실제 실행·전체 실행 단계 상태 일치 |
| CP4-006 | 증거의 존재·허용 경로·해시, 신규 후보의 로그·Screenshot·Trace |
| CP4-007 | 제품 TC·환경 점검·고정 사례 집계 분리 |

CP4는 저장 결과를 검사하며 새 모델 호출/제품 재시험은 하지 않습니다. 유효한 실패 결과를 보고하는 것은 파이프라인 오작동이 아닙니다.

</details>

<details>
<summary>사람 승인·외부 보고·UI·과거 호환 · 끝부분과 공통 운영 조건</summary>

| 영역 | 조건과 분기 | 대표 구현 |
|---|---|---|
| 구조 계약 | 필수 필드·허용 상태값·MODIFIED·복원/계획 내부 정합성 위반 거부 | contracts의 StrictModel·validator·Enum |
| 최초/후속 입력 | JSON·SRS·Run ID·TC ID·파일 존재·허용 경로·스냅샷 검증 | execution의 read/load/resolve 함수 |
| 인계 | 버전·정책 필드·단계·해시·저장 CP와 재계산 CP 대조 | `_load_verified_agent1_run`, `_load_verified_agent2_run`, `_candidate_execution_record` |
| 과거 호환 | 과거 계약별 검사 옵션. 이전 PASS 안내 문구만 제한적으로 비교 제외; 판정/실패 이유 유지 | `_legacy_wording_policy`, `_checkpoint_revalidation_matches` |
| 새/과거 혼합 방지 | 새 필수 계약 누락·구버전으로 하향·수정 전 근거 검토 재사용 거부 | `_load_grounding_review` 및 버전 확인 |
| 생성 코드 | 허용 행동·Assertion·읽기/복원 방식만 생성, 런타임 사전조건·복원 로그 유지 | `compile_automation_candidate`와 관련 builder |
| 공식 등록 자격 | 최종 PASS·CP3/CP4·실제 시험 PASS·필수 증거·후보 코드·현재 제품 해시 | `_candidate_approval_check` |
| 현재 제품 변경 | 시험 뒤 HTML 변경이면 현재 제품에서 재시험 필요. 코드가 다르면 거부 | `revalidate_candidate_asset` |
| 재시험 증거 | 시작/종료의 코드·HTML 동일성 확인, 실패 시 이전 유효 기록 보존 | `revalidate_candidate_asset` |
| 승인 입력 | 검토자·근거·필요한 SRS 별도 동의·후보 연결 검증 | `_decide_candidate_asset_impl` |
| 보류 | 이유 기록, SRS·공식 TC 변경 없음. 이미 승인한 기록을 보류로 취소하지 않음 | `decide_candidate_asset` |
| 중복 승인 | 같은 Run/TC는 기존 승인 반환. 등록 오류 시 이번 변경 복구 | 승인 함수·`_restore_files_after_error` |
| 기존 TC 전용 승인 | 원문·선택 TC 근거·실행·현재 제품을 확인해 SRS_ONLY 가능. TC 신규 등록 없음 | `_existing_srs_approval_check`, `decide_existing_srs` |
| 사람 검토서 | 양식 생성과 실제 승인은 별개. 사람이 편집한 문서 덮어쓰기 금지 | `_write_human_review_document` |
| 보고 전송 | CP4/보고 원본 검증 후 payload 생성. 기본 preview, `--send`만 전송 | `run_external_reporting` |
| Slack/Notion 오류 | 설정·HTTP·응답 오류를 전송 결과로 남김. 실행/등록 성공과 구분 | `_send_slack_report`, `_upsert_notion_reports` |
| Notion 상세 TC | 검증된 상세 명세만 연결. 같은 내용 중복 생략, 수동 편집 보존, 부분 실패 기록 | `_sync_notion_tc_detail` |
| UI 권한 모드 | 공개/직접 HTML은 데모, 기본 서버는 조회. live/approval 플래그로 쓰기 허용 | PipelineUiBridge·make_handler |
| 요청 보호 | 같은 Origin·허용 Host·JSON·허용 요청 파일·Run 경로만 허용 | `do_POST`, `request_path` |
| 동시성 | Live 파일 잠금, 승인 직렬화, 오류 시 잠금 해제 | LiveRunFileLock·PipelineUiBridge |
| 화면 상태 | Run 전환 시 승인 입력 초기화·늦은 응답 무시·연결 실패 별도 표시 | virtual-controller.html의 QA 패널 script |
| 중간 성공 표시 | 최종 보고 전에는 전체 성공으로 표시하지 않음. 유효한 환경 실패는 A4 보고 가능 | `_run_pipeline`, `summarize_run` |
| 민감정보/경로 | 전송 입력 제한·실행 출력/Trace 로컬 경로 정리·증거 경로 제한 | 모델 입력 builder·io·trace·실행/보고 검사 |
| 촬영 보조 | 저장 결과 조회용. 실제 API·공식 승인·게시 금지, 이미 등록된 Run 미리보기 조건 확인 | scripts/record_demo.py |

이 표는 운영 규칙의 이름표입니다. 모든 `if`를 독립된 QA 요구로 늘려 부르지 않습니다. 낮은 수준의 문자열 파싱·표시 형식·HTTP 오류 분기까지는 아래 색인의 함수/행 번호에서 확인합니다.

</details>

### 누락과 문서 노후화를 확인하는 방법

1. [정적 색인](logic_inventory.json)은 전체 `src/*.py`의 함수/클래스, if/삼항식/불리언 단락 평가, 반복/내포식 필터, try/except, raise/return/break/continue, with, match, assert를 자동 추출합니다. 각 항목에 원본 행·함수·조건식·상위 if/else를 남깁니다. 스키마 선언·상태값·Checkpoint 호출·프롬프트/생성 코드 문자열 위치도 함께 찾을 수 있습니다.
2. baseline 테스트·승인 자동화·촬영 보조는 별도 파일 항목으로 포함합니다. 제품/포폴 HTML의 인라인 JavaScript와 이벤트 핸들러도 Acorn으로 파싱해 조건·반복·예외·종료·단락 평가·선택적 접근을 색인화합니다. 외부 script 라이브러리 내부는 포함하지 않습니다. 동적 생성 Python/JavaScript는 builder/문자열 위치를 연결한 것이지 모든 생성 변형을 실행한 결과가 아닙니다.
3. 색인 생성기는 소스를 파싱할 뿐 import·모델 호출·브라우저 시험·공식 승인을 하지 않습니다. 파일 해시와 색인을 다시 대조하면 코드 변경 후 목록이 낡았는지 알 수 있습니다.
4. 이 목록은 **정해진 범위의 정적 누락 방지 장치이지, 전체 경로 테스트 완료나 모든 의미 규칙의 수작업 검수 완료 주장이 아닙니다.** 테스트와 실제 API 검증 상태는 인계 문서에서 별도로 관리합니다. 외부 라이브러리 내부·프로젝트 2·과거 Run·ignored 임시 스크립트는 대상 밖입니다.

색인 갱신에는 Python 3.10+와 Node가 필요합니다. 로컬 Acorn 또는 Node에 내장된 Acorn을 사용하며 별도 다운로드/설치는 하지 않습니다. 내장 파서 경로는 Node 내부 구현이므로 배포판에 따라 없을 수 있습니다. 그 경우 JavaScript를 몰래 생략하지 않고 실패합니다. 사용한 파서와 Node 버전은 색인에 남깁니다.

```powershell
python scripts/audit_logic_inventory.py --self-test
python scripts/audit_logic_inventory.py          # 현재 소스로 기술 색인 갱신
python scripts/audit_logic_inventory.py --check  # 변경 없이 현재 소스와 대조
```

### 앞으로 정리할 우선순위 — 아직 구현 변경하지 않은 제안

- 먼저 현재 실행/과거 호환/모델 안내/실제 차단을 구분해 읽습니다. 오래된 단어 규칙이 파일에 남아 있다는 이유로 새 실행에서도 모두 작동한다고 보지 않습니다.
- CP3-006A의 두 용도는 문서에서 분리했습니다. 코드 ID 변경은 과거 증거·로더·테스트 호환성을 검토한 별도 작업이어야 합니다.
- 다음 리팩터링 후보는 여러 곳의 계약 옵션·최종 상태 계산·인계 검사를 공통 책임으로 모으는 것입니다. 같은 해시를 생성 시점과 승인 시점에 다시 확인하는 것은 필요한 경계 검사일 수 있으므로 단순 중복으로 삭제하지 않습니다.
- 최근 공통 모델 검토의 실효성은 아직 실제 호출로 평가해야 합니다. 새 규칙을 더 추가하기 전에 정상/오류/불확실 사례에서 오탐·누락·추가 호출 비용을 확인합니다.

### 현재 공통 근거 검토 계약

새 인계 버전은 **Agent 1 2.11 / Agent 2 3.11 / Agent 3 4.9**이며 `grounding_contract=1.0`을 요구합니다. 생성 프롬프트는 각각 2.17 / 2.38 / 3.33, 공통 검토 프롬프트는 grounding-1.0입니다. 아래 세부 계약의 이전 버전 설명은 각 보호 장치가 도입된 시점이며 새 버전은 그 장치를 유지합니다.

| 단계 | 별도 모델이 검토하는 내용 | 근거 |
|---|---|---|
| Agent 1 | 조건·역할·영향·절차/제외/정보부족 분류 | 최초 변경 요청·SRS |
| Agent 2 | TC 범위·사전조건·조작·기대결과·복원·기존 TC 선택·SRS 제안 | 최초 요청·SRS, 기존 TC 명세. Agent 1 분석은 참고이며 원문 대체 불가 |
| Agent 3 | 기대결과 전체와 Assertion의 대상·값·시점, 실제 조작·사전조건·복원 연결 | CP2 통과 TC·제한된 UI Inventory·생성 계획 |

코드가 검토 대상을 열거하고 모델은 모든 항목에 `SUPPORTED / UNSUPPORTED / UNCERTAIN`, 인용문과 이유를 반환합니다. `SUPPORTED`의 인용은 실제 원문에 있어야 합니다. 누락·중복·순서/ID 오류·없는 인용은 실패입니다. 기대결과의 `single_fact`는 독립된 사실 하나인지 모델이 판단한 값이며 마침표/단어 규칙이 아닙니다. 여러 사실을 묶은 기대결과는 기존 요구를 보존해 분리하도록 반환합니다. 코드가 인용의 의미적 지지 여부 자체를 증명하는 것은 아닙니다.

생성·검토 클라이언트의 SDK 전송 자동 재시도는 모두 0회입니다. 명시적인 단계 재작성만 사용합니다. 외부에서 지정한 총 호출 예산은 생성뿐 아니라 검토 전송도 함께 계수해야 하며 예산 소진을 통과로 처리하지 않습니다.

구조 검사 실패 시 검토 API는 생략합니다. 근거 부족/연결 실패는 기존 1회 재작성의 피드백으로 전달하고 수정본도 다시 검토합니다. 불확실은 REVIEW로 중단하며, Agent 1의 부분 진행 REVIEW와 달리 근거 검토의 REVIEW는 다음 단계 진행을 허용하지 않습니다. 통신/응답 오류는 ERROR 경로로 남기며 통과로 대체하거나 검토기를 자동 재호출하지 않습니다. 생성 시도와 별도로 검토 모델·응답 ID·사용량을 기록하고 총 사용량에 합산합니다.

`agentN_grounding_input_attempt_K.json`은 전송할 데이터 미리보기, `agentN_grounding_review_attempt_K.json`과 `agentN_grounding_review.json`은 시도별/최종 검토 기록입니다. Manifest는 최종 검토 파일 SHA-256을 보존합니다. 입력 전체와 생성 산출물의 해시를 묶어 수정 전 검토 재사용을 차단하고, 후속 인계·사람 승인 경로에서 다시 확인합니다. 저장 재검사는 API를 재호출하지 않습니다. 과거 기록은 당시 계약으로 읽으며 새 검토가 있었다고 소급 표시하지 않습니다.

근거 있는 미구현 기능은 유효한 시험 요구일 수 있습니다. UI 부재만으로 요구를 삭제하지 않으며 기존 자동화 지원 분기와 실제 제품 시험 결과를 따릅니다. 모델 검토는 생성자와 별도 호출이지만 같은 계열 모델일 수 있어 독립적인 정답 보장이 아닙니다. 새 검토의 실제 정확도·정상 표현 오탐·일관성은 별도 Live 평가가 필요합니다. 후보 1개 기준 최대 생성 6회+검토 6회이며, 모델 호출 승인 상한을 늘리지 않습니다.

공통 구현은 `src/qa_pipeline_grounding.py`에 두고 세 단계의 실행 연결은 `qa_pipeline_execution.py`에서 관리합니다. 순수 `evaluate_checkpoint1/2/3`는 기존 결정론적 코드 검사이며 단독 호출로 새 모델 검토를 실행하지 않습니다. 실제 CLI와 저장 로더는 두 결과를 합쳐 최종 Checkpoint를 판정합니다.

사용자 확인 질문은 새 검증 실행에서 Agent 1 분석 파일의 Manifest 해시를 검증한 뒤 `사용자 확인 요청:` 접두어로 기존 `최종_확인_사항`에 전달합니다. Agent 4 JSON·사람 최종 검토서·해당 필드를 보여주는 UI에서 확인할 수 있습니다. 질문 원문을 보존하고 중복만 제거하며, 답을 추정하거나 자동 승인으로 해석하지 않습니다. 기존 실행 보고서와 외부 게시물을 소급 갱신하지 않습니다.

## 1. 무엇을 하는 프로젝트인가

기존 제품 SRS와 베이직 TC가 있는 상태에서 새로운 변경 요청을 받습니다. 이미 관리하는 TC로 확인할 수 있는 조건은 재사용하고, 새로 필요한 조건만 후보 TC로 만들어 시험합니다. 결과와 근거를 보고 사람이 공식 자산으로 채택할지 판단합니다.

| 용어 | 의미 |
|---|---|
| SRS | 제품이 어떻게 동작해야 하는지 정한 요구사항 |
| TC | 사전조건·절차·기대 결과·복원을 가진 테스트케이스 |
| 기존/베이직 TC | V1 기준 TC와 사람이 승인한 V2 공식 TC |
| 후보 TC | 이번 변경을 위해 만든 TC. 시험 통과 후에도 공식 승인 전에는 후보 |
| 관련 회귀 | 이번 변경과 관련 있고 여전히 유효한 기존 TC의 재실행 |
| Checkpoint(CP) | Agent 산출물의 구조·근거·값·인계를 검사하는 자동 단계 |
| Run | 변경 요청 한 번의 입력·분석·TC·시험·보고 묶음 |
| 후보 시험(Candidate Trial) | 생성한 자동화 코드를 별도 임시 위치에서 실제 실행하는 과정 |

```text
기존 SRS + 기존 TC + 변경 요청
  → Agent 1 / CP1: 확정 조건·정보 부족 분리
  → Agent 2 / CP2: 기존 TC 대조·변경분 후보 설계·SRS 개정 제안
  → Agent 3 / CP3: 후보별 UI 확인·AI 계획·컴파일·시험
  → 변경 검증: 완료 후보 확인·환경 점검·선택한 기존 TC 실행
  → Agent 4 / CP4: 원인 분류·증거/집계 확인·보고
  → 사람: SRS 개정·공식 TC 등록 승인 또는 보류
  → 승인한 자산을 다음 Run에서 기존 TC로 대조
```

Agent 1·2는 모델이 구조화 JSON을 작성합니다. Agent 3는 모델이 동작·검증 계획을 작성하고 컴파일러가 Playwright Python을 생성합니다. Agent 4는 생성형 AI를 사용하지 않습니다.

## 2. 시연 시나리오

| 사례 | 처리 | 확인할 장면 |
|---|---|---|
| 정상 변경·관련 기존 TC 재사용 | MED 적용 후 카드의 중풍 표시와 내부 fanSpeed=MED를 확인, 관련 기존 HIGH TC 선택 결과를 함께 확인 | 신규 TC → 자동화 → 실제 시험, 기존 TC 재사용 → 분류 |
| 모드별 온도 범위 | AUTO 18~30°C, COOL·HEAT 16~30°C, FAN·DRY 온도 비활성화 요청 | 조건·경계값 설계와 기대/관찰 비교 |
| 제품 불일치 | 잠금 시 온도 버튼 비활성화 요구와 실제 활성 상태를 비교 | 실패 증거와 제품 불일치 후보 분류 |
| 정상 변경의 후속 승인 | PASS 후보와 SRS 개정안을 사람이 검토 | 공식 등록 또는 보류, 이후 기존 TC 재사용 |

위 기능명·값은 시연용 예시입니다. 요구사항을 생성 규칙에 고정하지 않습니다. 정상 요청에는 MED 변경만 요청하는 입력과 HIGH 유지 조건을 포함한 입력이 있습니다. 기존 TC 선택 여부는 해당 Run의 실제 결과로 확인하며 항상 HIGH를 실행한다고 고정하지 않습니다. 짧은 소개 영상은 정상 변경·사람 승인, 제품 불일치, 미정 조건 분리의 세 사례로 구성하며 온도 범위는 추가 자료로 유지합니다.

잠금 실패 사례는 실패 증거로 보존합니다. 제품을 고치거나 기대값을 바꿔 이 결과를 PASS로 만드는 자동 절차는 없습니다. 제품 TC가 실패해도 그 실패의 증거와 집계가 유효하면 CP4는 통과할 수 있습니다.

영상은 정상 변경·관련 TC 재사용·사람의 SRS/TC 등록을 먼저 보여주고, 제품 불일치와 미정 조건 사례를 비교합니다. 로컬 실제 실행 영상의 대기 구간을 편집하면 표시합니다. 공개 HTML의 저장 데모, 실제 API 실행, 저장된 실제 결과 조회를 구분합니다. 루트 project.html은 세로 스크롤형 정적 소개 페이지이며, 실행 결과에는 풍량·잠금·미정 조건이 남은 사례를, 분석·보고 이후에는 실제 자산 등록 상태를 보여줍니다. 초기 온도 시험은 별도 추가 사례로 유지합니다. V1 원본은 보존하며, 사용자 요청으로 V2 소개에 V1의 표현 형식을 참고했습니다. 영상 미촬영 상태를 표시하고 당시 공개 증거와 최신 로컬 실행을 구분합니다.

### 90초 촬영 대본과 편집 순서

목표는 **정상 결과만 나열하는 것이 아니라, 등록 가능한 결과·실패·미확인 범위를 구분하는 QA 흐름**을 보여주는 것입니다. 전체를 실시간으로 90초 안에 실행한다는 뜻이 아닙니다. 아래 타임라인은 저장된 실제 실행 증거 조회와 촬영 시점의 실제 사람 승인을 편집하는 구성입니다. 원본 녹화는 각 장면 앞뒤 여유를 두고 길게 남깁니다. 내레이션 자막은 [90초 한국어 SRT](demo/demo-90s.ko.srt)에 있습니다. 아직 녹화·편집한 영상은 아닙니다.

| 시간 | 화면·조작 | 내레이션 |
|---|---|---|
| 00:00–00:06 | 제목: 변경 요청에서 검증과 공식 등록까지. 실제 결과 조회 표시 | AI가 만든 테스트를 그대로 믿지 않고, 근거와 실행 결과를 확인하는 QA 파이프라인입니다. |
| 00:06–00:14 | 정상 Run → Agent 1. MED 변경/HIGH 유지 요청과 확정 조건만 확대 | 중풍 표시 변경을 요청하면, AI가 변경할 조건과 유지할 조건을 정리합니다. |
| 00:14–00:24 | Agent 2 → 신규 TC 펼침. 선택→설정→적용, 기대결과·복원 순서 강조 | 기존 테스트와 비교해 필요한 TC를 만들고, 조작 순서와 확인 대상, 복원 방법을 구체화합니다. |
| 00:24–00:32 | Agent 3 → 저장 시험 증거. 화면 중풍·내부 MED, 복원 로그 | 검사된 계획을 코드로 변환해 시험합니다. 화면과 내부 값, 시험 뒤 복원 결과까지 확인합니다. |
| 00:32–00:39 | Agent 4의 제품 2건+환경 1건 결과 → Notion 상세 짧게 전환 | 새 중풍 시험과 기존 강풍 테스트를 함께 확인하고, 상세 TC와 결과를 보고합니다. |
| 00:39–00:51 | 원본 정상 Run의 승인 패널. 검토자·근거 입력, SRS 동의, 승인→최종 확인. 등록 완료와 실제 SRS diff/Registry 화면으로 컷 | 마지막은 사람의 판단입니다. 변경 문구와 증거를 검토해 승인하면, SRS에 반영되고 공식 TC와 자동화가 등록됩니다. |
| 00:51–00:59 | 실패 Run의 잠금 요구사항·TC 기대결과. 당시 실행 기록 표시 | 잠금 뒤 온도 버튼이 비활성화돼야 하는 요청에서는, 실제로 버튼이 활성 상태였습니다. |
| 00:59–01:10 | 실패 행·관찰 로그·제품 불일치 후보 분류. 공식 등록 불가 | 이 결과는 실패와 제품 불일치 후보로 남깁니다. 기대값을 바꿔 통과시키지 않고, 사람에게 검토를 요청합니다. |
| 01:10–01:19 | 미정 조건 Run → Agent 1. 알림 색상 미정 원문과 확정 조건 분리 | 반대로 알림 색상이 정해지지 않았다면, 임의의 색상을 기대값으로 만들지 않습니다. |
| 01:19–01:26 | 실행 3건 통과 + 정보 부족 1건 + 최종 사람 검토를 한 화면에 | 확정된 조건의 시험이 통과해도 미정 항목을 남기고, 최종 판단은 사람 검토로 보냅니다. |
| 01:26–01:30 | 마무리: 근거 있는 기대값 · 실제 실행 증거 · 사람의 공식 승인 | 테스트 생성부터 검증, 보고, 공식 등록까지 연결했습니다. |

#### 승인 전 영상 미리보기 생성

V1의 녹화 원리를 참고한 V2 전용 `scripts/record_demo.py`는 로컬 브리지에서 저장된 실제 Run을 읽고 Playwright 녹화와 FFmpeg 편집으로 무음 자막 영상을 만듭니다. V1 스크립트·영상·고정 결과를 복사하거나 실행하지 않습니다. OBS는 필수가 아니며 `imageio-ffmpeg` 패키지의 FFmpeg를 사용합니다.

```powershell
python -m pip install ".[agent3,video]"
python scripts/record_demo.py --smoke  # 4초 시험 녹화
python scripts/record_demo.py          # 90초 승인 전 미리보기
```

출력은 새 `runs/manual_checks/video-preview-.../` 폴더에만 생성되며 기존 폴더를 덮어쓰지 않습니다. 원본 화면은 수정하지 않고 촬영 세션에서 글자 확대와 설명 자막만 적용합니다. 각 장면의 준비 대기 구간을 잘라 합친 편집 영상이며 실시간 전체 실행이 아닙니다. 선택한 Run의 본문 ID가 일치한 뒤 녹화하고, 원본 증거·SRS·공식 자산 해시 보존과 MP4 디코딩/길이를 검사합니다.

스크립트는 현재 로컬 브리지의 GET 조회만 허용하며 새 API 실행·외부 접속·승인/보류 POST를 차단합니다. 원본 MED가 미등록인 상태의 **승인 전 미리보기**만 생성합니다. 위 최종본 대본의 39~51초 승인 완료 구간은 대신 미등록 검토 화면으로 표시하고 마지막 자막도 원본 불변으로 바꿉니다. 이 미리보기를 실제 공식 등록 완료 영상으로 게시하지 않습니다. 실제 승인 장면은 사용자 승인과 별도 녹화 후 교체해야 합니다.

원본이 이미 등록됐다면 이 도구는 중단합니다. 승인 전 상태를 재연하기 위해 SRS·Registry를 되돌리지 않습니다. 사용자의 명시적 지시에 따라 대행 등록할 때에는 검토자·메모에 승인 주체와 대행 사실을 구분해 기록하며, 해당 지시를 다른 후보의 포괄 승인으로 사용하지 않습니다.

공식 자산 파일은 Registry의 SHA-256과 연결되므로 Git에서도 줄바꿈을 변환하지 않고 원래 바이트를 보존합니다. 파일 내용을 편집하거나 포맷팅한 뒤 해시만 맞추는 방식으로 승인 증거를 갱신하지 않습니다.

이 도구의 촬영 범위는 로컬 Run 요약·상세 TC·실행 결과·승인 전 문구입니다. 별도 제품 시험 브라우저의 움직임이나 실제 Notion 페이지 방문을 녹화한 것은 아닙니다. 최종 대본의 해당 장면은 저장된 시험 증거/승인된 외부 화면 자료와 별도로 편집해야 합니다. 미리보기의 페이지 오류·파일 무결성 검사는 최종 영상의 전체 내용 검수를 대신하지 않습니다.

#### 실제 클릭과 녹화 체크리스트

- 정상 사례는 **조회할 Run** 선택 후 본문 Run ID가 같은 값으로 바뀐 것을 확인합니다. 전환 대기 중 이전 결과를 촬영하지 않습니다. Agent 버튼으로 단계를 바꾸고 TC ID를 눌러 상세를 펼칩니다. UI는 실제 장비 조작을 중계하지 않으므로 실행 중 장면은 저장된 Trial 증거나 별도 브라우저 원본 녹화로 보여줍니다.
- 등록 전: 원본 SRS·Registry·승인 대상 Run의 백업과 해시를 남기고, 최신 자동 등록 조건이 충족됐는지 다시 확인합니다. 검토자는 본인 이름과 실제 검토 근거를 입력합니다. **SRS 동의 → 공식 TC·자동화 등록 승인 → 확인: 공식 자산 등록**의 두 번 클릭을 녹화합니다. 앞선 리허설은 임시 사본이며 실제 공식 등록으로 소개하지 않습니다.
- 등록 후: 화면의 공식 TC ID, `approved_assets/registry.json`, 해당 TC JSON과 자동화 파일, SRS의 MED 문구 추가를 확인합니다. 공식 ID는 화면에 나온 실제 값을 사용하며 번호를 미리 고정하지 않습니다. 등록 후 새로고침에서도 완료 상태가 유지돼야 합니다. 단순 JSON 문서 생성이나 Notion 게시를 공식 승인으로 표현하지 않습니다.
- 실패·미정 사례는 **당시 실제 실행 기록 조회**라고 표시합니다. 현재 검사 계약/제품과 차이가 있다는 승인 경고를 숨기거나 과거 Run을 최신 모델 실행처럼 설명하지 않습니다. 실패 결과와 미정 조건은 서로 다른 이유의 사람 검토이며, CP4 PASS는 제품 시험 PASS가 아니라 보고 일관성 검사입니다.
- 편집: 1920×1080 기준 본문 중심으로 확대하고 한 컷의 강조는 1~2개만 둡니다. 긴 JSON 전체나 모델 대기 시간을 90초에 억지로 넣지 않습니다. 실제 승인 쓰기가 늦어지면 완료 시점으로 컷하되 ‘처리 대기 구간 편집’ 표기를 둡니다. API 키·`.env`·로컬 사용자 경로·다른 Notion 페이지는 녹화하지 않습니다.
- 촬영 종료 뒤 영상 길이·자막 겹침·수치/Run ID·승인 후 파일 일치·민감정보를 검수하고 포트폴리오에 영상 링크를 연결합니다. 등록은 자동 원복되지 않습니다. 재촬영을 위해 공식 기준을 임의로 되돌리거나 과거 실패를 덮어쓰지 않습니다.

## 3. 공통 QA 기준과 TC 구성

| 기준 | TC에 담는 내용 |
|---|---|
| 1단계 공통 QA | 해당 규칙의 정상·예외·경계·복구 관점 |
| 2단계 도메인 QA | 올바른 장비 선택, 화면·내부 상태 정합성 |
| 3단계 기능 기준 | 요구사항에 명시된 값·동작·표시 |
| 추적성 | Requirement·확정 조건·단계·기대 결과의 연결 |
| 독립성 | 다른 TC에 의존하지 않는 준비·초기 상태·복원 |
| 이중 검증 | 필요한 경우 같은 동작 직후 화면과 내부 상태 확인 |

하나의 TC는 하나의 관제점에서 하나의 업무 규칙을 검증합니다. 같은 범위의 하한·상한이나 같은 비활성 규칙의 여러 모드는 한 TC로 묶을 수 있습니다. 무관한 요구사항·제어 경로·실패 원인은 분리합니다.

| 조건 구성 | 계약 | 실행 방식 |
|---|---|---|
| 단일 흐름 | SINGLE_FLOW | 한 흐름의 동작과 판정 |
| 독립 입력 조건 | INDEPENDENT_VARIANTS | 조건 사이 재준비·초기화 기록 |
| 순차 전환 자체가 목적 | SEQUENTIAL_TRANSITION | 승인한 전환 순서 유지 |

관찰값 하나마다 기대 결과를 나누고 `verify_after_step`으로 판정 위치를 지정합니다. 화면 표시와 내부 값이라는 이유로 같은 조작을 두 번 반복하지 않습니다. Agent 3는 `after_action_id`로 해당 동작 직후 검증을 배치합니다. REQUIRED 이중 검증은 두 관찰 계층의 판정 단계 집합이 일치해야 합니다. 두 계층 모두 판정 단계가 없는 단일 흐름은 기존 종료 시점 검증을 유지합니다.

CP는 구조·명시 값·연결 관계를 검사합니다. 자연어 의미가 완전히 같은지, 모든 누락을 탐지했는지, 기준에 적은 모든 관점을 시험했는지까지 보장하는 것은 아닙니다.

## 4. Agent 1: 요구사항 분석과 CP1

변경 요청과 관련 SRS에서 변경 전·후 값, Requirement 영향, 출처가 있는 확정 조건, 제외 범위, 정보 부족을 만듭니다. 조건 역할은 변경·유지·보조 근거로 구분합니다. 현재 자동화 지원 여부를 이유로 제품 영향도를 낮추거나 명확한 조건을 버리지 않습니다.

CP1은 요청 ID·변경 유형·Requirement·값 보존, 요청/SRS 근거, 인수 조건의 전달과 진행 판정 일관성을 검사합니다.

새 실행의 CP1-011은 대상 외 VERIFY·UPDATE_REQUIRED에 `scope_evidence`를 요구합니다. 요청에서 나온 긍정 확정 조건 ID, 해당 연관 SRS 원문, 직접 요청/간접 영향 구분을 보존하며 영향 경로는 기존 `reason`에 설명합니다. SRS에 존재한다는 사실이나 같은 화면에서 실행한다는 이유만으로 범위를 추가하지 않습니다. 근거가 없는 항목은 NO_IMPACT로 검토 기록만 남기되, 명시된 요청 조건을 지워 검사를 피해서는 안 됩니다.

- 근거 누락·허위 출처·준비/제외/변경 전 문구를 근거로 사용: FAIL → 기존 제한 재작성 1회 → 미해결 시 차단.
- 직접 요청: 연결된 요청 원문에 연관 Requirement ID 또는 그 SRS 요구사항/인수 기준 전체 문장이 있는 경우 자동 대조합니다. 단순 공통 단어나 모델의 Requirement 연결만으로 직접 요청임을 인정하지 않습니다.
- 요청의 근거 연결만 수행: `VERIFY + REQUEST_TRACE_ONLY`는 요청에 이미 있는 제품 판정 조건만 연결합니다. 연결된 조건 ID와 근거 목록은 정확히 같아야 하며 CHANGE_REQUEST 출처를 유지합니다. 설명·기대결과 문장 전체의 원문 일치는 새 정책에서 요구하지 않습니다. 추가 SRS 조건·새 기대값·UPDATE_REQUIRED를 허용하거나 관련 SRS 전체 검증을 뜻하는 것은 아닙니다.
- 요청 밖 간접 영향 또는 직접 요청의 다른 자연어 표현: 근거를 기록해도 CP1-011 REVIEW·PAUSE로 범위 확인을 기다립니다. 현재 규칙은 인과관계나 모든 바꿔 쓴 표현을 자동 판독하지 않습니다. 필요한 범위를 요청에 명시해 새 Run으로 확인하며, 자동 통과를 위해 요구사항을 축소하지 않습니다.

새 Agent 1 Manifest 2.10은 `scope_guard_contract=1.1`, `input_routing_contract=1.0`, `wording_policy=STRUCTURAL_ONLY_V1`, `srs_quote_contract=1.0`, prompt 2.17을 저장합니다. 작성 지침은 전달 목록의 `/`가 모두 복사하라는 뜻이 아닌 역할 선택지임을 명시합니다. 제품 조건과 절차를 중복하지 않되, 분리 가능한 정보 부족의 세 목록 보존 계약은 유지합니다. 이 지침은 모델의 올바른 분류를 보장하지 않습니다. 과거 Run은 당시 검사 결과와 해시를 유지하며 새 정책 누락·알 수 없는 정책·버전 불일치는 거부합니다. 과거 결과를 새 분류로 바꾸지 않습니다. 기존 공식 TC 원문은 수정하지 않습니다.

새 정책은 분석·설계의 상태 단어·경계 표현 비교와 특정 유지 표현 요구를 차단 기준에서 제외합니다. 근거 밖 수치, 코드·수치의 부분 문자열 인용, Requirement별 원문 누락은 계속 검사합니다. 같은 값으로 다른 의미를 작성한 오류를 놓칠 수 있으므로 CP 통과를 의미 정확성으로 설명하지 않습니다.

| 분석 결과 | 후속 처리 |
|---|---|
| PROCEED | 확정 범위 계속 |
| PARTIAL_PROCEED | 확정 범위 계속, 정보 부족은 별도 보고 |
| WAITING_FOR_USER | 핵심 시험 범위를 정할 수 없어 보류 |
| BLOCKED 또는 CP1 FAIL | 후속 인계 차단 |

기존 REVIEW는 확정 범위가 있으면 마지막 확인 사항으로 전달합니다. 다만 CP1-011의 범위 REVIEW는 PROCEED·PARTIAL_PROCEED여도 PAUSE로 인계를 멈춥니다. 중단 Run을 이어 붙이는 재개 기능은 없으며, 입력을 보완해 새 Run을 만듭니다.

PARTIAL_PROCEED의 정보 부족·제외된 정보 부족·제외 범위 연결은 유지합니다. 새 정책은 미정 여부를 특정 단어로 판정하지 않습니다. 원문이 제품 조건·절차·제외·정보 부족 중 어디에 전달됐는지 확인하되 분류 자체가 올바른지 자동 증명하지는 않습니다. 필수 제품 조건을 절차나 제외로 잘못 분류하는 위험은 평가·사람 검토 대상입니다.

시험 시작 전 확인·종료 후 복원 메모는 제품 판정 조건과 구분해 Agent 2로 전달합니다.

Agent 1 입력에는 인수 조건의 원문과 가능한 전달 위치를 제공합니다. 절차는 `procedure_notes`에 입력 원문 그대로 명시합니다. CP1-012는 입력에 없는 절차·중복·제품 조건/제외/정보 부족/제외된 정보 부족과의 중복 분류를 차단합니다. `[준비]`·`[복원]`·`[시험 절차 메모]`로 시작하는 인수 조건은 같은 전달 목록을 사용해 절차 원문 보존을 요구합니다. 같은 원문이 요청의 `out_of_scope`에도 있으면 제외가 우선이며 절차와 중복할 수 없습니다. 본문의 동사·동의어는 다시 해석하지 않습니다. CP2는 명시된 절차가 사전조건·단계·복원에 보존됐는지 검사하고, 기존 TC만 선택한 경우에는 수행 여부를 최종 검토로 인계합니다.

기존 기록의 누락 필드는 조회 시 빈 목록으로 읽으며 과거 원문에서 새 필드를 추정하지 않습니다. 이를 새 실행의 ‘절차 없음’으로 사용하지 않도록 새 `agent2` 실행은 검증된 Agent 1 2.8·2.9·2.10 / `STRUCTURAL_ONLY_V1` 인계만 받습니다. 과거 Agent 1에서 시작하려 하면 모델 클라이언트 생성·실행 예약·카탈로그 사본 작성 전에 새 Agent 1 Run을 안내하고 중단합니다. 완료된 과거 Agent 1·2 결과의 조회·당시 계약 재검증은 그대로 지원하며 기존 파일을 바꾸지 않습니다.

### 입력 분류와 차이 검사: 바꾼 범위와 유지한 제한

| 검사 | 현재 기준 |
|---|---|
| 제품 허용 범위와 시험 제외 | 단어로 제외를 추정하지 않음. 요청 `out_of_scope` 보존과 분석 결과의 제외 연결을 대조 |
| 준비·복원 | `procedure_notes` 원문 전달, 입력의 명시 역할 표시 보존, 다른 분류와 중복 차단. 본문 동사로 역할을 다시 분류하지 않음 |
| 기존 범위 유지 | description에만 있는 범위는 동일 target Requirement의 SRS 요구사항/인수 기준 전체를 statement·source_text에 그대로 보존한 `유지` 조건으로만 인정. after_value의 범위·다른 Requirement·변조된 문장에는 적용하지 않음 |
| 경계·값 대응 | 분석·설계의 문장 틀·상태 단어 비교는 제거. 원문 및 명시 코드·수치 근거는 유지. 같은 값의 의미 반전은 자동 검증 범위가 아님 |
| 계속 유지 | 원문/Requirement/ID/명시 값, 조건 연결, SRS 개정 근거, CP2 관찰 대상·시점, CP3 허용 동작·기대값·실제 복원·해시 인계 |

Agent 1·2의 문장 의미 추정 검사를 자동 평가 모델로 대체하지 않았습니다. 모든 미지원 문장을 REVIEW로 보내는 기능도 아닙니다. CP1의 정보 부족/범위 보류와 사람의 최종 승인, 별도 반복 평가를 유지합니다. Agent 3에서는 UI 이름·설명·내부 경로 간 단어 중첩 요구를 제거하지만, 실제 값의 원문 근거·사전조건 읽기 지원·조작 순서·표시값 축약 방지 검사는 실행 보호를 위해 유지합니다. 따라서 문자열 처리가 모두 사라진 것은 아닙니다.

배경 설명의 유지 온도 범위는 Agent 1 계약 2.9부터 대상 SRS 전체 원문·같은 Requirement ID·UNCHANGED 역할로 근거를 확인합니다. 분석 설명이 원문과 똑같아야 한다는 요구는 제거했습니다. 원문 변조·다른 요구사항의 숫자 차용·새 수치·변경 후 정책을 기존 SRS로 대체하는 것은 계속 차단합니다. 같은 값의 반대 의미까지 자동 판별하는 것은 아니며, 2.8 이하의 저장 판정은 당시 문장 일치 기준으로 읽습니다.

2.10의 SRS 인용 계약은 `|`를 원문 조각 구분자로만 사용합니다. 조각의 순서·개수로 필드 종류를 가정하지 않습니다. 각 조각은 연결된 Requirement의 요구사항 또는 인수 기준 한 필드 안에 연속 원문으로 존재해야 하고, 각 연결 ID도 최소 한 조각의 근거가 있어야 합니다. 여러 ID를 연결한 경우 출처 없는 조각과 근거 없는 ID 추가를 모두 거부합니다. 범위 근거 `scope_evidence.srs_source_text`는 해당 effect의 ID만 사용합니다. 빈 조각을 조용히 제거하지 않으며 코드·수치 토큰 경계 검사를 유지합니다. 단일 인용과 2.9 이하의 저장 판정은 기존 방식입니다.

CP1-007(출처), CP1-008(배경 범위), CP1-011(범위 근거)이 같은 분할/근거 검사 함수를 사용합니다. 배경 범위에는 연결된 모든 조각의 출처가 맞고 같은 대상의 전체 기준을 인용한 조각이 있어야 합니다. 새 조건의 의미 조합을 증명하거나 TC를 자동 분리하는 기능이 아닙니다. 변경 요청 출처·기대값·절차·SRS 개정 문구에는 `|` 분할 정책을 적용하지 않습니다.

## 5. Agent 2: 기존 TC 대조·설계와 CP2

확인 대상·판정 단계·복원 기준은 기존 구조화 필드를 사용합니다. observation_target의 누락과 판정 단계 연결은 검사하지만 설명 안에 같은 이름이 반복되는지는 검사하지 않습니다. CP2와 재작성 안내에 같은 연결 함수를 사용합니다. 단어가 같다는 이유로 같은 대상이라고 보장하지 않으며 값·시점·출처 검사를 유지합니다.

CP2·CP3는 복원 기준의 제한된 시점 표현을 공통으로 읽습니다. ‘시험 전’, ‘시험 시작 전’, ‘실행 시작 직전’ 등은 같은 관찰 기준 표현으로 처리하되, 원문 자체는 변경하지 않습니다. 실제 복원 비교는 기존 ER·읽기 경로·대상별 기준에 연결되어야 하며 다른 대상의 비교 구절이나 내부 코드를 화면 값으로 빌려 쓰는 것은 계속 거부합니다. CP2는 작성 검사, CP3는 지원 계획·컴파일러 연결 검사이므로 둘의 전체 판정 범위가 같다는 뜻은 아닙니다.

입력은 검증된 Agent 1 결과, 확정 조건, 관련 SRS, 기존 TC 카탈로그입니다. 카탈로그에는 기준 회귀와 승인 공식 TC의 ID·Requirement·함수·검증 동작이 있습니다. 승인 파일의 경로·SHA-256·TC 구조·단일 함수를 검사해 Run별 카탈로그 사본으로 고정합니다. 새 사본에는 승인 TC 원문도 보존합니다.

승인 공식 TC는 기대결과 요약 외에 검증된 명세의 사전조건·조작 절차·시험 데이터·기대결과와 판정 시점·중간 초기화·복원·독립 실행 근거도 Agent 2에 전달합니다. 검토자·등록 이력·파일 경로·자동화 코드는 이 명세 입력에 포함하지 않습니다. 명세의 절차가 실제 실행됐다는 증거는 Run에서 별도로 확인합니다. 과거 카탈로그에는 없는 상세 정보를 소급 추가하지 않으며, 기준 회귀는 기존 검증 동작 요약을 유지합니다.

모델은 조건과 기존 TC의 실제 검증 동작을 대조합니다. Requirement ID만 같다고 재사용하거나 관련 회귀를 자동 보충하지 않습니다. 모든 확정 조건은 신규 후보 또는 관련 기존 TC에 연결되어야 합니다. 기존 TC로 모두 검증되면 신규 후보 0건도 허용합니다.

| CP2 검사 | 내용 |
|---|---|
| 추적성 | 범위 밖 ID·조건 누락 차단 |
| TC 품질 | ID 고유성, 업무 규칙 묶음, 조건별 판정·중간 초기화 |
| 새 초안 상세화 | 확인 대상·유일한 판정 단계·구조화 복원 연결 검사. 대상 선택·입력/적용의 상세 서술은 작성 지침이며 문장 형식으로 차단하지 않음 |
| QA 기준 | 3단계 기준·독립성·필요한 이중 검증 |
| 역할 경계 | 제품 TC에 Selector·Python·구현 지시 금지 |
| 제품 경로 | 중앙 관제 패널·대상·시험 데이터 명시 |
| 정보 부족 | 불명확한 범위를 TC로 만들지 않고 원문 인계 |
| 재사용 | 카탈로그 밖 ID, 유지 조건의 중복 후보 생성, 명시 값 불일치 차단 |
| SRS 제안 | 개정 대상·현재 원문·제안·Condition 근거의 누락·중복·불일치 차단 |

같은 TC의 서로 다른 관찰값은 별도 기대 결과입니다. 모드가 단순 시험 조건인 문장은 별도 모드 표시 검증으로 추정하지 않습니다. 요구사항에 없는 행동 성공을 기대 결과로 추가하지 않으며, 기능 가능 여부가 요구사항이면 판정 가능한 표시·값·활성 상태로 구체화합니다.

새 작성 지침은 대상 선택 → 값 선택/입력 → 적용을 실제 조작에 맞춰 나누고 각 단계에 위치·대상·값·행동을 적도록 합니다. 준비·조작 확인은 preconditions/steps에, 제품 판정은 expected_results에, 복원은 restore_steps에 둡니다. 근거 없는 버튼명·선택 표시·내부 필드를 만들어 상세함을 채우지 않습니다. 단계 수를 고정하거나 읽기 전용 시험에 불필요한 조작을 만들지 않습니다.

새 CP2-020은 확인 대상 누락, 없거나 중복된 판정 단계 연결을 차단합니다. 특정 조작 문구·본문과 대상 이름의 반복은 차단 기준에서 제외합니다. `verify_after_step`은 실제 steps 한 곳에 연결하며 UI·내부 값은 같은 조작 뒤 확인합니다. 실제 조작 누락은 Agent 3 계획과 실행에서 별도로 검사합니다.

`scope_restoration_policy=STRUCTURED_V1`인 새 CP2는 `REQUEST_TRACE_ONLY`의 참고 연결만으로 이중 검증을 요구하지 않습니다. 직접 상태 정합성 조건·`STATE_CONSISTENCY` TC·명시적 `REQUIRED` 정책은 이중 검증을 유지합니다. 구조화 복원이 있으면 CP2-015의 복원 설명 단어 검사를 생략하되 CP2-023의 조작·확인 대상·순서·비교 기준 검사는 유지합니다. 최초 생성·재작성·저장 결과 로더는 같은 설정 함수를 사용합니다. 과거 계약에 이 정책을 소급 적용하지 않습니다.

풍량·온도·읽기 전용 상세 작성 예시는 유지하되 값·문구·조건 ID를 무단 복사하지 않습니다. CP2-021의 문장 표현 검사는 새 실행에서 사용하지 않습니다. 실제 복원은 CP2-022/023과 CP3에서 계속 검사합니다. 읽기 전용·기존 TC 전용에는 불필요한 적용·신규 후보를 강제하지 않습니다.

새 Agent 2 Manifest는 `contract_version=3.10`, `scope_restoration_policy=STRUCTURED_V1`, `procedure_preservation_contract=1.0`, `wording_policy=STRUCTURAL_ONLY_V1`, `input_routing_contract=1.0`, `tc_detail_contract=1.2`, `srs_revision_contract=1.1`, `structured_restoration_contract=1.0`, prompt 2.37입니다. `restoration.operation_steps`와 `restoration.confirmations`의 설명을 순서대로 합치면 상세 `restore_steps`와 정확히 같아야 합니다. CP2-023은 빠진 줄·중복·순서와 기존 상태 ER의 연결을 검사합니다. 각 확인은 `result_ids`와 같은 순서의 `comparisons`를 가지며, `basis=OBSERVED_BASELINE`, `verify_when=AFTER_RESTORE`만 사용합니다. 읽기 전용 TC는 구조화 계약의 조작·확인 목록을 비웁니다. 제품 판정은 기존대로 `verify_after_step`과 Agent 3의 `after_action_id`를 통해 연결된 조작 직후 실행합니다.

CP2-014의 새 절차 보존 계약은 같은 TC의 동일한 `preconditions`, `steps`, 또는 복원이 필요한 TC의 `restore_steps` 안에서 연속 항목을 이어 원문 전체와 대조합니다. 공백·대소문자 외의 문구·값·순서를 바꾸지 않습니다. 한 원문의 조각을 다른 TC나 절차 필드에 분산하거나 중간에 다른 조작을 넣으면 인정하지 않습니다. 일반 절차 누락을 무조건 사전 준비 누락이라고 표시하지 않습니다. 새 CLI 최초·재작성·후속 인계·승인 전 검토는 이 계약을 적용하고, 과거 3.8 이하에는 단일 항목 대조를 유지합니다. 버전과 계약 필드의 불일치를 거부하며 원본 산출물·판정은 재작성하지 않습니다.

내부 평가 함수를 직접 호출해 새 분할 보존을 시험할 때는 `legacy_wording_checks=False, allow_split_procedure_notes=True`를 함께 지정합니다. 후자의 기본값은 과거 검사 재현을 위해 False이며, 실제 로더는 저장된 계약으로 선택합니다.

TC 작성 예시는 입력창 근거 없이 '온도를 입력한다'고 단정하지 않도록 수정했습니다. 제공된 근거가 있을 때만 실제 조작 수단·횟수·화면 표시명을 적고, 없으면 대상·목표값·행동을 기술한 뒤 Agent 3의 화면 관찰에서 수단을 확인합니다. UI의 표시 상태와 내부 enum은 구분합니다. Agent 2가 새 UI 관찰을 수행하는 기능은 추가하지 않았으며, 작성 지침과 별개로 실제 자동화 가능성은 Agent 3와 시험으로 확인해야 합니다.

Agent 3는 구조화 TC의 `restoration.confirmations`를 계획의 `restore_confirmations`에 그대로 전달합니다. 설명 문장을 다시 해석하거나 대상별 구절로 나누지 않습니다. `source_excerpt`도 해당 설명의 전체 문장을 보존합니다. 기존 상태 ER마다 확인을 한 번씩 연결하고 새 Selector·기대값·알림 검사를 만들지 않습니다. 확인 설명을 추가 클릭으로 바꾸지 않으며, 실제 reader가 없는 ER은 차단합니다. 복원 조작은 별도 허용 목록과 TC 근거·순서 검사를 유지합니다.

구조화 계약에서는 복원 설명의 단어를 지운 뒤 남은 어휘를 거부하는 검사를 사용하지 않습니다. 실행 의미의 기준은 설명 문장 대신 구조화 필드입니다. 설명의 모든 의미·부정·모순을 자동 증명하는 기능은 아니며, 제품 기대결과의 값·대상·근거 검사는 별도로 유지합니다. 컴파일러는 준비 전 관찰값을 보존하고 같은 reader로 복원 후 비교합니다. 실제 비교가 성공하고 본 시험이 완료됐을 때만 `RESTORE_CONFIRMATIONS_VERIFIED` 로그에 연결 ER ID를 남깁니다.

구조화 TC의 Agent 3 Manifest는 `contract_version=4.8`, `wording_policy=STRUCTURAL_ONLY_V1`, `plan_fidelity_contract=1.1`, `restore_confirmation_contract=1.2`, `structured_restoration_contract=1.0`, prompt 3.32입니다. 구조화 계약은 CP2와 CP3가 같은 함수로 검사합니다. 과거 TC는 인계 4.4 이하의 당시 복원 문장·구절 검사로 읽고 새 필드만 과거 계약으로 위장하거나 누락시키는 인계는 거부합니다. 과거 공식 TC·실행 기록을 자동 변환하지 않습니다.

계획 충실성 계약은 온도·모드 전용 경로를 포함해 TC의 TEST·RESTORE 조작 순서와 누락을 검사합니다. UI 문구는 원문의 완전한 단어·값을 사용하고 한국어 조사를 제한적으로 허용합니다. 예를 들어 `중풍` 대신 `풍`만 검사하는 계획은 반려합니다. 한 글자라도 원문에 독립된 값으로 명시돼 있으면 허용하므로 특정 풍량 값이나 최소 글자 수에 고정하지 않습니다. 모든 문장의 의미나 메시지 전체 일치 여부를 증명하는 기능은 아닙니다. 계약 없는 과거 실행은 당시 판정을 보존하되 현재 공식 승인·재시험에는 새 검사를 적용합니다.

복원 비교 연결 오류는 CP3-006B로 구분하며 조작 누락은 기존 순서 검사에서도 차단합니다. 실행 진입점·후속 인계 재검증·컴파일·공식 승인 전 검사에서도 해당 TC의 계약을 확인합니다. 자유 설명의 표현을 검사하지 않는 것과 대상·비교 기준·확인 시점을 임의 변경하도록 허용하는 것은 다릅니다. 실제 모델의 반복 안정성은 별도 Live 증거로 확인해야 합니다.

계획 충실성 계약 1.1은 READ_ONLY TC의 장비 선택만 있는 계획에서, 기존 순서 검사에서 인정하던 마지막 조회·확인 단계를 시점 검사에서도 인정합니다. 상태 변경 TC에 이 예외를 확대하지 않습니다. 기대결과에 연결된 마지막 확인은 마지막 TEST Action 직후 Assertion으로 구현하며 가상의 클릭을 추가하지 않습니다. 중간 관찰, 누락된 조작, 이전 Action·최종 복원 뒤에 잘못 연결한 판정은 계속 차단합니다. 확인 동작 인식은 기존의 제한된 규칙을 공유하며 동의어 목록을 확대하지 않았습니다. 조작이 전혀 없는 계획은 READY의 최소 Action 계약 때문에 아직 지원하지 않습니다. 과거 4.7 이하에는 이전 시점 검사를 유지합니다.

복수 모드·온도는 `requested_modes`·`requested_temperatures_c`에 기록합니다. 준비·복원 전용 값은 시험 입력값에 섞지 않습니다. 기술 ID만 중복되면 의미·값·단계를 유지한 채 번호를 정리합니다.

신규 후보의 준비 메모는 사전조건/단계, 복원 메모는 복원 단계에 원문으로 보존합니다. 기존 TC 전용 요청에서는 새 후보를 강제하지 않고 메모와 선택 TC를 마지막 사람 확인 사항으로 전달합니다. 기존 코드가 그 절차를 실제 수행했다고 추정하지 않습니다.

MODIFIED·UPDATE_REQUIRED는 시험 영향 구분이며 문서 개정 필요와 같지 않습니다. 코드가 요청의 `after_value` 전체 문장과 고정 SRS의 대상 인수 기준을 비교합니다. 앞뒤 공백만 제외하고 같을 때 해당 대상의 개정안을 생략할 수 있으며, Agent 2 최초·재작성 입력에 개정 필요/이미 반영된 Requirement 목록을 함께 제공합니다. 다른 영향 Requirement는 기존 개정 요구를 유지합니다. 부분 일치·대소문자 변환·문장 유사성·TC 재사용 여부로 개정 불필요를 추정하지 않습니다.

CP2-018은 이 범위 밖의 개정안, 변경되지 않은 전·후 문구, 필요한 개정안 누락을 반려합니다. 이미 반영된 문장을 다시 제안하면 그 개정안을 제외하라는 피드백을 주며 검사기가 결과를 몰래 삭제하거나 통과시키지 않습니다. SRS 개정이 없어도 TC 대조·필요한 시험·실행 증거 검사는 유지합니다. 실제 개정안은 전·후 인수 기준과 근거를 실행·보고·사람 검토까지 전달하고, 이 단계에서 기준 SRS를 변경하지 않습니다.

새 인계는 SRS 개정 계약 1.1을 재검사하며 누락·하향 변경을 거부합니다. 과거 계약 1.0 실행은 당시의 개정 필수 규칙으로 재검사하므로 과거 실패 기록을 성공으로 소급 변경하지 않습니다. 정확한 전체 문장 일치만 처리하는 제한 규칙으로, 표현이 다른 문장의 의미 동일성이나 자유 서술 전체의 타당성을 자동 판정하지 않습니다.

새 계약의 기존 TC 재사용과 SRS 제안은 명시 값·근거를 계속 대조하되, 상태 단어의 반전으로 의미를 판정하지 않습니다. SRS 제안의 코드·수치는 현재 기준과 변경 근거 안에 있어야 합니다. 의미 일치는 자동 확정하지 않으며 사람의 SRS 동의는 계속 필요합니다.

Agent 2의 2.29 입력은 복합 조건의 담당 TC 연결을 명시적으로 안내합니다. 처음에는 조건별 제한 추출 값, 재작성에는 해당 조건에 실제 연결된 기존 TC ID·그 연결로 충족되지 않는 값·신규 담당 후보 ID를 보여줍니다. 다른 조건에만 연결된 TC는 현재 조건의 근거로 합산하지 않습니다. 하나의 조건을 여러 TC가 나누어 검증할 수 있지만 값만 같다는 이유로 자동 연결하지 않습니다. 조건이나 기대값을 삭제해 통과시키지 않고 대상·조작·결과·복원을 대조합니다. 빈 추출 값은 검증 불필요가 아닙니다. CP2-019 및 다른 검사 코드·재작성 횟수·과거 판정은 그대로이므로 인계 계약 버전은 변경하지 않았습니다.

## 6. Agent 3: 자동화 계획·컴파일·시험과 CP3

신규·수정 후보를 한 건씩 독립 처리합니다. 기존 TC의 자동화는 다시 만들지 않습니다.

1. 후보의 자동화 가능성을 확인하고 필요한 UI만 조사합니다.
2. AI가 실제 관찰 요소로 조작·검증 계획을 만듭니다.
3. CP3가 TC·값·관찰 계층·판정 시점·복원·근거를 검사합니다.
4. 필요하면 기술 계획을 최대 1회 재작성합니다.
5. 컴파일러가 허용 조작으로 Python을 생성하고 정적 검사합니다.
6. 분리된 임시 위치에서 시험하고 로그·화면·Trace·해시를 저장합니다.

API 입력에는 해당 TC, 관련 SRS와 필요한 UI 확인 결과를 사용합니다. 키·로컬 절대경로·전체 HTML·Screenshot·Trace를 넣지 않습니다.

### UI 조사와 허용 범위

기존 기능은 필요한 Selector·하네스 키만 확인합니다. 처음 보는 기능도 안정적인 ID·data-testid·접근성 이름과 단순 상태로 구현 가능한지 조사합니다.

| 구분 | 허용 범위 |
|---|---|
| 공통 조작 | CLICK, FILL, SELECT_OPTION, CHECK, UNCHECK |
| V1 호환 조작 | 해당 TC에 필요한 장비 선택·모드 선택·온도 설정·중앙 적용 |
| UI 검증 | 텍스트·값·체크·활성 상태·온도 표시·알림과 차단 의미 |
| 내부 검증 | 실제 확인한 고정 하네스 아래의 장비 필드·단순 읽기 값 |

내부 필드 사용은 실제 조사에서 확인되고, TC 기대 결과에 필드·기대값 근거가 있으며, 허용 루트 아래의 단순 읽기여야 합니다. 임의 JavaScript·쉘·파일 변경·외부 호출·Assertion 삭제·기대값 변경은 허용하지 않습니다. 드래그·Canvas·복잡한 위젯처럼 지원하지 않는 기술이 필요한 TC는 사유와 함께 제외합니다.

조사한 Selector는 일치 요소 수를 기록하고 단일 요소일 때만 조작·범용 검증에 사용합니다. 장비 필드는 다른 장비의 합집합이 아닌 고정 시험 대상 ID 1에서 수집합니다. 범용 계획은 TEST·RESTORE의 모든 조작 문장과 순서를 반영해야 합니다. 마지막 읽기 전용 확인 문장은 연결된 검증 조건으로 수행하므로 불필요한 클릭을 만들지 않습니다. 비활성을 활성 True로 검사하거나 ‘표시’ 같은 일반 단어만 검사하는 계획은 거부합니다.

변경 후 동적 텍스트는 초기 조사에 없을 수 있습니다. 승인 기대 결과가 대상 장비 카드를 명시하고 정확한 대상 카드 Selector를 사용할 때만 실행 후 텍스트 검증을 허용합니다. 다른 카드나 상위 컨테이너로 대신 검증하지 않습니다. 차단 안내는 Toast 존재만으로 통과시키지 않고 차단 의미를 확인합니다.

### 시험·복원·증거

#### 새 후보의 유형별 상태 복원 계약

Agent 2의 새 인계는 3.10 / 프롬프트 2.37 / `scope_restoration_policy=STRUCTURED_V1`, `procedure_preservation_contract=1.0`, `wording_policy=STRUCTURAL_ONLY_V1`, `input_routing_contract=1.0`, `state_restoration_contract=1.0`, `structured_restoration_contract=1.0`입니다. 각 새 TC의 `state_effect`와 복원 필요 여부를 CP2-022에서, 구조화 확인 연결은 CP2-023에서 검사하고 후속 로더도 재검사합니다. 계약 누락·하향 변경을 차단하며 과거 계약은 당시 기준으로 읽습니다. Agent 3 프롬프트 3.32와 CP3-006C는 다음 정책을 계획·컴파일에 연결합니다.

| TC 유형 | 실행 기준 |
|---|---|
| READ_ONLY · 조회 | 값 변경·복원 조작 없음. 대상 장비 선택과 관찰만 허용 |
| STATE_CHANGE · 값 변경 | 시험 전 관찰값 저장 → 시험 → 승인된 복원 조작 → 동일 대상 관찰값 비교 |
| BLOCKED_CHANGE · 변경 차단 | 시험 직전 상태와 비교. 예상 밖 변경은 제품 불일치로 기록하고 복원. 상태가 유지됐고 준비 조작도 없었던 경우에만 복원 생략 |
| 복원 실패 | 원래 제품 불일치도 보존하고 복원 실패를 별도 기록. 해당 브라우저 문맥을 종료하며 재사용하지 않음 |

`RESTORE_STATUS`는 RESTORED(복원·비교 완료), UNCHANGED(상태 유지), NOT_REQUIRED(조회), NOT_STARTED(변경 동작 전 중단), FAILED(복원 실패)로 남습니다. Agent 4의 사람 검토서에는 해시 검증된 로그의 상태만 표시합니다. 복원 실패 후 다른 TC를 실행한다면 기존처럼 별도의 초기 브라우저·실행 사본에서 수행하며, 실패 환경에서 이어서 실행하는 것이 아닙니다.

복원 비교 범위는 TC·계획에 연결된 관찰 대상이며 동적 HVAC 복원은 대상 장비의 모드·설정 온도도 비교합니다. 시스템 전체 또는 관찰하지 않은 다른 장비의 복원을 보장하지 않습니다. 고정 복원값을 임의로 만들지 않으며 안전한 관찰·복원 근거가 부족하면 자동화 지원 보완 대상으로 남깁니다. 과거 준비·복원 동작과 기존 공식 자산은 변경하지 않습니다.

새 정책의 모드·온도 준비에는 기존 `restore_observed_hvac_state`와 단일 `RESTORE_OBSERVED_HVAC`를 연결합니다. `initial_mode`·`initial_temperature_c`는 준비 완료 조건이고, 복원 값은 준비 전 실제 관찰값입니다. CP2는 새 TC에서 이 두 의미의 공존을 허용하고 CP3는 지원되는 HVAC 동작과 관찰 기준 복원만 허용합니다. 원래 상태는 대상 선택 후 첫 변경 전에 보존하고, 차단 판정을 위한 준비 상태는 TEST 직전에 별도 보존합니다. 준비 실패·사전조건 불일치도 finally에서 복원하며, 조회 TC의 준비 변경은 계속 금지합니다. FAN·DRY로 돌아갈 때는 온도 입력이 가능한 모드에서 원래 온도를 먼저 적용한 뒤 원래 모드를 적용합니다. 임의 범용 CLICK·FILL의 준비 역동작까지 지원한다는 뜻은 아닙니다.

아래는 과거 계약을 포함한 공통 실행 설명입니다. 새 정책은 위 제한을 추가로 적용합니다.

시험은 초기 문맥 확인 → 승인된 준비 → 조건 조작 → 해당 결과 검증 → 필요한 중간 초기화/전환 → 다음 조건 순서입니다. 마지막 제품 관찰 Screenshot을 남기고 finally에서 복원·재확인한 뒤 Trace를 종료합니다. 실행 전후 대상 파일 해시를 확인합니다.

생성 코드는 같은 판정 단계의 UI·내부 관찰 묶음을 최대 2초 동안 50ms 간격으로 다시 읽습니다. 조작은 재실행하지 않습니다. 브라우저 조회 자체의 대기 때문에 전체 시간이 정확히 2초라는 뜻은 아니며, 실제 장비 응답 완료나 불변 상태의 지속 시간을 증명하지 않습니다.

고정 초기값이 없는 V1 온도·모드 묶음은 `restore_observed_hvac_state` 계약으로 실행 전 mode·setTemp를 저장하고 UI로 복원·적용한 뒤 다시 비교합니다. 고정 복원과 혼합하거나 일반 기능 내부 쓰기로 확장하지 않습니다. 범용 복원에서도 검증 대상의 관찰 상태를 실행 전후 대조합니다.

시험 결과는 PASS, PRODUCT_MISMATCH_CANDIDATE, AUTOMATION_ERROR, ENVIRONMENT_ERROR, TIMEOUT입니다. 앞의 두 상태가 필수 증거를 갖추면 완료 후보로 인계합니다. 실패·미지원 후보는 제외 사유를 남겨 다른 후보 처리를 계속합니다.

예상된 기술 실패·미지원 제외와 프로그램 내부 예외는 구분합니다. 내부 예외 또는 agent3_error.json이 있으면 다른 후보의 독립 처리를 마친 뒤 전체 상태 ERROR·종료 코드 1을 기록하고 정상 후속 인계를 차단합니다. 일부 TC만 명시 선택하면 나머지는 미선택 사유로 남기고 PARTIAL로 표시합니다. 설계에 없는 TC ID는 오류이며, 오류 기록 중 손상 JSON을 만나도 원래 오류 유형과 읽지 못한 파일명을 남깁니다.

시험 기본 제한은 90초입니다. 초과하면 pytest·Playwright 자식 프로세스를 정리하며 쓰기가 끝나지 않은 Trace는 증거에서 제외합니다. 정상 Trace도 로컬 경로·비밀정보 정제를 마친 뒤 SHA-256을 기록합니다. 과거 Run을 소급 수정하지 않습니다.

`evidence_complete`는 Screenshot·Trace 존재 확인이며 복원 성공까지 의미하지 않습니다. 실제 해시가 맞는 로그의 RESTORE_MISMATCH를 Agent 4가 확인합니다. 제품 불일치와 복원 실패가 함께 있으면 두 관찰을 보존하고 자동화 문제·HOLD로 분류합니다. 같은 TC를 두 번 집계하지 않으며 코드에 인용된 표식이나 변조 로그는 근거가 아닙니다.

임시 작업공간은 원본과의 파일 분리를 제공하며 컨테이너·OS 권한 격리를 제공하지 않습니다. 실행기는 시작 상태를 위해 브라우저 저장소를 초기화하고, 기존 회귀에는 준비·오류 주입 하네스가 있습니다. 이를 일반 사용자의 UI 조작으로 표현하지 않습니다.

### 사전조건을 실제 값으로 확인하는 절차

새 Agent 3 계획은 각 TC 사전조건 원문에 `precondition_checks`를 연결합니다. 준비 Action을 실행했다는 사실이나 UI Inventory의 초기 값만으로 충족을 확정하지 않습니다. CP3-006A는 누락·원문 불일치·미관찰 경로·기대값 변형·복합 조건의 명시 값 누락을 검사합니다. 현재 CP3의 기본값과 새 실행·현재 후보 승인은 이 검사를 요구합니다.

읽기 방식은 UI 텍스트·입력값·체크 상태·활성 상태, TC에 근거하고 관찰된 내부 단일 값, 기존 중앙제어 장비의 제한된 기본 문맥입니다. 기본 문맥은 대상 카드 표시·오류 없음·잠금 해제·온라인을 각각 확인합니다. 온라인은 가상 제품의 OPERATION/STOP/ERROR 상태이며 실제 장비 통신 증명이 아닙니다. 관리자 로그인·인증을 장비가 보인다는 사실로 대신 증명하지 않습니다.

관찰 정보에 있는 모든 상태를 검사하는 것은 아닙니다. TC 사전조건에 적힌 내용만 확인하며, 원문에 없는 온라인·선택 상태 등을 추가 요구하지 않습니다. 기본 문맥 확인이 실패하면 항목 번호·관찰 이름·연결 원문·실패 이유를 재작성 입력으로 전달합니다. 불필요한 추가 확인을 제거하는 것과 실제 조건을 삭제하는 것을 구분하며, 기존 검사 기준·최대 1회 재작성·런타임 비교는 유지합니다.

모델 입력의 `precondition_context_bindings`에는 원문별 허용 BASELINE_CONTEXT 항목을 표시합니다. 검사기가 이미 사용하는 원문 패턴·관찰 근거를 기준으로 만들며 새 의미 해석이나 기대값을 추가하지 않습니다. 목록이 비어도 그 조건을 생략할 수 없고, 내부 초기값 등은 다른 허용 읽기 방식으로 확인해야 합니다. 실제 관찰값이 false이면 이를 true로 바꾸지 않으며 런타임에서 원래 기대값과 비교합니다. 최초 계획과 재작성에 같은 목록을 전달하고, Action만 지적된 경우 정상 사전조건·Assertion·복원 연결을 유지하도록 안내합니다. 출력은 여전히 기존 CP3 규칙으로 검증하며 검사 결과·허용 범위·재작성 횟수는 바꾸지 않습니다.

Agent 3는 각 모델 계획을 검사 전에 시도별 파일로 보존해 이후 검사 오류와 분리합니다. 공통 저장 함수는 같은 폴더의 짧은 고유 임시 파일을 쓴 뒤 원자적으로 교체합니다. 최종 경로 자체가 운영체제 제한을 넘는 경우까지 지원한다는 뜻은 아니므로 실행 루트는 짧게 유지합니다.

| 상황 | 처리 |
|---|---|
| 사전조건 확인 항목 누락·부적합 | CP3 실패, 기존 최대 1회 재작성 후에도 실패하면 해당 TC 제외 사유로 최종 보고 |
| 허용된 읽기 방식으로 확인 불가 | 지원 범위 확장 사유를 남기며 실행 코드를 만들지 않음 |
| 준비 뒤 실제 값이 기대와 다름 | 본 TEST를 시작하지 않고 사전조건 불충족 기록. 자동화 실행 문제·HOLD이며 제품 불일치가 아님 |
| 모든 확인값 일치 | 본 TEST 진행. 성공한 시험도 stdout에 읽은 값과 확인 완료 기록 보존 |

컴파일러는 모든 PRECONDITION Action 뒤, 첫 TEST 앞에서 확인합니다. 비동기 상태 반영은 기존 2초 재관찰 helper를 이용하며 준비 조작을 반복하지 않습니다. 같은 시점의 일반 복원 기준은 유지하고, 원래 HVAC 상태 복원은 별도 계약대로 준비 전 값을 사용합니다. 실패 시 Screenshot·Trace와 오류 로그를 남깁니다.

새 Manifest의 `precondition_proof_contract=1.0`은 이 계약을 사용했다는 표시입니다. 과거 Manifest가 없는 계약의 인계·조회는 역사적 동작을 유지하지만 새 증명을 완료한 것으로 해석하지 않습니다. 현재 공식 후보 승인과 승인 전 재검증에는 증명 계획이 필요하므로 과거 계획은 새 계약의 계획·시험 확보가 필요할 수 있습니다. 기존 승인 자산을 소급 취소하거나 바꾸지는 않습니다.

이 계약은 **사전조건마다 확인 근거를 연결하고 실제 값을 비교하는 것**입니다. 임의의 자연어·복합 문장 전체가 올바르게 해석됐다는 수학적 증명이나 모든 제품의 의미 동등성 보장은 아닙니다. 관찰 가능한 조건과 확인 불가능한 조건을 구분하며, 최신 프롬프트의 실제 모델 생성 검증은 별도 실행 결과로 판단합니다.

## 7. 변경 검증과 기존 회귀

`execute`는 Agent 3 Manifest·계획·코드·시험·증거를 확인하고 현재 CP3로 다시 검사합니다. 현재 컴파일러 출력과 같으면 시험 결과를 재사용하고, 다르면 별도 위치에서 재컴파일·재시험합니다.

환경 점검 TC-ENV-000을 먼저 실행하고 Agent 2가 명시한 관련 기존 TC만 중복 없이 실행합니다. 신규 후보가 없거나 전부 제외돼도 요약·환경 점검·선택 기존 TC·보고를 계속할 수 있습니다. 환경 점검이 실패하면 기존 회귀를 중단합니다.

기본 대상은 `product_baseline/virtual-controller.html`, 기존 회귀는 같은 폴더의 `tests/test_controller.py`입니다. 테스트와 HTML을 임시 Workspace에 복사하고 선택 함수만 실행합니다. 승인 공식 TC도 카탈로그 사본과 현재 자산 해시가 같아야 실행하며 기준 파일은 변경하지 않습니다.

기존 회귀도 후보 시험과 같은 프로세스 트리 종료 함수를 사용합니다. SKIPPED 여부는 임의 경고문 단어가 아닌 pytest 결과 요약 형식에서 판독합니다. 임의 테스트 프레임워크 출력 전체를 지원하는 파서는 아닙니다.

## 8. Agent 4: 분류·보고와 CP4

모델 호출이나 시험 재실행 없이 저장 결과를 제품 불일치 후보·자동화 문제·환경 문제·근거 부족·미실행으로 분류합니다. TC 설계 유형인 정상·경계·예외·상태 정합성과 실행 후 원인 분류는 별도 정보입니다.

| 상황 | 최종 권고 |
|---|---|
| 실행 범위 통과, 증거 유효, 자동화 제외·정보 부족 없음 | PASS |
| 유효한 제품 기대값 불일치 | HUMAN_REVIEW: 사람이 판단 |
| 실행한 TC는 통과했지만 자동화 제외·정보 부족이 남음 | HUMAN_REVIEW: 미확인 범위 검토 |
| 실행 결과의 자동화·환경 오류, 미실행·증거 부족 | HOLD: 보류 |
| 제품 불일치와 복원 실패 동시 발생 | HOLD, 두 관찰 보존 |
| CP4의 증거·집계 검사 실패 | HOLD, 외부 전송 차단 |

앞 단계에서 기술 실패 후보가 제외 목록으로 전달되면 자동화 제외·사람 검토 경로를 따릅니다. 의도적으로 정한 범위 밖 항목만으로 사람 검토를 강제하지 않습니다. 개별 기대 결과의 관찰 근거가 없으면 통과로 추정하지 않습니다.

CP4는 Run·입력 해시, TC 중복, 상태·종료 코드·메시지·증거, 제품/환경 집계, Agent 3→실행 전체 인계와 보고 수치를 확인합니다. V1 회귀는 기준 코드 해시, 승인 회귀는 카탈로그 사본과 승인 코드 해시로 검증합니다.

`사람_최종_검토.md`에는 기대·실제 관찰·증거, 제품 수정/요구사항 보완/자동화 재검토/환경 재실행/종결 선택지, 검토자·일자·기한·후속 조치란이 있습니다. Manifest는 생성 원본과 보고의 해시를 연결합니다. 사람이 편집한 문서는 자동 갱신으로 덮어쓰지 않으며, 미편집 원본만 `human-review --refresh`로 갱신할 수 있습니다.

CP4와 최종 보고 해시가 통과해야 Slack·Notion Payload를 생성합니다. 기본은 미리보기, CLI `--send`는 실제 전송입니다. Notion은 Run ID:TC ID로 같은 실행만 갱신하고 다른 실행을 보존합니다. 최초 전송 기록은 유지하고 후속 시도는 별도 폴더에 저장합니다. 화면은 최신 시도와 과거 실제 전송을 구분합니다.

Notion 기록은 실행 결과가 있는 TC의 제목·결과·분류·보고 요약과 상세 TC 본문입니다. Agent 2 인계 Manifest가 있고 기존 보고 소비 단계에서 해시·Checkpoint 재검증이 통과한 Run에 한해 저장된 사전조건·조작·기대결과의 확인 대상/판정 단계·복원을 `notion_payload.json`의 `tc_detail`로 전달합니다. 본문은 사전조건 → 조작 순서·단계별 기대결과 → 복원 순서입니다. 기대결과는 설계이며 실제 관찰값이나 공식 등록 승인으로 표시하지 않습니다. 승인 TC는 해시가 맞는 보존 명세를 사용하고, 원문 단계에 유일하게 연결되지 않는 과거 기대결과는 별도 목록으로 표시합니다. 상세 설계 연결이 없는 환경 점검·과거 기록에는 미기록 상태를 표시하며 내용을 추정하지 않습니다. Agent 2 초안만 별도 게시하는 기능은 없습니다.

본문은 내용 해시로 구분한 자동 기록 펼치기 블록을 추가합니다. 같은 내용은 기존 본문과 대조한 뒤 건너뛰고, 변경된 내용은 새 기록으로 남깁니다. 사람이 작성한 블록을 삭제하지 않으며 자동 기록 내부의 문구가 편집됐으면 덮어쓰지 않고 실패로 알립니다. 긴 본문은 나누어 전송하고, 중간 실패 뒤 재시도하면 확인된 부분은 유지합니다. 요약 속성 저장 후 본문 전송이 실패할 수 있으므로 상세 전송까지 끝나야 해당 건을 완료로 집계합니다. 동시에 같은 기록을 보내는 작업의 중복 방지나 오류 자동 재시도는 보장하지 않습니다. 읽기·삽입 권한과 실제 계정에서의 게시 확인은 별도로 필요합니다. 요청 크기·중첩은 [Notion 본문 추가 규격](https://developers.notion.com/reference/patch-block-children)과 [요청 제한](https://developers.notion.com/reference/request-limits)을 따릅니다.

## 9. 중앙제어 화면과 사람 승인

| 모드 | 동작 |
|---|---|
| HTML 직접 열기·공개 접속 | MED 정상 변경 저장 데모, API·시험·실제 파일 반영 없음 |
| 기본 로컬 서버 | 저장된 Run 조회 |
| --allow-live-run | 허용된 변경 요청 선택·화면 확인 뒤 Agent 1→4 실행 |
| --allow-asset-approval | 조건이 유효한 후보의 사람 승인·보류 |

로컬 입력은 `examples/change_request*.json`이며 임의 경로·쉘 명령은 받지 않습니다. Run ID를 명시하고 기존 ID 재사용을 거부합니다. 서버 SRS·승인 자산 경로를 분석·실행·승인에 함께 전달하며 저장소 단위 파일 잠금으로 중복 Live 실행을 막습니다.

쓰기 POST는 서버 포트의 localhost/127.0.0.1 Host와 동일 Origin, application/json 본문을 요구합니다. 외부 출처·Origin 없음/null·다른 Host 요청은 거부합니다. 이는 로컬 브라우저 요청 보호이며 사용자 인증이나 악성 로컬 프로그램 격리가 아닙니다. 공개 서버를 추가하지 않습니다.

제품 시험은 별도 브라우저에서 수행됩니다. 페이지는 실행 상태·결과를 조회하며 재접속 후에도 갱신합니다. 조회한 Run과 실행 중 Run을 구분하고 늦은 응답은 현재 선택을 덮지 않습니다. 연결 실패는 확인 불가로 표시·재시도합니다. GET 대기는 10초, 승인/재검증 POST는 180초이며 HTTP 대기 종료가 서버 시험 취소를 의미하지 않습니다. Run 전환 시 승인·SRS 동의·메모를 초기화합니다.

최종 보고가 없으면 중간 단계 PASS를 전체 PASS로 표시하지 않습니다. 환경 실패로 유효한 BLOCKED 실행 결과가 저장됐으면 Run·해시 확인 뒤 Agent 4로 넘기며, 결과 누락·손상이나 다른 실행 오류는 중단합니다.

TC 표는 설계·실행·제외·분류를 합쳐 유형·결과·사전조건·절차·기대·복원·관찰·증거를 보여줍니다. 기록 없는 유형·우선순위·기존 TC 상세는 추정하지 않습니다. 새 Run은 승인 TC 원문 사본을 사용하고 과거 Run은 지정된 자산 폴더와 해시를 확인합니다.

### 공식 TC 등록

1. 사람이 PASS 후보의 명세·증거·확인 사항을 검토합니다.
2. 서버가 최종 PASS, CP3·CP4 PASS, 변경 검증 PASSED, 필수 증거와 코드·현재 HTML 해시를 확인합니다.
3. HTML이 바뀌었으면 같은 후보를 현재 화면에서 모델 호출 없이 재검증합니다.
4. SRS 제안이 있으면 후보 Requirement·Condition에 연결된 개정 전후 문구를 보여주고 별도 동의를 받습니다.
5. 승인 TC·Python과 검토자·메모·출처·해시를 approved_assets에 기록합니다.
6. 다음 Agent 2가 등록 자산을 기존 TC 카탈로그에서 대조합니다.

승인 후보 코드는 Agent 3와 변경 검증 기록의 코드 해시 모두와 일치해야 합니다. 다른 코드이면 승인과 승인 전 재검증을 차단합니다. 재검증 Raw 증거는 로컬 Run에, 공개 가능한 요약은 approved_assets/provenance에 보존하고 두 해시를 구분합니다.

승인 전 재시험 시작·종료 시 제품 HTML과 후보 코드 해시를 대조합니다. 중간에 변경되어 종료 시 해시가 다르면 PASS 기록을 게시하지 않고 재시험을 요구하며 이전 `latest.json`은 보존합니다. 시험 도중 변경 후 원복까지 감시하는 기능은 아닙니다.

같은 Run·TC 중복 승인은 같은 기록을 반환합니다. 보류는 사유만 남기며 이미 승인한 자산을 보류로 되돌리지는 않습니다. 등록 중 오류가 나면 해당 승인에서 바꾼 SRS·TC·Python·Registry·임시 파일을 복구합니다. 사람 검토서 작성만으로 Registry가 바뀌지 않습니다.

기존 TC만 실행한 Run도 유효한 Agent 1/2 인계·실행·현재 제품·기존 TC 조건 연결·SRS 원문을 확인해 SRS_ONLY 승인을 할 수 있습니다. 검토자·메모·SRS 동의와 화면 확인이 필요하며 TC·Registry는 새로 만들지 않습니다. 판단 기록 실패 시 SRS를 원상복구합니다. 동일 제안은 멱등 처리하고 다른 원문으로 바뀐 SRS는 거부합니다.

## 10. 파일과 인계 계약

| 파일/경로 | 역할 |
|---|---|
| agent1_change_analysis.json, agent2_test_design.json | 분석·제품 TC·SRS 제안 |
| checkpoint1.json ~ checkpoint4.json | 자동 검사 결과 |
| agent3_selection.json | 후보 선택·사전 제외 사유 |
| agent3_candidates/각-TC/ | 후보별 계획·코드·시험·증거 |
| agent3_run_summary.json | 다중 실행의 완료·제외 요약 |
| orchestrator_manifest.json | Agent 1~3 종료와 주요 해시 |
| validation_execution.json | 변경 검증·선택 기존 회귀 결과 |
| agent4_analysis.json, final_report.json | 원인 분류·최종 권고 |
| 사람_최종_검토.md와 Manifest | 사람 작성 양식·원본 추적 |
| external_reporting.json, external_reporting_attempts/ | 최초/후속 보고 기록 |
| approved_assets/registry.json | 공식 TC·자동화 등록 |
| approved_assets/srs_revisions/ | 사람이 동의한 SRS 개정 이력 |

단일 agent3 CLI는 루트 agent3_manifest.json·agent3_trial.json을 사용하며 다중 요약이 없습니다. CP4는 정확한 단일 경로·해시를 검사하고, 다중 실행에는 요약을 요구합니다. 과거 일괄 모델 호출 agent3_batch_*는 현재 실행 계약이 아닙니다.

공통 구조는 Pydantic JSON·SHA-256 인계·단계별 최대 1회 재작성입니다. 역사적 existing_procedure_review_contract, existing_behavior_values_contract, double_assert_timing_contract가 없는 Run에는 새 CP2 규칙을 소급 적용하지 않습니다. 현재 후보 재검사와 과거 증거 조회는 서로 다른 처리입니다.

과거 CP1·CP2의 재검증에서는 SHA-256 원본 확인과 Manifest 계약 확인을 먼저 수행합니다. 이후 저장 결과와 재계산 결과에서 양쪽 모두 PASS인 항목의 `message`만 비교에서 제외합니다. 검사 ID·개수·순서·판정, FAIL/REVIEW 메시지, 인계 상태·최종 검토 사항 등 나머지 필드는 정확히 일치해야 합니다. `STRUCTURAL_ONLY_V1` 계약은 PASS 메시지까지 비교합니다. 원본 기록을 재작성하지 않으며, 검사 항목이나 실제 판정이 다른 오래된 기록까지 수용하는 마이그레이션은 아닙니다. 과거 조회가 가능해져도 새 Agent 2 실행은 새 Agent 1 인계가 필요하다는 조건은 유지됩니다.

새 정책에서도 meaning_guard_contract=1.0의 출처·명시 값 근거 검사는 유지합니다. 문장 의미 추정 사용 여부는 wording_policy로 구분합니다. 과거 인계는 당시 기준으로 대조하며 원본 판정·해시를 바꾸지 않습니다.

공개 진입점 src/qa_pipeline_v2.py는 import·CLI 호환을 유지합니다. 내부 구현은 contracts, agent1, agent2, agent3, execution, reporting, orchestrator와 공통 grounding/io/trace, UI 표시 계층으로 나뉩니다. 테스트는 grounding을 포함한 8개 역할 파일과 pipeline_test_support.py를 사용합니다.

### 후속 감사에서 보완한 연결 검사

- 신규 TC의 기대 결과는 연결된 조건의 코드·수치 근거를 보존해야 합니다. 테스트 입력을 정상 출력의 근거로 삼지 않습니다. 명시 범위 안 값·유지 조건의 사전 값·차단 입력 근거 검사는 유지하지만, 분석·설계 문장 속 상태 단어 비교만으로 의미 정확성을 판정하지 않습니다.
- 새 Agent 2 Manifest의 `candidate_expectation_contract=1.0`은 이 검사를 실행했다는 표시입니다. 과거 Run은 당시 CP2 판정으로 조회하되 현재 신규 자산 승인에서는 기대 결과와 CP3 계획을 다시 검사합니다.
- 상태를 확인하는 문자 검사는 고정된 제어 이름만 검사해서는 안 됩니다. 비활성 전용 전략은 명시적 비활성 기대에만 사용합니다.
- 일반 고정 복원은 TC 준비 완료 직후, TEST 이전의 상태와 비교합니다. `RESTORE_OBSERVED_HVAC`는 준비 전 모드·온도를 복원합니다. 사전조건 원문별 읽기 근거와 실행 직전 실제 값 확인은 위 사전조건 절차를 따릅니다. 임의 자연어 의미 전체의 동등성은 보장하지 않습니다.
- 보고 재사용 시 실행 JSON→Agent 4 분석→최종 보고의 해시와 공통 내용, 후보·회귀 증거, 존재하는 Agent 1·2 원본 연결을 재검사합니다. 공식 후보 승인과 승인 전 재검증은 TC→계획·관찰·CP3 연결도 확인합니다.
- 필수 UI 인터페이스 누락은 그 TC의 제외 사유이며 예기치 않은 코드 예외는 내부 오류입니다. 같은 서버의 동시 승인 요청은 잠금으로 직렬화하고 Run 전환 후 늦은 POST 응답은 화면에 적용하지 않습니다.

## 11. V1 기준 자산과 검증 경계

V1 커밋 ba62b611251180cbd0426eae0f9ab43a67a12abf에서 HTML·pytest.ini·tests/conftest.py·tests/test_controller.py 네 파일만 product_baseline에 가져왔습니다. 기존 TC는 변경분 생성 재료가 아니라 관련 회귀 실행용 기준 코드입니다. V2 HTML은 제품 보완과 Run·데모 패널 추가로 V1 원본과 전체 해시가 다릅니다.

V1의 13건은 환경 점검 1건, 제품 기능 후보 7건, 제품 불일치 분류 고정 사례 1건, 파이프라인 분류 고정 사례 4건이 섞인 자료입니다. 제품 회귀 성공률로 합산하지 않습니다. 당시 TC/요구사항 대응과 GAP 감사표는 [결정 기록의 V1 감사 부록](../DECISION_LOG.md#v1-baseline-audit)에 보존합니다.

현재 제품 범위는 중앙 관제 패널입니다. 실제 Modbus/MQTT, 역할 전환 UI, 연결되지 않은 필터·장치정보·도움말, 벽면 리모컨은 구현 범위로 소개하지 않습니다. 제품 기대값은 [SRS](01_PRODUCT_SRS.md)를 따르며 구현과의 차이를 자동으로 정상 기준으로 바꾸지 않습니다.

검증에서는 요청→조건→TC→Action/Assertion→코드→증거→보고 추적, 후보/회귀/환경 집계, 원본 불변, 승인 동의·원상복구, 공개 데모 무통신·비반영을 확인합니다. 자동 테스트 수와 제품 Run TC 수는 합산하지 않습니다. 현재 통과 이력·간헐적 오류·실제 API 검증 범위는 인계 문서에서 확인합니다.
