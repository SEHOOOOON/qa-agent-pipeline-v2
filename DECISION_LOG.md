# QA Agent Pipeline 의사결정 기록

최종 갱신: 2026-09-19
사용자: 오세훈

## 2026-09-19 교육 기반 평가 후 사용자 질문의 최종 보고 연결

사용자가 평가에서 확인한 보완점 수정을 요청했다. 현재 Agent 2는 이미 조작 분리·관찰 시점·복원 확인 지침과 CP2-020/021을 갖고 있으므로 과거 TC를 덮어쓰거나 검사를 더 엄격하게 확장하지 않는다. 관련 정상·오류 반례를 재검사한다. 빠진 사용자 질문 전달은 기존 최종 확인 사항 경로에 연결한다. 검증 실행 시 Manifest와 분석 해시를 대조하고 질문 원문을 보존하며, 새 계약 필드·모델 호출·공식 자산 변경 없이 Agent 4 및 사람 검토서로 전달한다. 기존 저장 Run과 외부 게시물은 그대로 유지한다.

## 2026-09-19 원본 MED 등록 승인 적용

사용자가 원본 MED의 SRS·공식 TC 등록까지 명시적으로 승인했다. 원본을 백업하고 실제 UI의 SRS 동의·등록 최종 확인을 수행했으며 `사용자 승인 · Codex 대행`으로 기록했다. 자동 검사 통과를 사람 승인으로 대체하지 않았고, 이 승인을 향후 다른 후보에 대한 포괄 승인으로 사용하지 않는다. 원본 승인 후 자동 원복은 하지 않는다.

등록 자산의 TC·코드·SRS 개정 해시와 제안 문구만 반영된 SRS 차이를 확인하고, 공식 자산 로더를 통해 실제 재실행했다. 촬영에는 실제 승인 조작을 사용한다. 기존 조회 전용 촬영 도구의 미등록 전제는 보존하며 등록된 상태를 미등록인 것처럼 재연하지 않는다. Notion 로그인·외부 영상 게시가 완료되지 않은 상태에서는 최종 게시 완료로 표시하지 않는다.

등록 후 Git 저장 필터 검수에서 Windows CRLF 승인 파일이 기존 `eol=lf` 정책으로 바뀌어 Registry의 바이트 해시와 달라지는 문제를 확인했다. 공식 자산은 `-text`로 줄바꿈 변환을 막고 이미 승인된 파일과 해시를 그대로 보존한다. LF·CRLF 두 경우의 저장 필터 동일성 회귀 검사를 추가했다. 무결성 검사를 완화하거나 승인 파일을 재작성해 맞추지 않는다. CLI 인계 테스트의 공식 TC 한 건 고정 기대값도 실행 전 실제 등록 목록을 대조하도록 바꿨다.

## 2026-09-19 최종 감사 반례 3종 보완

### 후속 촬영 준비 재검수

V1의 `record_demo.py`를 읽기 전용으로 조사한 결과 Playwright 영상 녹화와 `imageio-ffmpeg`를 사용했다. 현재 환경에도 필요한 패키지와 FFmpeg가 있어 OBS 설치는 필수가 아니다. V1의 고정 Workflow 시연·정상 회귀 로그·외부 보고 캡처를 V2의 새 실제 실행으로 재사용하지 않는다.

사용자의 재검수·문제 보완·진행 요청에 따라 별도 V2 촬영 도구를 작성했다. 현재는 원본 자산 승인이 별도로 확인되지 않았으므로 조회 전용 90초 미리보기만 생성한다. 촬영 브라우저의 로컬 GET 이외 요청을 차단하고 서버의 새 모델 실행·승인 기능을 끈다. 원본 상태·결과 텍스트를 바꾸지 않고 글자 확대와 명시적인 설명 자막만 적용한다. 대기 구간을 잘라 합친 편집 영상이며 실시간 모델 실행 녹화로 설명하지 않는다. 원본 MED 등록 완료 영상 및 외부 게시는 별도이다.

### 코드 반례 수정

최종 검수에서 UI 문구의 부분 단어가 기대값으로 허용되는 문제, 온도·모드 전용 경로의 TC 단계 순서 검사 누락, 승인 전 재시험 중 변경된 HTML에 PASS 해시가 연결되는 문제를 재현했다. 사용자의 수정·검수·커밋·푸시 요청에 따라 완전한 단어/한국어 조사 경계 검사, 모든 현재 계획의 단계 대조, 재시험 전후 제품·코드 해시 대조를 추가했다. 특정 MED 값이나 TC ID에 고정하지 않는다.

새 계획 계약은 4.4(`plan_fidelity_contract=1.0`), 모델 안내는 3.28이다. 과거 실행 원본의 판정은 당시 계약으로 보존하되 현재 공식 승인 검사는 새 규칙을 적용한다. 이 검사는 자연어 전체 의미나 시험 중 변경 후 원복까지 탐지하는 감시 기능이 아니다. 원본 공식 자산 등록과 영상 녹화 완료는 별도 실제 수행 증거가 있어야 기록한다.

## 2026-09-19 촬영 리허설과 검토 사유 표시 보완

사용자는 촬영 준비 중 발견한 문제를 최대 3차까지 보완하고 API 사용을 최대 2회로 제한했다. 비용 범위는 보수적으로 개별 모델 호출 2회로 취급하며, 이미 확보한 실제 실행 증거를 우선 사용한다. 원본 MED 승인 상태는 촬영 전 보존하고, 별도 Run·SRS·자산 복사본에만 승인 리허설을 수행한다. 성공 결과나 승인 완료 화면을 조작하지 않는다.

첫 검수에서 Agent 4의 구조화 검토 항목이 화면에 Python 사전 전체 문자열로 표시되는 가독성 문제를 확인했다. 1차 보완으로 TC ID와 기록된 판단 근거를 표시하고, 과거 문자열·근거 누락도 그대로 구분한다. 보고 원문·분류·자동 검사·승인 계약은 바꾸지 않는다. 영상 구성은 정상 변경/실제 승인, 제품 불일치, 미정 조건 분리의 세 사례를 90초로 편집하며 저장 결과 조회와 새 실제 실행을 구분한다. 상세 대본은 기존 프로젝트 안내에 통합하고 자막은 별도 SRT 산출물로 둔다.

## 2026-09-18 사전조건별 모델 입력 안내와 재작성 범위 명시

촬영 전 실제 MED 실행에서 상세 TC와 복원 비교 연결은 통과했으나, 첫 계획은 장비 선택 인터페이스를 지원되지 않는 CLICK으로 사용했다. 재작성은 SELECT_DEVICE로 고쳤지만 원문에 연결되지 않는 target_device_visible 확인을 새로 추가해 CP3에서 차단됐다. 이 실행을 성공으로 바꾸거나 결과 원본을 수정하지 않는다.

사용자 추가 승인으로 Agent 3 입력에 현재 검사기의 원문 패턴·관찰 근거와 일치하는 `precondition_context_bindings`를 제공하고, Action 수정 시 정상 사전조건·Assertion·복원 확인을 유지하도록 안내한다. [OpenAI의 프롬프트 지침](https://developers.openai.com/api/docs/guides/prompt-engineering)의 명시적 지시·관련 문맥 분리 원칙을 참고했다. 모델·검사 규칙·기대값·재작성 횟수·인계 계약은 유지하고 prompt 3.27로 구분한다. 입력 안내가 의미 검증이나 모델의 반복 안정성을 보장하지 않으며 새 실제 실행·브라우저 결과로 별도 확인한다. 공식 SRS·TC 승인은 수행하지 않는다.

## 2026-09-16 화면 복원과 내부 초기값 비교 구분

사용자 승인으로 복원 확인의 대상별 비교 기준을 명시한다. 문제는 기대값 통제 자체가 아니라 자유 문장의 연결 어미를 반려하고 서로 다른 관찰 계층의 초기값 근거를 혼동한 데 있었다. Agent 2는 화면 표시 근거가 없으면 시험 전 상태와 비교하도록 작성하고, 내부 코드를 UI 표시로 가정한 초안은 CP2에서 보완한다. Agent 3는 원문의 대상별 구절과 OBSERVED_BASELINE/PROVED_INITIAL을 연결한다. [OpenAI 구조화 출력 안내](https://developers.openai.com/api/docs/guides/structured-outputs)에 따른 형식 제약과 별개로 원문·대상·초기값 증명을 검사하며 모델의 선언만 신뢰하지 않는다.

같은 관찰 대상의 기존 컴파일러 비교를 유지하고 새 Selector·기대값은 허용하지 않는다. 대상끼리 초기값이나 비교 문구를 공유하지 않으며 일반 연결 어미는 처리한다. 복원 확인 오류(CP3-006B)와 실제 조작 누락을 구분한다. Agent 2 3.3/상세화 1.2, Agent 3 4.3/복원 1.2 계약으로 새 실행만 변경하고 과거 Run·SRS·공식 자산은 보존한다. 실제 실패 TC의 명확화 사본과 기능별 대역 실행은 새 Live 모델 성공으로 취급하지 않는다.

## 2026-09-16 복원 확인 원문과 자동화 비교의 명시 연결

사용자가 상세 복원 문장을 계획에 연결하고 다른 요구사항에서도 같은 문제가 생기는지 확인하도록 요청했다. 저장된 MED 계획의 조작·Assertion 자체는 있었으나 문장 해석기가 설명 단어를 지원하지 않아 CP3가 차단했다. TC 문장이나 기대값을 줄이지 않고 Agent 3 계획에 원문과 기존 ER ID를 연결하는 `restore_confirmations`를 추가한다. [OpenAI 공식 구조화 출력 안내](https://developers.openai.com/api/docs/guides/structured-outputs)를 참고하되 형식 검증만으로 완료하지 않고 원문·대상·컴파일러 지원 비교·초기값 증명을 대조한다.

관찰 Selector·새 기대값·새 제품 Assertion은 추가하지 않는다. 생성 코드는 기존 복원 비교를 유지하고 실제 비교 성공 로그만 추가한다. 다른 기능의 표현과 실제 복원 실패 반례를 로컬 시험하고, 과거 Run·실패 결과·노션 기록은 수정하지 않는다. 새 모델 호출·전체 Live·외부 재게시는 별도이며 로컬 저장 계획 재현과 구분한다. 여전히 제한된 자연어 표현만 지원하므로 모든 TC 표현에 일반화됐다고 설명하지 않는다.

## 2026-09-16 이미 요청된 검사의 근거 연결과 추가 범위 구분

MED 실제 실행에서 요청에 있는 화면·내부값 조건을 공통 요구사항에 연결하면서 CP1이 추가 검사 범위로 보류했다. 사용자 승인으로 `REQUEST_TRACE_ONLY`를 추가해 근거 연결과 새 검사 요청을 구분한다. 이름만 바꾸어 통과시키지 않도록 VERIFY만 허용하고 모든 연결 조건의 요청 출처·원문 동일성·전체 목록을 검사한다. Agent 2 기대결과도 해당 요청 원문 한 문장을 유지하도록 제한하며, 관련 Requirement 이름만으로 알림/상태 검사를 강제하지 않는다. 추가 기대값·관련 SRS 전체 검사·개정은 이 경로로 허용하지 않는다. 간접 영향과 직접 범위 확인은 기존 보류 규칙을 유지한다.

구조화 형식만으로 내용의 정확성이 확보되는 것은 아니므로 [OpenAI 공식 Structured Outputs 안내](https://developers.openai.com/api/docs/guides/structured-outputs)를 참고해 작성 지침과 실제 검사 규칙·반례 테스트를 함께 보완했다. Agent 1 2.6/범위 1.1 계약과 prompt 2.11, Agent 2 prompt 2.26으로 구분하며 모델·재시도 횟수·기존 Run·공식 자산은 바꾸지 않는다. 자연어 관계 자체를 모두 검증하거나 모든 초안을 통과시키는 기능으로 설명하지 않는다.

## 2026-09-16 합의한 예시 수준의 TC 절차·복원 확인 보완

실제 통합 실행에서 Agent 2가 대상 선택과 복원 확인 설명을 생략했는데도 기존 상세화 검사를 통과했습니다. 실제 생성 자동화에는 두 동작이 있었으므로 제품 판정 누락과 TC 문서 품질 공백을 구분했습니다. 사용자와 합의한 상세 예시를 적용하기 위해 새 TC 필드는 추가하지 않고 기존 사전조건·steps·expected_results·restore_steps를 유지합니다. 풍량·온도·읽기 전용 작성 예시를 지침에 포함하고, 예시의 값이나 단계 수를 고정 규칙으로 쓰지 않습니다. 예시 기반 지침 구성은 [공식 Prompt engineering 문서](https://developers.openai.com/api/docs/guides/prompt-engineering)의 서로 다른 입력/출력 예시 활용 방식을 참고했으며, 모델·SDK·전송 방식은 변경하지 않았습니다.

새 CP2-021은 필요한 대상 선택 순서와 복원 확인 설명의 명시 항목을 검사합니다. 검사 실패는 기존 최대 1회 재작성 경로로 전달하며 미해결 결과는 인계하지 않습니다. 요청 원문의 복원 메모는 정확히 보존하고 확인 문장만 뒤에 추가합니다. Agent 3는 그 확인 문장을 기존 관찰 대상·초기값 증명·컴파일러 복원 비교와 연결하며, 확인을 위한 가짜 클릭이나 제품 기대결과 추가는 허용하지 않습니다. 컴파일러는 변경하지 않습니다. 새 Agent 2 상세화 1.1과 Agent 3 복원 확인 계약으로 버전을 나누고 과거 실행 결과를 소급 수정하지 않습니다. 자연어 의미 전체나 모델의 반복 품질을 보장하는 검사로 표현하지 않으며 최신 검증 수치와 다음 실제 실행은 인계 문서에서 관리합니다.

## 2026-09-15 실행 보고와 상세 TC의 Notion 연결

사용자 요청에 따라 실행 결과가 있는 TC의 사전조건·조작·단계별 기대결과·복원을 Notion 실행 페이지 본문에 연결한다. 신규 초안 단독 게시 서비스나 기존 TC의 재작성은 추가하지 않는다. 기존 인계 무결성 검사 뒤 저장된 내용을 사용하고, 과거 명세의 빠진 판정 단계는 추정하지 않는다. 수동 메모를 삭제하지 않는 내용 해시별 추가 기록 방식을 사용하며, 같은 내용 재전송·부분 실패 후 재시도를 검사한다. 기대결과와 실제 관찰값·사람의 공식 등록 승인은 구분한다. 실제 외부 전송과 유료 모델 재호출은 이번 코드 연결 검증에 포함하지 않는다.

## 2026-09-15 생성 TC 초안의 절차·관찰 위치·판정 시점 상세화

사용자가 승인한 상세 수준에 맞춰 Agent 2가 대상 선택·값 선택/입력·적용을 구분하고 사전조건·기대결과·복원을 구체화하도록 한다. 준비 확인과 제품 판정을 섞지 않으며 단계 수를 고정하지 않는다. 새 ER의 observation_target은 사람이 읽는 위치이고 verify_after_step은 단일 흐름에서도 필수다. CP2-020으로 명시적인 압축 절차·모호한 확인 대상·판정 단계 누락을 검사하고 Agent 3는 연결 조작 직후 판정을 강제한다. 입력에 없는 UI나 기대값을 추가하는 상세화는 금지한다. 새 tc_detail_contract로 과거 Run·승인 TC를 보존한다. 사용자 캡처와 공식 자산을 수정하거나 새 모델 결과를 만들었다고 주장하지 않는다. 노션은 현재 결과 요약만 전송함을 확인했으며 상세 TC 게시 기능·실제 전송은 이번 TC 생성 수정과 구분한다.

## 2026-09-15 요청 밖 검사 범위의 근거와 인계 통제

사용자 승인으로 Agent 1의 대상 외 VERIFY·UPDATE_REQUIRED에 요청 조건과 연관 SRS를 연결하는 범위 근거를 추가한다. 명세에 존재한다는 사실을 이번 변경의 영향 증명으로 취급하지 않는다. CP1-011은 누락·허위 출처를 제한 재작성 대상으로 만들고, 간접 영향이나 직접 대조되지 않는 자연어 표현은 REVIEW·PAUSE로 처리한다. 직접 요청의 자동 대조는 원문에 Requirement ID 또는 SRS 요구사항/인수 기준 전체 문장이 있는 좁은 범위로 제한한다. 일반 의미·인과관계 판독기로 표현하지 않는다. 특정 풍량 TC나 알림 기능을 강제 제외하지 않으며 요청을 임의로 축소하지 않는다. 새 Manifest의 scope_guard_contract로 구분해 과거 증거를 보존한다. 사용자 TC 캡처는 참고 자료이며 원본 TC·제품·SRS를 변경하지 않는다. 생성 TC 절차 상세화는 이번 범위 보완과 별도 작업이다.

## 2026-09-14 승인 TC 재사용 판단용 명세 전달 보완

실제 HIGH 재사용 확인에서 Agent 2에 기존 TC의 기대결과만 전달되고 사전조건·조작·복원은 빠져 신규 후보를 설계하는 문제가 확인됐다. 사용자 승인으로 해시·구조 검사가 끝난 승인 TC의 제품 명세를 허용 필드로 추려 카탈로그와 모델 입력에 연결한다. 등록 메타정보·파일 경로·자동화 코드·과거 Condition ID는 전달하지 않는다. 최초 설계와 제한된 재작성에 같은 명세를 제공하며 특정 TC 재사용을 강제하지 않는다. 명세 내용은 실제 수행 증거와 구분하고 CP2 검사·공식 등록 승인 기준은 유지한다. 상세 정보가 없는 과거 Snapshot은 소급 보강하지 않으며 기준 회귀 요약도 기존 상태를 유지한다. 실제 재실행 결과는 인계 문서에서 구분한다.

## 2026-09-12 긴 진단 경로 저장 오류 수정

사용자 승인으로 공통 원자적 저장의 임시 파일명을 짧게 하고 Agent 3 모델 계획을 검사 전에 시도별로 보존한다. 같은 폴더의 고유 임시 파일과 원자적 교체는 유지하며 검사 기준·제품 기대값·API 재시도 정책은 바꾸지 않는다. 무API 재현으로 저장 오류를 확인하되 유실된 실제 모델 계획의 검사 실패 원인을 추정하거나 복구했다고 주장하지 않는다. 최종 경로 자체가 운영체제 제한을 넘지 않도록 짧은 실행 루트를 사용하는 원칙도 유지한다.

## 2026-09-12 사전조건 지침·재작성 안내만 보완

사용자는 정상 TC가 실행 전에 제외된 문제에 대해 AI 지침과 구체적인 오류 안내 두 항목만 보완하도록 승인했다. 관찰 가능한 모든 상태를 새 사전조건으로 추가하지 않도록 지침을 정리하고, 기본 문맥 검사 실패에 확인 항목 번호·이름·원문·실패 이유를 제공한다. 기존 허용 규칙과 재시도 횟수는 바꾸지 않으며 필요한 조건을 삭제해 통과시키지 않는다. 모델 대역으로 잘못 추가된 확인의 재작성 인계와 필수 조건 누락 차단을 검증한다. 새 관찰 기능·선택 상태 계약·제품 변경·보고 양식 개편은 추가하지 않는다. 포트폴리오 본문은 사용자 흐름과 실제 증거를 중심으로 설명하고 이 세부 규칙을 전부 나열하지 않는다. 최신 프롬프트의 실제 API 성공은 별도 확인 사항이다.

## 2026-09-12 사전조건의 실행 전 실제 값 확인

사용자가 남은 사전조건 증명 보완을 승인했다. 새 에이전트나 자연어 의미 판정 엔진을 추가하지 않고, Agent 3의 구조화 계획에 사전조건 원문별 읽기 전용 확인 항목을 연결한다. CP3는 원문·관찰 경로·명시 값·기본 문맥 누락을 확인하고 컴파일러는 준비 행동 뒤 본 TEST 전에 실제 값을 비교한다. 준비 행동 자체를 충족 증거로 대신하지 않는다.

읽기 근거를 제공할 수 없으면 해당 TC의 자동화 제외 사유로 남기고, 런타임 값 불일치는 제품 판정 전 중단·자동화 문제·HOLD로 보고한다. 성공 stdout에도 실제 관찰값을 남긴다. 기본 장비 문맥의 온라인·오류·잠금·표시는 각각의 가상 제품 사실이며 관리자 인증이나 실제 네트워크 연결을 증명하지 않는다.

현재 CP3 기본값과 새 실행·현재 신규 후보 승인은 증명을 요구한다. 과거 Manifest에는 계약을 소급 삽입하지 않으며 역사적 인계는 당시 계약으로 유지한다. 기존 승인 자산도 소급 변경하지 않는다. 과거 계약용 단위 테스트는 별도 test-support 어댑터로 기존 규칙을 시험하고 신규 증명 테스트와 구분한다.

정상·누락·불충족 반례 및 저장된 MED TC의 무API 브라우저 재시험을 통해 구현 연결을 확인한다. 직접 작성한 확인 계획을 새 모델 산출물로 표현하지 않는다. 최신 프롬프트 전체 Live와 임의 자연어 의미의 일반적 정확성, 반복 평가는 이번 증거만으로 완료했다고 주장하지 않는다. 유료 API·외부 전송·공식 등록·커밋·푸시는 수행하지 않는다.

## 2026-09-12 후속 감사: 신규 TC 기대값과 최종 소비 단계 보완

사용자가 감사에서 확인한 문제의 수정 착수를 승인했다. MED 요구를 HIGH 기대값으로 바꾼 신규 TC도 기존 CP2가 통과시키는 반례는 모델 오류만의 문제가 아니다. 요구사항 ID 연결 검사가 실제 기대값의 일치 검사까지 대신한다고 취급한 검증 공백이었다. CP2에 연결 조건별 명시 값·상태 방향 검사를 추가하고 과거 계약과 새 검사를 버전으로 구분한다. 차단해야 하는 입력값과 허용 출력값을 혼동하지 않으며 특정 풍량 값을 제품 규칙으로 하드코딩하지 않는다.

같은 범위에서 CP3의 고정 이름만 검사하는 상태 Assertion, 활성 기대에 대한 비활성 전략, 준비 전/후 복원 기준 혼동을 보완한다. 최종 승인·보고의 원본 대조, 같은 브리지 내 승인 동시 진입, 다른 Run 선택 후 늦은 POST 응답, 필수 인터페이스 누락과 내부 오류의 분리를 보완한다. 제품 동작·기존 Run·공식 자산을 수정해 PASS를 만들지 않는다.

임의의 자연어 사전조건 전체에 대한 충족 증명은 아직 미완료다. 새 의미 추정 규칙을 급히 추가해 증명이 된 것처럼 처리하지 않고 별도 후속 항목으로 남긴다. 이번 보완은 유료 API·외부 전송·공식 등록·커밋·푸시 승인으로 확대하지 않는다. 세부 결과는 PROJECT_HANDOFF.md와 자동 테스트 카탈로그에서 관리한다.

## 1. 이 문서의 목적

이 문서는 구현 명세가 아니라 왜 이 프로젝트를 시작했고, 무엇을 중요하게 생각하며, 왜 현재 구조를 선택했는지 새 작업에 전달하는 기록입니다.

핵심 생각은 다음과 같습니다.

> AI가 QA 결과를 생성했다는 사실보다, 그 결과가 어떤 근거에서 나왔고 정해진 품질 기준을 지켰으며 실제 실행과 증거로 연결되는지를 확인할 수 있어야 한다.

대화 원문을 그대로 복사하지 않고 반복해서 확인된 생각과 방향 변경의 이유를 정리했습니다.

## 2. 포트폴리오를 만든 이유

오세훈은 Data QA/QC와 Software QA/QE 경험을 바탕으로 단순 테스트 수행자보다 다음 역량을 보여주고자 했습니다.

- 모호한 요구사항을 검증 가능한 조건으로 바꾸는 능력
- 정상·예외·경계값·상태 전이·회귀 영향을 보는 관점
- UI뿐 아니라 데이터·로그·내부 상태의 정합성을 비교하는 방식
- 실패를 바로 제품 결함으로 단정하지 않고 원인을 분리하는 판단
- TC·체크리스트·코드·보고서를 재사용 가능한 품질 자산으로 남기는 습관
- 반복 작업은 자동화하되 자동화 결과 자체도 검증하는 태도
- AI 산출물을 그대로 믿지 않고 사람이 정의한 기준으로 통제하는 방식

포트폴리오의 중심 메시지는 `검증 기준을 구체화하고 품질 지식과 반복 검증을 다시 사용할 수 있는 구조로 만드는 QA Engineer`입니다.

## 3. 프로젝트 1의 의도와 뒤늦게 발견한 한계

프로젝트 1은 가상 중앙제어 시스템에서 요구사항 분석, 3단계 QA 기준 기반 TC 설계, Checkpoint 검토, Playwright 실행, UI·내부 상태 Double-Assert, 실패 원인 분류와 보고까지 한 흐름으로 보여주기 위해 시작했습니다.

5가지 원인 분류는 실패를 많이 보여주려는 것이 아니라, 실무에서 모든 비통과가 제품 결함은 아니며 원인에 따라 다음 조치가 달라진다는 점을 보여주기 위한 Dataset입니다. 정상 회귀 실행 영상과 분류 Dataset은 서로 목적이 다릅니다.

Antigravity 자체가 Agent 플랫폼이어서 애플리케이션 내부의 실제 모델 호출과 플랫폼이 만들어 준 산출물을 처음에는 구분하지 못했습니다. 완성 후 코드를 다시 확인하면서 다음을 발견했습니다.

- Agent 1은 저장된 요구사항 분석 예시였습니다.
- Agent 2는 미리 작성된 제품 기능 TC였습니다.
- Agent 3는 신규 TC 코드를 생성하지 않고 사람이 작성한 기존 Playwright 테스트를 실행했습니다.
- 다른 요구사항을 입력해도 분석·TC·코드가 실제로 달라지는 구조가 아니었습니다.
- 화면의 단계 전환과 실제 산출물 인계는 같은 의미가 아니었습니다.

따라서 프로젝트 1의 정확한 성격은 `Fixture 기반 Workflow Prototype`입니다. 가상 시스템, QA 기준, 기존 회귀 테스트, Double-Assert와 보고 구조는 유효하지만 실제 Agentic Pipeline이라는 설명과 구현 사이에는 간격이 있었습니다.

이 한계를 포트폴리오 제출 전에 스스로 발견한 일도 중요한 QA 경험으로 봅니다. 화면이 그럴듯한지보다 입력과 결과 사이에 실제 인과관계가 있는지를 다시 검증했기 때문입니다.

## 4. V2를 별도로 만든 이유와 대표 시나리오

프로젝트 1을 전부 다시 만들면 기존 자산을 훼손하고 범위가 지나치게 커집니다. 프로젝트 1은 읽기 전용 기준 자산으로 유지하고, 실제 모델 연결과 자동 인계를 V2에서 단계적으로 보완하기로 했습니다.

V2가 증명해야 할 것은 세 가지입니다.

1. 입력 변경이 Agent 결과를 실제로 바꿉니다.
2. 앞 단계의 검증된 JSON이 다음 Agent의 실제 입력이 됩니다.
3. Agent 2의 제품 기능 TC가 Agent 3 자동화 후보와 시험 결과에 영향을 줍니다.

아무것도 없는 상태에서 전체 SRS·TC·자동화를 생성하는 대신, 운영 중인 가상 중앙제어 시스템에 변경 요청이 들어오는 변경관리 시나리오를 선택했습니다. Product SRS, 기존 TC, 사람이 작성한 기준 Playwright 자동화와 실행 결과가 이미 존재하는 상태에서 `MODIFIED` 요청부터 처리합니다.

AUTO 온도 범위 변경은 연결 검증용 예시일 뿐 대표 요구사항으로 고정하지 않습니다. Prompt나 Checkpoint에 특정 값과 TC ID를 하드코딩하지 않고 서로 다른 유효 요청에도 같은 계약이 적용되어야 합니다.

## 5. Product SRS와 Agent 역할

Agent가 근거 없는 내용을 만들었는지 판단하려면 공식 제품 기준이 필요합니다. 프로젝트 1 화면과 테스트에 흩어진 규칙을 역추적해 `docs/01_PRODUCT_SRS.md`로 정리했습니다.

- 화면과 코드에서 확인 가능한 내용만 제품 요구사항으로 사용합니다.
- QA Drawer, Register와 `window.__vccs`는 제품 기능과 테스트 지원 인터페이스를 분리합니다.
- 화면에 없는 AI 운전, 이중화와 실제 프로토콜 기능을 추가하지 않습니다.
- 기대 기준과 현재 구현이 다르면 감사 문서에 차이를 남깁니다.

Agent 역할은 다음처럼 고정합니다.

- **Agent 1 변경 분석**: SRS와 변경 요청을 비교해 변경 전·후, 영향 범위, 확정 정보와 정보 부족을 구조화합니다. TC를 만들지 않습니다.
- **Agent 2 제품 기능 TC 설계**: 사전조건·단계·테스트 데이터·기대 결과, Requirement 추적성과 Double-Assert를 정의합니다. Playwright 코드를 만들지 않습니다.
- **Agent 3 자동화 후보 구현**: Agent 2 TC를 실제 화면에서 실행할 계획과 Playwright 후보로 변환합니다. 테스트 목적, Requirement ID, 경계값과 기대 결과를 바꿀 수 없습니다.
- **Agent 4 결과 분석**: 제품·자동화·환경·근거 부족을 분리하고 보고 정합성을 확인합니다. 현재는 생성형 AI가 아니라 규칙 기반 분석기입니다.

## 6. Agent 3와 Checkpoint 설계 결정

사용자는 Agent 3가 기존 스크립트 실행에 머물지 않고 실제 자동화 후보를 만들기를 원했습니다. 그러나 모델이 자유롭게 Python 전체를 작성하고 바로 실행하면 기대값 변조, Assertion 삭제, 가짜 Selector와 위험 코드 문제가 생길 수 있습니다.

그래서 다음 구조를 선택했습니다.

```text
Agent 2 TC
→ 실제 UI Inventory
→ Agent 3 모델이 제한된 행동·Assertion 계획 생성
→ 허용 목록 컴파일러가 Playwright Python 생성
→ CP3 정적 검사
→ 신규 자동화 후보 시험(Candidate Trial)
```

모델은 구현 계획을 판단하고 컴파일러는 허용된 동작만 코드로 만듭니다. 화면 조사는 Selector 현실성을 높이지만 제품 기대 결과의 정확성을 보장하지 않습니다. 기대 결과는 SRS와 Agent 2 TC에서 고정됩니다.

Checkpoint는 계약·ID·출처·근거·Double-Assert·코드 추적성·금지 패턴·SHA-256 인계를 검사합니다. 자연어 의미를 완벽히 판단한다고 주장하지 않으며, 자동 결정이 어려운 문제는 `REVIEW` 또는 `PAUSE`로 남깁니다.

초기 Agent 3는 실행 전에 선택 TC를 고정 행동·검증 조건으로 표현할 수 있는지 결정론적으로 판정하고, 미지원 TC를 모델 호출 전에 `NOT_AUTOMATABLE`로 종료했습니다. 전체 허용 목록을 매번 조사하지 않고 선택 TC에 필요한 Selector와 하네스만 확인한 점은 유지하되, 아래 유연성 보완으로 미지원 처리 방식을 확장했습니다.

### Agent 3 통제와 유연성 사이에서 확인한 핵심 어려움

AI의 환각, 기대값 변경과 불안정한 코드를 막기 위해 제품별 허용 행동과 Selector를 강하게 고정했지만, 이 방식은 품질 통제를 높이는 대신 새로운 관제점이나 유사 기능이 추가될 때 사전 등록되지 않았다는 이유만으로 자동화를 중단시키는 한계가 있었습니다. 이는 프로젝트에서 실제로 겪은 핵심 설계 어려움이며 이후 회고·면접 설명에서 반드시 보존합니다.

해결 원칙은 **변경하면 안 되는 QA 기준**과 **확장할 수 있어야 하는 자동화 기술**을 분리하는 것입니다. SRS 기대값, Requirement ID, Expected Result, 검증 조건 삭제 금지와 증거 규칙은 계속 고정합니다. 반면 UI Locator, 클릭·입력·선택·체크 방식과 읽기 전용 상태 관찰은 실제 화면 근거 안에서 AI가 유연하게 구성할 수 있게 합니다. 특정 기능 예시는 설명용일 뿐 Prompt·Requirement 목록·코드에 제품 고정값으로 추가하지 않습니다.

`agent3-3.5`부터 기존 온도 기능은 필요한 UI만 좁게 확인하고, 처음 보는 기능은 안정적인 ID·`data-testid`·접근성 이름을 가진 범용 UI와 읽기 가능한 내부 상태를 동적으로 확인합니다. AI는 실제 관찰 요소로 실행 가능한 구조화 코드 의도를 만들며, 컴파일러는 이를 안전한 Python으로 변환합니다. 범용 조작으로 표현할 수 없으면 `AUTOMATION_SUPPORT_EXTENSION_REQUIRED`와 기술 사유를 남기고 코드를 생성하지 않습니다. 내부 JSON의 `Capability`, `Eligibility`, `Assertion` 같은 기존 영문 필드는 호환을 위해 유지하되 사용자 문서에서는 각각 자동화 지원 범위, 자동화 가능성 사전 확인, 검증 조건으로 설명합니다.

2026-08-15 진단 Live Run에서 행동과 Selector의 대응을 프롬프트에 더 명시해야 불필요한 CP3 재작업을 줄일 수 있음을 확인했습니다. 컴파일러가 사용하지 않는 `expected_text`는 모델 자유 입력으로 남기지 않고 CP3에서 차단합니다. 토큰 비용은 마지막 호출이 아니라 모든 계획 시도의 누적으로 기록하고, 오류 산출물이 생긴 시도는 불변 진단 기록으로 보존해 재시도와 섞지 않습니다.

2026-08-17 범용 전원 선택·적용 Live에서는 공용 `requested_mode`의 OPERATION·STOP을 HVAC 모드로 오인한 사전 판정, 한국어 조사 때문에 `적용`과 `적용을`·`status`와 `status가`를 연결하지 못한 CP3, Expected Result 문장 전체를 실제 알림 문구로 사용한 계획, 범용 복원 후 상태 미재확인을 발견했습니다. `agent3-3.7`은 HVAC 고정값과 그 밖의 상태값을 분리하고, 2글자 이상 핵심어의 조사·어미 포함 연결을 허용하며, 알림 검증에는 Expected Result의 짧은 근거 구절만 허용합니다. 컴파일러는 범용 복원 전 UI·내부 상태를 저장하고 복원 Action 후 초기값과 직접 비교합니다. 실패 Run은 삭제하지 않고 재시도별 진단 증거로 보존했습니다.

첫 보완 후 두 번째 진단 Live Run에서는 Action Selector 문제가 사라졌지만 모델이 내부 상태 인터페이스에 인덱스와 속성을 덧붙여 다시 CP3 재작업이 발생했습니다. 이에 Action뿐 아니라 Assertion 전략별 Selector도 프롬프트와 CP3에서 동일하게 고정했습니다. 또한 Playwright Trace 내부에는 stdout 마스킹과 별개로 로컬 URL·stack/source 경로가 남으므로, 알려진 로컬 경로의 문자열·URI·JSON escape 형태를 ZIP 내부에서 치환한 뒤 Manifest 해시를 계산하기로 했습니다.

`agent3-3.2` 세 번째 진단 Live Run은 첫 계획 CP3 PASS와 Trace 로컬 경로 0건을 확인했지만, 모델이 `target_device_id=1`을 주고도 `SELECT_DEVICE` Action 값은 비워 두었습니다. 컴파일러는 대상 ID를 직접 사용해 실행 결과에는 영향이 없었으나 구조화 계약의 일관성 문제이므로, `agent3-3.3`부터 CP3가 TC ID·MVP 대상 장비 ID와 선택 Action 값을 함께 검사합니다.

같은 Run의 Screenshot에서 “성공적으로 적용되었습니다” Toast가 표시됐는데 기존 `TOAST_VISIBLE`은 ER-007 “차단 안내 Toast”를 통과시켰습니다. 정확한 전체 문구를 고정하지 않는 SRS 원칙은 유지하되, `agent3-3.4`부터 차단 기대 결과는 `TOAST_BLOCKING` 전략과 제한된 차단 의미 신호를 사용합니다. 로컬 재시험에서 성공 Toast가 ER-007 불일치로 추가 기록되는 것을 확인했습니다.

현재 `agent3-3.4` 공개 Live Run은 첫 계획에서 CP3 PASS했고 input 2,533·output 594·total 3,127 tokens를 사용했습니다. `SELECT_DEVICE value=1`과 `TOAST_BLOCKING` 계약을 준수한 신규 자동화 후보 시험은 ER-005·006·007의 `PRODUCT_MISMATCH_CANDIDATE`를 기록했습니다. 모든 인계 SHA-256과 Project1 대상 해시가 일치하고 공개 텍스트와 Trace의 로컬 경로·비밀정보 검사도 통과했으므로, 이 Run을 현재 Agent 3 공개 증거로 채택합니다.

최소 Orchestrator는 기존 Agent 함수와 단계별 Manifest를 다시 구현하지 않고 같은 Run ID로 조합합니다. 대상 HTML을 첫 모델 호출 전에 확인하고, 단계 종료 코드가 0이 아니면 후속 Agent를 호출하지 않으며, 별도 `orchestrator_manifest.json`에는 종료 코드·중단 단계·기존 Manifest SHA-256만 기록합니다. `PRODUCT_MISMATCH_CANDIDATE`는 제품 판정 후보를 얻은 정상 Pipeline 완료로 유지합니다. 실제 `RUN-20260815-062606-E36A76`에서 자리표시자 `after_value`를 Agent 1이 정보 부족으로 판정해 CP1 `PASS + PAUSE`가 되었고 Agent 2·3 호출 없이 중단됐습니다. 이 Run은 중단 계약의 Live 증거지만 A1→A3 정상 완료 증거는 아닙니다.

구체 요청 Orchestrator `RUN-20260815-063800-3B624C`은 Agent 1·2가 PASS했지만 이전 공개 Run의 `TC-CAND-003`을 명시한 것이 새 Run에서는 LOCAL TC를 가리켜 Agent 3 Eligibility에서 중단됐습니다. Agent 1 1회·Agent 2 2회의 실제 누적 사용량은 total 34,814 tokens였고 Agent 3 모델은 호출하지 않았습니다. 생성형 단계의 TC ID는 Run 내부 식별자이지 Run 간 안정 키가 아니므로, Orchestrator 기본값은 현재 CP2 결과의 모든 TC를 Eligibility로 평가해 적격 후보를 결정론적으로 선택하는 `AUTO`로 바꿉니다. 전체 후보 판정은 `agent3_selection.json`에 저장합니다.

같은 Run의 Agent 2는 CENTRAL TC를 모두 `MULTIPLE_ALLOWED_TEST_DEVICES`로 만들고 나머지를 LOCAL로 만들어 현재 Agent 3가 실행할 후보가 0개였습니다. 제품 TC를 Agent 3 기술 제약 때문에 삭제하지는 않되, Agent 2 Prompt `agent2-2.3`에서 CENTRAL 변경 검증에 `PRIMARY_TEST_DEVICE` 자동화 후보를 최소 한 건 함께 만들도록 명시합니다. 이 보완은 현재 MVP 연결 후보를 확보하기 위한 Prompt 지침이며 CP2의 제품 기능 범위를 축소하지 않습니다.

보완 후 Orchestrator `RUN-20260815-090130-F023B1`은 Agent 1 `PASS + CONTINUE` 뒤 Agent 2 첫 시도의 중복·추적 오류를 재작업했지만, 재작업 결과가 LOCAL 직접 변경 Requirement 연결을 누락해 최종 CP2 FAIL로 중단됐습니다. Agent 1 1회·Agent 2 2회의 실제 누적 사용량은 total 30,227 tokens였고 Agent 3는 호출하지 않았습니다. 이는 최대 1회 재작업과 Fail-closed 계약이 실제로 동작한 증거이며 정상 완료 증거는 아닙니다.

이 비용 감사에서 Agent 1·2 Manifest 최상위 `usage`도 마지막 시도만 기록해 재작업 비용을 과소 집계하는 문제를 확인했습니다. 계약 2.3부터 Agent 1·2도 `usage`에 모든 시도 누적을 기록하고 `final_attempt_usage`에 마지막 호출을 분리합니다. 기존 공개 계약 2.2 산출물은 역사적 증거로 그대로 보존합니다.

세 번째 Run의 Agent 2 재작업은 첫 시도의 실패를 고치면서 기존 CP2-008 PASS를 새 FAIL로 바꿨습니다. 실패 메시지만 전달하면 보존해야 할 규칙을 모델이 직접 보지 못하므로, 이후 재작업에는 CP2 전체 `rule_id + PASS/FAIL + message`를 전달하고 PASS 규칙과 근거 보존·새 FAIL 금지를 명시합니다. 비용 상한을 바꾸지 않도록 최대 1회 재작업은 유지합니다.

네 번째 Orchestrator `RUN-20260815-092107-0C075E`은 Agent 1·2·3과 CP1·2·3을 모두 통과하고 AUTO가 `TC-CAND-001`을 선택해 Trial PASS로 정상 완료됐습니다. Agent 1 5,607, Agent 2 두 시도 누적 27,752, Agent 3 3,194로 총 36,553 tokens를 사용했습니다. 모든 단계·선택·시험 Manifest SHA-256과 Project1 대상 해시가 일치하고 텍스트·Trace 민감정보와 로컬 경로 검사도 통과했습니다. 이로써 최소 Orchestrator 정상 완료 Live 검증은 완료로 판단하되 Agent 4와 최종 보고까지 완료됐다고 표현하지 않습니다.

정상 완료 산출물 감사에서 복원 TC가 초기 온도를 적용만 하고 UI·내부 상태를 재확인하지 않는 틈을 발견했습니다. CP3-006은 복원 Action 존재만 보지 않고 변경된 모드·온도의 초기값과 CENTRAL 적용을 확인하며, 결정론적 컴파일러는 복원 후 UI 온도와 내부 `setTemp`를 재확인합니다. 복원 실패가 본 시험 오류를 덮지 않게 본 시험 성공 시에만 별도 `RESTORE_MISMATCH`로 실패시키고, 동일 Live TC·계획의 무API 재시험 PASS를 확인했습니다.

중간 사람 승인이 너무 많으면 Agent 흐름이 끊기므로 정상 PASS는 자동 진행하고, 근거 부족·정책 충돌·위험 실행에만 조건부 검토를 사용합니다. 모든 검증이 끝난 뒤 SRS·TC·자동화를 정식 재사용 자산으로 등록할 때 사람 승인 1회를 둡니다.

## 7. 중요하게 생각하는 품질 원칙

- **실제 인과관계**: 단계 애니메이션이 아니라 실제 JSON과 SHA-256이 다음 입력으로 전달되어야 합니다.
- **추측 금지와 정보 활용**: 확인 가능한 내용은 Agent 2가 활용할 수 있게 충분히 정리하되 근거 없는 내용을 확정하지 않습니다.
- **Double-Assert**: UI와 내부 시뮬레이터 상태 또는 명령 처리 결과를 함께 확인합니다.
- **실패 원인 분리**: 제품 불일치, 자동화 계획·코드 문제, 실행 오류, 환경·복원 문제, 요구사항 불명확과 증거 부족을 구분합니다.
- **자동화 신뢰성**: 실행 가능성, TC 의도 충실성, 반복 안정성과 대표 오류 검출을 분리해 봅니다.

현재 V2는 신규 자동화 후보 시험 1회와 후속 현재 후보 재검증·관련 기존 회귀 실행까지 구현했습니다. 정상 3회 반복과 대표 오류 검출은 후속이며 완료로 표현하지 않습니다.

## 8. 사실성·비용·보안 원칙

Fixture는 저장 결과이고 Live는 실제 모델 호출입니다. 사람이 작성한 기존 자동화와 Agent 3 Candidate도 구분합니다. Candidate Workspace는 원본과 분리된 임시 위치이지만 컨테이너나 OS 권한 격리를 제공하는 보안 Sandbox는 아닙니다.

API 키는 로컬 환경변수로만 사용합니다. GitHub 방문자는 공개 결과를 보고, 직접 실행할 사람은 자신의 키를 사용합니다. Live 실행 전 `--preview-only`로 전송 내용을 확인하며 키, 로컬 절대경로와 전체 HTML은 전송하거나 커밋하지 않습니다.

면접에서 직접 설명할 수 있도록 다음 과장 표현을 피합니다.

- 완전 자율 Multi-Agent 팀
- 자동화 코드 신뢰성 보장 또는 모든 결함 검출
- 구현되지 않은 Full Regression
- 규칙 기반 Agent 4를 생성형 Agent로 표현
- Fixture를 Live Agent 성능으로 표현
- Candidate Workspace를 보안 Sandbox로 표현
- Checkpoint가 자연어 의미를 완벽히 판정한다고 표현

## 9. 미룬 방향과 작업 기준

탐색적 테스팅 Agent는 관찰·가설·재계획·복원과 비용 통제가 복잡해 V2 이후로 미뤘습니다. Agent 품질 평가 Harness와 Dashboard PoC도 만들었지만 실제 반복 Live Trial이 부족하므로 V2 Agent가 반복 실행 가능해진 뒤 연결합니다.

모든 변경 유형, Full Regression, 여러 모델 비교, 완전한 Self-Healing과 자유로운 Agent 토론은 현재 MVP 종료선을 흐리므로 후순위입니다.

작업자는 평가 요청이면 파일을 수정하지 않고, 수정 전 문제·수정 전후·영향과 검증 방법을 설명해야 합니다. 사용자가 명령한 범위만 변경하며 코드와 문서 정합성을 함께 맞춥니다. 커밋 메시지는 한글로 작성하고 커밋·푸시는 요청받았을 때만 수행합니다.

## 10. 구현 경과와 당시 판단 기록

아래는 단계별 역사적 기록입니다. 각 시점의 `현재`, 테스트 수, 지원 범위와 후속 과제는 당시 상태이며 이후 기록에서 변경될 수 있습니다. 최신 구현·검증 상태는 `PROJECT_HANDOFF.md`와 `README.md`를 기준으로 확인합니다.

현재 Agent 1·2 OpenAI 구조화 호출, CP1·CP2, SHA-256 인계와 최대 1회 재작업을 구현했습니다. Agent 3는 기존 기능의 좁은 UI 확인, 신규 기능의 범용 UI 동적 조사, AI 구조화 코드 의도, 결정론적 컴파일러, CP3와 신규 자동화 후보 시험을 구현했습니다. 특정 제품 기능명을 코드에 고정하지 않은 범용 전원 선택·적용 Live에서 Agent 1·2·3 실제 모델 인계, 동적 발견, 코드 생성, 브라우저 시험과 UI·내부 상태 복원 PASS를 확인했습니다. 현재 계약은 UI 기대 결과 원자성·준비 완료 뒤 초기 실행 문맥·중앙 관제 패널 단일 경로·여러 후보 계속 실행·기존 TC 대조·Slack·Notion 외부 보고를 포함하며 자동 테스트는 132건입니다. 해당 과거 진단의 전체 실제 모델 사용량은 44,334 tokens이며 최종 `agent3-3.7` 호출은 4,391 tokens였습니다.

Agent 4는 중립 실행 결과와 Manifest SHA-256을 다시 확인하고, 시험 재실행이나 모델 호출 없이 제품 불일치 후보·자동화 실행 오류·환경 오류·근거 부족을 분류해 CP4와 최종 보고 JSON을 생성하도록 구현했습니다. Assertion 실패는 제품 결함 확정이 아닌 후보로만 기록하며, 해시 불일치·중복 TC·실패 근거 부족뿐 아니라 증거 파일 누락·경로 이탈·SHA-256 불일치도 CP4 FAIL로 차단합니다. 2026-08-17에는 저장된 실제 `RUN-20260815-092107-0C075E`의 4개 PASSED 실행 결과를 현재 Agent 4로 재분석해 CP4 PASS·최종 PASS 권고와 모든 출력·증거 SHA-256 연결을 확인했습니다. 이 Agent 4 부분은 새 모델 호출이 아닌 저장 증거 재분석입니다. 같은 날 새 API Run에서 Agent 2 동시 실행 보호, UI 초기화 대기, TC 추적·요청 모드 TestData 검사를 보완했고, Agent 3는 내부 mode+setTemp 복합 검증이 현재 허용 목록 밖임을 REVIEW로 차단했습니다. 이후 UI 조사에서 실제 확인한 장비 필드명과 TC Expected Result에 함께 있는 필드·값만 고정 대상 장비에서 비교하는 복합 내부 검사로 확장했습니다. 첫 구현의 가변 객체 Schema는 OpenAI Responses API가 허용하지 않는 `propertyNames`를 생성해 실제 호출에서 차단됐고, 이를 `field_name`·`expected_value` 목록으로 교체했습니다. 수정 뒤 새 API Run `RUN-20260817-054536-678B65`은 Agent 1~4 Checkpoint PASS, 복합 `mode=AUTO`·`setTemp=18` 검사와 후보·환경·관련 회귀 4건 PASS, 검토 항목 0건·최종 PASS 권고까지 확인했습니다. 모델은 임의 내부 경로나 JavaScript를 만들 수 없습니다. 조건부 검토 재개와 서로 다른 변경 요청 반복 검증은 아직 남아 있습니다.

2026-08-18 결정: 사람 검토를 매 단계의 기본 통과 절차로 사용하지 않습니다. Agent 1이 `PROCEED`로 판단한 뒤 남은 보완 확인과 Agent 2의 `최종_확인_사항`은 실행을 계속하고 Agent 4 최종 보고에 모읍니다. 당시에는 `WAITING_FOR_USER`·`PARTIAL_PROCEED`를 모두 PAUSE로 두었으나, 아래 2026-08-22 V1 정렬 결정에서 `PARTIAL_PROCEED`의 의미를 수정했습니다. Agent 2 `중단_확인_사항`처럼 핵심 기대 결과를 분리할 수 없는 경우는 계속 PAUSE로 남깁니다.

2026-08-22 결정: Agent 3·4 재감사에서 과거 CP3 PASS와 저장된 파일 존재만으로 후속 판정을 신뢰하지 않기로 했습니다. 전용 중앙제어 TC에 범용 Action·Assertion이 섞여도 필수 조작 순서를 생략할 수 없고, 모든 Action의 근거 문장은 승인 TC 원문과 정확히 일치해야 합니다. 범용 내부 상태 경로·장비 필드는 TC 원문에서 이름이 확인되는 항목만 모델 입력에 남깁니다. Trial의 stdout·stderr·Screenshot·Trace SHA-256을 Agent 3 시점에 고정하고, `execute`는 현재 CP3·현재 정적 규칙과 실제 증거를 다시 검사합니다. 후보 기술 실패는 가능한 경우 예외로 버리지 않고 Agent 4가 분류할 중립 결과로 보존합니다. Agent 4는 Agent 3→검증 실행의 전체 Manifest 체인, 실제 후보 코드 파일과 출처·상태·종료 코드·증거를 확인하며, 자동화·환경·증거 문제는 제품 불일치 후보보다 우선해 `HOLD`로 판단합니다. 저장된 실제 전체 Run 복사본은 API 호출 없이 강화된 Agent 4로 CP4 PASS, 제품 3건·환경 점검 1건·고정 사례 0건을 확인했습니다.

같은 결정에서 역사적 공개 Run과 현재 재사용 가능 Run을 구분합니다. 계약 3.5 이전 Agent 3 산출물에는 네 증거의 개별 해시가 없으므로 현재 `execute`가 그대로 재사용하지 않습니다. 기존 공개 Run은 당시 연결의 사실 증거로 보존하되, 현재 계약으로 다음 단계에 인계하려면 새 Agent 3 실행이 필요합니다. TC 단위 제외, 여러 신규 후보 연속 실행, 후보 생성 전 중단 상태의 Agent 4 보고 연결은 별도 후속 범위로 남깁니다.

2026-08-22 새 API 전체 재검증 결정: 통제를 강화한 뒤에는 저장된 테스트만 통과하는 것으로 충분하지 않고 실제 모델이 만든 자연어 TC·계획과 규칙이 함께 동작하는지 다시 확인합니다. `RUN-20260822-043328-1EC2E0`에서 한국어 `내부 설정 온도`가 전용 `setTemp` 의미와 연결되지 않는 입력 필터 문제와, Agent 2가 장비 선택을 첫 TEST 단계로 승인했는데 CP3가 PRECONDITION만 허용하는 단계 충돌을 확인했습니다. 범용 필드는 계속 TC의 영문 필드명 직접 근거를 요구하지만 기존 전용 온도 전략은 요청 온도 데이터·INTERNAL_STATE의 내부 설정 온도 기대·실제 UI 조사 `setTemp`가 모두 있을 때만 허용합니다. 장비 선택은 승인 TC에 따라 PRECONDITION 또는 첫 TEST 조작에 둘 수 있지만 모드·온도·적용 뒤의 늦은 선택과 선택 누락은 계속 차단합니다.

보완 후 최종 별도 시도는 Agent 3 첫 계획·CP3·후보 Trial, 현재 후보 재검증, 환경 사전 점검, 관련 회귀 2건과 Agent 4 CP4·최종 PASS를 완료했습니다. 성공 경로의 실제 모델 사용량은 38,267 tokens이고 두 Agent 3 진단 시도까지 포함한 이번 재검증 전체 사용량은 52,933 tokens입니다. 실패 시도는 삭제하거나 성공 산출물로 덮지 않고 별도 Run 위치에 보존합니다.

2026-08-22 V1 정렬 결정: V1의 `Partial Proceed`는 전체 중단이 아니라 확정된 범위의 계속 진행과 정보 부족 범위의 개별 제외를 뜻합니다. 따라서 V2도 `PARTIAL_PROCEED`를 CONTINUE로 인계하고, 제외 범위와 정보 부족은 Agent 2 TC에서 분리해 검증 실행과 Agent 4 최종 보고의 `제외_범위`·`제외된_정보_부족`으로 보존합니다. 핵심 기대 결과를 분리해 확정할 수 없는 `WAITING_FOR_USER`만 PAUSE로 유지합니다. 제외는 품질 기준 완화를 뜻하지 않습니다. 실행하는 모든 TC에는 V1의 1단계 공통 QA·2단계 중앙제어 도메인·3단계 기능 Requirement 기준, 요구사항 추적성, 독립 실행 근거와 조건부 UI·내부 상태 이중 검증 정책을 명시하고 CP2-012~014로 검사합니다. 이 보완 후 자동 테스트 110건을 통과했으며 새 PARTIAL 경로의 Live API 실행은 아직 하지 않았습니다.

2026-08-22 다중 후보 보완 결정: 현재 계약 Live `RUN-20260822-105340-490418`에서 Agent 2가 화면 모드·온도·대기값 반영을 한 UI 기대 결과에 묶었고, Agent 3가 격리 실행기에서 이미 확인한 오류 없음·잠금 해제 문맥을 입력으로 받지 못해 지원 범위 확장 REVIEW로 중단됐습니다. 제품 요구사항이나 TC를 자동화 제약에 맞춰 삭제하지 않고, Agent 2는 서로 다른 UI 관찰값을 독립 Expected Result로 분리하며 Agent 3는 실제 확인한 초기 실행 문맥을 근거로 사용합니다. AUTO Orchestrator는 실행 가능한 여러 후보를 후보별 폴더에서 계속 처리하고, 지원 범위 밖 후보만 `자동화_제외_TC`로 검증 실행과 Agent 4 최종 보고에 전달합니다. 제외 자체는 제품 Finding이나 HOLD 사유로 만들지 않습니다. 기존 단일 후보 Run은 계속 읽습니다. 자동 테스트 116건을 통과했으며 새 Batch 계약의 Live API 재실행은 아직 하지 않았습니다.

2026-08-23 호출량 절감 결정: 단계별 모델과 추론 품질은 `gpt-5.6-terra`·`medium`으로 유지합니다. Agent 2에서 TC ID 또는 Expected Result ID만 중복되면 제품 의미·단계·기대값·Requirement를 바꾸지 않고 기술 ID만 순서대로 정리한 뒤 CP2를 다시 계산합니다. 다른 규칙이 실패하면 기존 최대 1회 모델 재작업을 그대로 수행합니다. AUTO의 Agent 3는 여러 TC를 한 구조화 응답에 묶어 공통 Prompt·UI 문맥 반복을 줄이되 요청 TC ID와 정확히 같은 독립 계획을 강제하고, 각 TC의 CP3·컴파일·Trial·증거·복원은 공유하지 않습니다. 묶음 호출 실패 시 기존 개별 호출로 전환하고 특정 계획만 CP3에 실패하면 해당 TC만 개별 재작성합니다. 세 Agent는 고정 Prompt cache key를 사용하며 SDK가 제공하는 캐시 입력·캐시 기록·추론 토큰을 기록합니다. 120개 자동 테스트를 통과했고, 새 계약의 실제 API 비용·토큰 감소량은 아직 측정하지 않았습니다.

2026-08-23 후속 Live 측정: 동일 요청 `RUN-20260823-020504-216277`에서 Agent 1·2는 50,908 tokens로 기존 동일 구간보다 1,250 감소했지만, 14개 TC의 Agent 3 묶음 호출이 156,227 tokens, 7개 개별 재작성이 118,839 tokens를 사용해 전체 325,974 tokens가 됐습니다. 첫 묶음 계획의 CP3 직접 PASS는 없었고 완료 후보 4개·제외 10개였습니다. 후속 환경 점검·관련 회귀 2건·CP4는 PASS했지만 신규 후보 3개 불일치로 최종 `HUMAN_REVIEW`였습니다. 필수 UI 요소 대기 전 초기 실행 문맥 기록, 기존 현장 제어 TC의 과도한 범용 조사, 알림 기대 결과가 없는 차단 온도 요청의 컴파일 분기를 원인으로 확인했습니다. 따라서 다건 단일 요청을 현재 형태로 비용 절감 성공으로 채택하지 않고 원인 보완 후 다시 비교합니다.

2026-08-23 Agent 3 후속 결정: 전체 선택 TC를 한 모델 요청에 넣는 방식은 현재 Orchestrator에서 제거하고 TC별 독립 요청으로 복귀합니다. 이는 실패한 한 계획의 영향과 거대한 중복 UI 입력을 제한하기 위한 결정이며 모델·추론 강도는 바꾸지 않습니다. 기존 HVAC LOCAL 온도 TC는 중앙 패널을 승인 초기 상태 준비에만 쓰고 TEST·RESTORE는 실제 현장 온도 `+/-`를 사용합니다. 장비 카드 UI와 내부 상태를 검사하고, 실제 현장 모드 조작이 없는 TC는 중앙 제어로 대체하지 않고 API 전에 제외합니다. 실행 문맥은 필수 UI·하네스 준비 뒤 기록하고 차단 요청은 요청값과 승인 기대값의 차이로 판정합니다. 저장된 14개 TC의 무API 판정은 중앙 1건·현장 온도 11건을 좁은 조사 대상으로, 현장 모드 2건을 제외 대상으로 분류했으며 자동 테스트 125건을 통과했습니다. 실제 API 비용 비교는 아직 수행하지 않았습니다.

2026-08-24 정정 결정: 위 문단에서 현장 `+/-`를 V2 실행 경로로 채택한 부분은 사용자 의도와 달라 폐기합니다. 이 프로젝트의 제품 조작 기준은 중앙 관제 패널 하나이며 벽면 리모컨·LOCAL 경로는 없는 것으로 취급합니다. Product SRS에서 REQ-LOCAL과 현장 조작을 제거하고, Agent 2 `agent2-2.7`은 CENTRAL TC만 생성하며 CP2는 LOCAL·REQ-LOCAL 참조를 거부합니다. Agent 3 `agent3-3.10`은 역사 LOCAL 후보를 UI 조사·API 전에 제외하고 중앙 경로로 바꾸지 않으며, UI Inventory·CP3·컴파일러에서 현장 리모컨 Selector와 조작 함수를 제거합니다. TC별 독립 호출, 필수 인터페이스 준비 뒤 실행 문맥 기록, 요청값과 승인 기대값 기반 차단 판정은 유지합니다. 이 정정은 무API 자동 테스트로 검증하며 기존 Run 증거는 당시 결과로 보존합니다.

2026-08-25 V1 최소화 결정: V2의 사용자 프로세스를 프로젝트 1의 `요구사항 분석 → 제품 기능 TC 설계 → 승인 TC 자동화·시험 → 관련 기존 회귀 → 규칙 기반 분류·최종 보고 → 사람 확인`으로 다시 단순화합니다. V1의 3단계 QA 기준·추적성·독립성·조건부 이중 검증·원인 분류는 유지하고, 실제 모델 연결에 필요한 구조화 JSON·SHA-256 인계·허용 목록 컴파일러·시험 증거는 내부 안전장치로 유지합니다. 현재 실행에 쓰이지 않는 Agent 3 일괄 모델 호출 Schema·Adapter·준비 객체·전용 산출물·과거 Batch 인계 검증을 제거하고, 실행 가능한 여러 TC는 계속 한 건씩 독립 처리합니다. 새 요약 파일은 `agent3_run_summary.json`입니다. 중단 Run 재개 UI와 정식 QA 자산 자동 등록은 현재 MVP 절차에서 제외하고 후순위로 둡니다. 과거 공개 Run과 기존 결정 기록은 당시 증거로 보존하지만 현재 실행 호환 대상으로 유지하지 않습니다. 관련 일괄 호출 전용 테스트 2건을 제거한 뒤 자동 테스트 121건을 통과했으며, 단순화 계약의 실제 API Live Run은 아직 수행하지 않았습니다.

2026-08-25 단순화 후 Live 결정: 동일 구체 온도 정책 입력의 첫 Run `RUN-20260825-122801-18C8C0`은 Agent 2가 “이전 TC에 의존하지 않는다”는 부정 문장을 실제 의존으로 오탐해 CP2-013에서 중단됐습니다. 다른 TC 언급 자체가 아니라 문장별 의존 의미를 검사하도록 최소 수정하고, 비의존 허용·실제 의존 차단 테스트를 추가했습니다. 보완 Run `RUN-20260825-123313-41B570`은 Agent 1·2 PASS, Agent 3 TC별 독립 실행 9건 중 완료 6건·자동화 제외 3건, 신규 자동화 6건·환경 점검 1건·관련 기존 회귀 3건 PASS, CP4·최종 권고 PASS를 기록했습니다. 성공 Run 모델 사용량은 94,241 tokens입니다. 자동화 제외는 제품 Finding이나 HOLD로 집계하지 않는 기존 결정을 유지하므로 최종 PASS는 실행된 범위의 PASS이며, 제외된 핵심 변경 TC까지 검증됐다는 뜻은 아닙니다. 제외 TC는 최종 보고와 함께 별도 확인합니다.

2026-08-25 Agent 3 범용 계약 정리: 새로운 기능을 지원할 때 제품별 기능명·모드·값·Selector를 Prompt·Checkpoint·컴파일러에 계속 추가하지 않습니다. 제품 동작의 기준은 변경 요청·SRS·승인 TC에서 받고, Agent 3는 실제 관찰한 UI를 공통 클릭·입력·선택·체크와 텍스트·값·활성 상태 검증에 연결합니다. 기존 V1 온도 전용 동작은 기준 화면 호환 경로로 유지하되 해당 동작을 사용하는 TC에서만 helper를 생성하며, 범용 TC 코드에는 사용하지 않는 온도 Selector나 내부 장비 경로를 포함하지 않습니다. QA 기준·기대값 보존·증거·허용 목록 통제는 그대로 유지합니다.

2026-08-26 업무 규칙 단위 TC 결정: TC를 입력값 하나마다 기계적으로 분리하지 않습니다. 같은 관제점·제어 경로에서 하나의 업무 규칙을 검증하고 입력값이나 모드만 다른 경우에는 하한·상한 또는 동일 비활성 조건을 한 TC의 여러 조건 구간으로 묶을 수 있습니다. 독립 조건은 중간 초기화를 명시하고, 순차 전환 자체가 검증 목적이면 승인된 순서를 유지합니다. 서로 다른 UI 관찰값은 같은 TC 안에서도 Expected Result를 나누되 TC 분리 사유로 사용하지 않습니다. Agent 2 구조화 계약에 복수 요청값, 조건 실행 방식, 묶음 근거, 중간 초기화, Expected Result 판정 단계를 추가하고 CP2-015가 이를 검사합니다.

같은 결정에서 Agent 3의 기존 `모든 Action 뒤 모든 Assertion` 컴파일 순서는 묶음 TC의 앞 조건을 마지막 상태에서 잘못 판정할 수 있음을 확인했습니다. 각 Assertion을 승인된 `verify_after_step`의 마지막 Action에 연결하고, 결정론적 컴파일러가 해당 Action 직후 검증하도록 변경했습니다. 처음 보는 기능명·모드·값·Selector라는 이유만으로 거부하지 않고 실제 관찰한 공통 브라우저 조작과 읽기 전용 검증으로 먼저 계획합니다. 기대값·Requirement·실제 UI 근거·허용 목록·금지 동작 통제는 완화하지 않습니다. 동일 업무 규칙 묶음 허용·초기화 및 판정 누락 차단·조건별 Assertion 순서의 자동 테스트 4건을 추가해 전체 127건을 통과했습니다. 새 계약의 실제 모델 Live 검증은 아직 수행하지 않았습니다.

2026-08-26 묶음 계약 Live 결정: 첫 Run `RUN-20260826-111634-2DA7C8`에서 Agent 2는 AUTO 허용 경계, AUTO 하한 차단, COOL·HEAT 허용 범위, FAN·DRY 비활성화를 각각 업무 규칙 단위 TC 4건으로 만들었습니다. 그러나 CP2-006이 `FAN/DRY 모드에서 버튼·온도 표시를 확인한다`는 조건 문맥을 별도 모드 표시 관찰로 오인했습니다. 모드가 화면 표시 검증 대상인 문장만 모드 관찰로 세도록 규칙을 좁히고, 실제 복합 관찰 차단과 문맥 허용 테스트를 함께 확인했습니다.

보완 Run `RUN-20260826-112212-6F777F`은 Agent 1·2 PASS와 같은 4개 업무 규칙 TC 생성을 확인했습니다. Agent 3는 AUTO 17°C 차단 TC를 실행해 UI 17°C, 내부 `mode=AUTO`·`setTemp=17`, 성공 Toast를 관찰했고 차단 기대와 달라 `PRODUCT_MISMATCH_CANDIDATE`로 기록했습니다. `TOAST_BLOCKING`은 Toast 표시만으로 통과하지 않고 성공 알림을 차단 알림으로 인정하지 않았습니다. 나머지 묶음 TC 3건은 실행 전 모드·온도를 기록해 중간·최종 복원값으로 재사용하는 제한된 변수 계약이 없어 임의 고정하지 않고 자동화 제외했습니다. 환경 점검 1건과 관련 기존 회귀 2건은 PASS했고 CP4 PASS·최종 `HUMAN_REVIEW`였습니다. 성공 Run의 실제 모델 사용량은 Agent 1 6,093, Agent 2 30,394, Agent 3 40,330으로 총 76,817 tokens입니다. 텍스트·Preview·Trace의 비밀정보와 로컬 경로는 0건이고 Project1 대상 해시는 유지됐습니다.

2026-08-26 V1 변경관리 정렬 결정: Project1에는 사람이 작성한 기존 TC와 자동화 코드가 이미 있는데 Agent 2 Prompt가 “구조화된 기존 TC 자산이 없다”고 가정해 유지되는 COOL·HEAT·FAN·DRY까지 새 `TC-CAND-*`로 만들고 Agent 3가 다시 구현하려 한 것은 기본 시나리오와 맞지 않습니다. Agent 2는 허용된 기존 TC 카탈로그를 입력으로 받아 실제 변경으로 새로 필요하거나 기대 결과·절차가 달라지는 후보만 `test_cases`에 만들고, 유지되는 영향 범위는 `관련_기존_TC`의 TC ID·Condition·선택 사유로 분리합니다. CP2-016은 기존 회귀 재생성, 카탈로그 밖 ID, 변경 조건 후보 누락과 추적 불일치를 차단합니다. Agent 3는 `CHANGE_VALIDATION` 후보만 모델·컴파일 대상으로 삼고, execute는 새 계약에서 Agent 2가 선택한 기존 TC만 회귀 실행합니다. 역사적 Run은 기존 Requirement 기반 선택을 호환 경로로 읽습니다.

같은 결정에서 V1 Agent 4의 Slack·Notion 외부 보고가 V2에서 빠진 기능임을 확인했습니다. V2도 CP4 PASS와 최종 보고 SHA-256이 일치할 때만 Payload를 생성하며 기본은 외부 변경 없는 Dry-run입니다. `--send`에서만 `SLACK_WEBHOOK_URL`, `NOTION_API_KEY`, `NOTION_DATA_SOURCE_ID` 환경변수를 사용하고, Notion은 TC ID 기준 Query 후 PATCH 또는 POST합니다. Payload와 PREVIEW·SENT·SKIPPED·FAILED·BLOCKED 결과는 `external_reporting.json`과 SHA-256으로 남기며 자격정보·로컬 경로는 저장하지 않습니다. 기존 Live Run에 적용한 Dry-run은 Slack 1건·Notion 4건 PREVIEW, 외부 전송 0건을 확인했습니다. 이후 사용자 승인으로 V1 `.env`의 자격정보를 프로세스에서만 읽어 실제 전송했고 Slack 1건·Notion 4건이 모두 `SENT`였습니다. 최초 Dry-run은 덮어쓰지 않고 `external_reporting_attempts`의 별도 전송 증거가 이전 보고 SHA-256을 참조합니다. 자동 테스트는 132건이며 새 Agent 2 계약의 실제 모델 Live는 아직 수행하지 않았습니다.

2026-08-27 기존 TC 우선 재사용 결정: 같은 Requirement ID만으로는 기존 TC가 새 조건을 실제로 검증하는지 판단할 수 없으므로, 허용된 기존 TC 카탈로그에 V1 테스트 코드에서 확인한 `검증 동작`을 추가합니다. Agent 1은 Requirement 영향도와 확정 조건을 전달하되 자동화 지원 여부를 이유로 영향도를 낮추거나 조건을 제외하지 않습니다. Agent 2는 Requirement와 검증 동작을 함께 대조해 그대로 유효한 기존 TC만 선택하고, 부족한 변경 조건만 신규·수정 후보로 만듭니다. 모든 확정 Condition은 변경분 후보 또는 관련 기존 TC에 연결되어야 하며 재사용할 수 없는 기존 TC는 억지로 선택하지 않고 이유를 기록합니다. CP3의 관찰된 Selector·정확한 내부 필드명·기대값 검사는 거짓 통과 방지를 위해 유지합니다.

첫 Live `RUN-20260827-113728-D0832E`에서 Agent 1은 REVIEW·CONTINUE, Agent 2는 변경분 TC 1건과 기존 `TC-MODE-001` 회귀를 설계했으나 CP2-010이 모드·온도 입력이 없는 잠금 상태 TC에 구조화 모드·온도 TestData를 강제해 중단됐습니다. 현재 TestData 계약이 모드·온도 값만 구조화하므로 이 검사는 실제 경계값 TC에만 적용하고, 잠금·전원처럼 UI 행동과 내부 필드를 직접 검증하는 상태·예외 TC에는 무관한 값을 만들도록 요구하지 않습니다. 첫 Run은 실패 증거와 24,974 tokens 사용량을 그대로 보존합니다.

보완 Live `RUN-20260827-114232-78DC4E`에서는 Agent 2가 첫 시도에 CP2 PASS하고 변경분 TC 1건과 기존 `TC-LOCK-001` 회귀를 분리했습니다. Agent 3는 잠금 설정·해제 버튼과 정확한 내부 `locked` 필드를 관찰했지만, 일반 상태 TC의 UI 조사에 중앙 패널 공통 적용 버튼이 포함되지 않아 클릭의 즉시 적용 여부를 추측하지 않고 지원 범위 확장으로 제외했습니다. 중앙 패널에서 대기 명령을 실제 장비에 반영하는 `.btn-apply-cmd`는 온도 전용 동작이 아니므로, 승인 절차에 적용·복원이 있고 기본 장비를 대상으로 하면 기능 종류와 무관하게 관찰 대상으로 포함합니다. 제품별 잠금 동작은 계속 범용 CLICK과 관찰된 필드로 처리합니다.

최종 Live `RUN-20260827-114925-507176`은 Agent 1 REVIEW·CONTINUE, Agent 2 첫 시도 CP2 PASS, 변경분 후보 1건과 기존 `TC-LOCK-001`·`TC-MODE-001` 회귀 분리를 확인했습니다. Agent 3는 장비 선택, 잠금 설정 CLICK, 공통 적용, 온도 버튼 활성 상태, 정확한 내부 `locked` 필드, 잠금 해제 복원을 계획해 CP3 PASS와 격리 Trial을 완료했습니다. 내부 `locked=true`는 통과했지만 온도 내림·올림 버튼이 모두 실제 `enabled=True`여서 `PRODUCT_MISMATCH_CANDIDATE`가 됐습니다. 환경 점검과 기존 회귀 2건은 PASS했고, Agent 4는 총 4개 결과를 검증해 CP4 PASS·최종 `HUMAN_REVIEW`, Slack·Notion PREVIEW를 기록했습니다. 비밀정보·로컬 사용자 경로는 0건이고 Project1 Git과 대상 해시는 유지됐습니다. 최종 Run은 36,127 tokens, 두 진단 Run을 포함한 합계는 103,705 tokens입니다. 세 Run 모두 변경분 후보 1건은 유지됐지만 보조 관련 회귀 선택은 일부 달랐으므로 AI 의미 판단의 완전한 결정성은 현재 범위 밖입니다.

2026-08-27 외부 보고 종료 결정: 최종 Run의 최초 PREVIEW를 보존한 채 사용자에게 Slack Webhook과 Notion Upsert 변경 범위를 명시하고 다시 승인받았습니다. `ATTEMPT-20260827-120544-570581-57E798`에서 Slack 요약 1건과 Notion TC 4건이 모두 `SENT`였고, 자격정보는 V1 `.env`에서 실행 프로세스로만 읽었습니다. 별도 전송 증거에는 비밀정보와 로컬 사용자 경로가 없으며 이전 Preview SHA-256을 참조합니다.

같은 날 사람 최종 판단을 JSON 필드만으로 맡기지 않기로 했습니다. Agent 4는 `사람_최종_검토.md`와 Manifest를 생성해 실행 요약, 관련 Requirement·TC, 기대 결과, 실제 불일치, 증거 링크, 제품 수정·요구사항 보완·자동화 재검토·환경 재실행·종결 선택지, 검토자·일자·후속 조치란을 제공합니다. 문서는 제품 불일치 후보를 결함 확정으로 표현하지 않으며 원본 `final_report.json`과 `validation_execution.json` 해시를 함께 기록합니다. Manifest의 문서 해시는 자동 생성 원본 기준이며, 사람이 작성한 뒤 달라지는 것은 정상입니다. 사람 판단을 덮어쓰지 않도록 자동 생성 원본과 Manifest 해시가 맞을 때만 `human-review --refresh`를 허용합니다.

보조 회귀 선택 변동성은 Agent 1이 `VERIFY`로 확정한 유지 Requirement에 연결된 카탈로그 TC를 결정론적으로 보충하는 것으로 좁혀 보완했습니다. 직접 변경된 Requirement의 기존 TC는 변경 후 기대와 충돌할 수 있으므로 자동 보충하지 않고 Agent 2의 검증 동작 대조를 유지합니다. 과거 묶음 TC 제외 원인이었던 초기 모드·온도 복원은 V1 중앙 관제 HVAC 흐름에 한해 실행 직전 `mode`·`setTemp`를 저장하고 최종 복원·적용 뒤 같은 값인지 확인하는 제한된 컴파일러 계약으로 구현했습니다. 이 계약은 일반 기능 내부 값을 직접 쓰는 허가가 아니며 실제 V1 Playwright 시험에서 조건 실행과 원상 복구를 확인했습니다.

2026-08-27 공개 증거 결정: 원본 `runs/`는 실행 중간 산출물과 대용량 증거를 포함하므로 계속 Git에서 제외합니다. 대신 최신 Agent 1→4 잠금 Live의 단계별 상태·사용량, 검증 집계, 사람 검토 대기 항목, Slack·Notion 실제 전송 상태와 원본 SHA-256을 `examples/results/agent1-agent2-agent3-agent4-lock-disable/`에 최소 공개 묶음으로 기록합니다. 공개 Manifest는 공개 파일 자체의 SHA-256을 제공하며 API 자격정보, 로컬 절대경로, 전체 모델 산출물, Screenshot과 Trace는 포함하지 않습니다.

2026-08-29 기준 제품 소유 결정: 잠금 Run은 제품 불일치 후보를 보여주는 실패 증거로 유지합니다. 성공 흐름은 기존 실패 요구사항을 바꾸지 않고, 현재 제품에서 근거를 확인한 별도 풍량 표시 변경 요청 `CR-SUCCESS-FAN-001`로 시험합니다. 이 요청은 HIGH 풍량을 사용자 화면의 `강풍`과 내부 `fanSpeed=HIGH`로 함께 검증하고 LOW로 복원하도록 했지만, 실제 Agent 1~4 Live 전에는 PASS라고 부르지 않습니다.

같은 날 최종 복사 범위를 정정했습니다. V2가 외부 Project1 경로 없이 독립 실행하려면 가상 제품뿐 아니라 이미 존재한다고 가정한 기존 회귀의 실제 실행 코드도 V2에 있어야 합니다. 따라서 출처 커밋 `ba62b611251180cbd0426eae0f9ab43a67a12abf`의 `virtual-controller.html`, `pytest.ini`, `tests/conftest.py`, `tests/test_controller.py` 네 파일을 `product_baseline/`에 둡니다. 포트폴리오 페이지·과거 보고서·영상·V1 Agent 4 코드·`.env`는 복사하지 않습니다. 신규·수정 TC는 V2 Agent 3가 별도로 만들고, 기존 테스트는 Agent 2가 관련 있다고 선택한 항목만 임시 Workspace에서 재실행합니다. 이후 제품 변경은 V2 HTML 복사본에만 적용하며 정식 TC 자산 승격은 실제 성공 실행 뒤 사람 승인에 따라 처리할 다음 구현 범위입니다.

2026-08-29 성공 후보 첫 Live에서 Agent 1 지침은 시험 준비·복원을 제품 Condition에서 제외하라고 했지만 CP1-008은 모든 acceptance_notes를 제품 판정 조건으로 강제해 중단됐습니다. 준비·복원 문장은 제품 Expected Result가 아니지만 실행 제외도 아니므로, Agent 1은 둘 다 만들지 않고 변경 요청 원문을 Agent 2에 넘깁니다. Agent 2는 준비 문장을 preconditions·steps에, 종료 후 복원 문장을 restore_steps에 보존하며 CP2-014가 누락이나 제외 목록 혼입을 차단합니다.

두 번째 Live에서는 Agent 2가 만든 풍량 TC와 내부 `fanSpeed=HIGH` 계획은 유효했지만, CP3가 실행 전 대상 카드의 `약풍` 텍스트만 보고 실행 후 `강풍` 검증을 의미 연결 없음으로 거부했습니다. 변경 후 문구가 초기 화면에 없는 것은 정상일 수 있으므로, 승인 Expected Result가 대상 장비 카드를 명시하고 정확한 대상 ID의 카드 Selector를 사용하는 경우에만 동적 텍스트 검증을 허용합니다. 이 예외는 다른 카드·상위 컨테이너·임의 Selector에는 적용하지 않습니다.

보완 후 `RUN-20260829-035050-2FF59B`은 Agent 1 REVIEW·CONTINUE, Agent 2·CP2 PASS, Agent 3 첫 계획·CP3·Candidate Trial PASS를 기록했습니다. 생성 TC는 초기 LOW 확인, HIGH 적용 직후 장비 카드 `강풍`과 내부 `fanSpeed=HIGH` 이중 검증, LOW 복원을 포함합니다. `execute`는 후보 증거를 해시 확인 후 재사용하고 환경 점검을 통과했으며 Agent 4는 제품 1건·환경 1건 PASSED, CP4·최종 권고 PASS, 검토 항목·자동화 제외 0건을 생성했습니다. Slack·Notion은 PREVIEW만 생성했고 실제 전송하지 않았습니다. 모델 사용량은 Agent 1 5,393, Agent 2 9,289, Agent 3 18,756으로 총 33,438 tokens입니다. 자동 테스트 150건과 카탈로그 수량도 일치합니다.

2026-08-29 중앙제어 실제 Run 연결 결정: V1에서 복사한 제품 제어 동작과 기존 회귀 인터페이스는 유지하되 V2 HTML의 팀장·Agent 1~4 버튼은 로컬 `qa_pipeline_ui`가 제공하는 실제 Run 요약 패널을 우선 엽니다. 연결부는 허용된 Run ID와 `examples/change_request*.json`만 받고 브라우저에 로컬 절대경로·비밀정보·전체 원본 산출물을 전달하지 않습니다. 기본은 저장 Run 조회 전용이고 `--allow-live-run`과 화면 확인이 함께 있을 때만 Agent 1→3, 검증 실행, 규칙 기반 Agent 4를 순서대로 실행합니다. 이 경로의 Agent 4는 `--send`를 사용하지 않아 Slack·Notion 실제 전송은 발생하지 않습니다. HTML을 직접 열어 브리지가 없으면 기존 데모가 유지되며 Project1 원본은 변경하지 않습니다. 연동 자동 테스트 5건을 추가해 전체 155건입니다.

2026-08-30 중앙제어 Live 실행 안정성 결정: 실제 UI 실행에서 여러 브리지의 메모리 잠금이 서로 독립적이어서 중복 API Run이 시작될 수 있고, 30초 Candidate Trial 제한이 정상 Playwright 종료·Trace 생성까지 포함하면 경계값이 되는 문제를 확인했습니다. 저장소 단위 파일 잠금으로 동시에 하나의 Live Run만 허용하고, Candidate Trial 기본 제한을 90초로 조정합니다. 시간 초과 시 pytest·Playwright 자식 프로세스 트리를 정리하고, 쓰기가 중단된 Trace는 경로·비밀정보 제거를 보장할 수 없으므로 증거에서 폐기합니다. UI의 API 비용 2단계 확인 상태는 자동 새로고침 뒤에도 유지합니다. 이 보완은 제품 기대값·TC 판정·Assertion을 변경하지 않습니다.

같은 날 최신 UI Live `RUN-20260829-054330-A18942`은 Agent 1 첫 응답의 인수 조건 누락을 CP1이 차단해 1회 재작업한 뒤 Agent 1 REVIEW·CONTINUE, Agent 2·CP2 PASS, Agent 3·CP3 및 Candidate Trial PASS를 기록했습니다. `execute`는 후보 증거를 재사용하고 환경 점검을 통과했으며, Agent 4는 제품 1건·환경 1건 PASSED, CP4·최종 권고 PASS를 생성했습니다. 자동화 제외·검토 항목은 0건이고 CP1 최종 확인 1건을 사람 검토서에 남겼습니다. Slack·Notion은 PREVIEW로 외부 전송하지 않았고 Run 텍스트의 비밀정보 패턴은 0건입니다. 모델 사용량은 Agent 1 11,069, Agent 2 9,699, Agent 3 18,885로 총 39,653 tokens이며 자동 테스트 159건과 카탈로그가 일치합니다.

2026-08-30 후보 자산 승인 결정: 중앙제어 실제 Run 패널에서 `저장 결과 보기`, `새 요구사항 실제 실행`, `후보 TC 공식 자산 판단`을 분리합니다. 공식 자산 판단은 `--allow-asset-approval`에서만 활성화하고, 최종 권고·CP3·CP4·변경 검증 PASS, 완전한 Screenshot·Trace, 후보·증거·현재 HTML SHA-256을 서버가 다시 확인합니다. Run 이후 HTML이 바뀌었으면 기존 검증 산출물을 덮어쓰지 않고 같은 후보 Python만 현재 HTML에서 API 호출 없이 재실행합니다. 사람 승인 시 후보 TC와 Python을 `approved_assets/`에 복사하고 출처·제품·자산 해시와 검토 기록을 Registry에 남깁니다. 보류는 사유만 Run에 기록하며 공식 자산을 만들지 않고, 이미 승인된 자산은 화면에서 보류로 되돌리지 않습니다. 기존 사람 작성 회귀 파일은 자동으로 수정하지 않습니다.

같은 날 `RUN-20260829-054330-A18942 / TC-CAND-001`은 Run·승인 UI 변경으로 대상 HTML 해시가 달라져 처음에는 공식 등록이 차단됐습니다. 중앙 화면의 `현재 화면에서 후보 재검증`으로 모델 API 없이 다시 실행해 54,781ms PASS와 완전한 Screenshot·Trace를 확보했고, 재검증 증거의 비밀정보·로컬 절대경로 패턴은 0건이었습니다. 승인 버튼은 활성화됐지만 사람 최종 판단을 대신하지 않으므로 공식 자산은 생성하지 않았습니다. 승인·보류·무결성·재검증 자동 테스트 4건을 추가해 전체 163건입니다.

같은 날 오세훈 검토자 승인으로 위 후보를 `TC-V2-001` 공식 자산으로 등록했습니다. Registry와 TC·Python 파일의 출처·제품·자산 해시를 확인했고, 복사된 Python을 현재 V2 HTML에 직접 실행해 PASS했습니다. 이 승인은 후보 보존과 단독 실행 가능성만 확정합니다. 감사 결과 `approved_assets/registry.json`을 Agent 2 또는 `execute`가 읽지 않아 다음 변경 Run의 기존 TC 대조·관련 회귀에 자동 편입되지 않으며, Agent 1의 `UPDATE_REQUIRED` 및 Agent 2의 SRS 후속 개정 메모도 최종 사람 검토서·공식 승인 조건으로 전달되지 않는다는 두 연결 공백을 확인했습니다. 후속 구현 전에는 공식 등록을 SRS·TC·회귀의 닫힌 순환 완성으로 표현하지 않습니다.

2026-08-30 승인 자산 재사용 결정: 공식 Registry를 단순 보관 목록으로 두지 않습니다. 다음 Agent 2 실행 전에 TC·Python·선택적 SRS 개정 기록의 경로와 SHA-256, 구조화 TC와 단일 테스트 함수를 검증하고 V1 카탈로그와 합친 Run Snapshot을 만듭니다. Agent 2는 Requirement뿐 아니라 검증 동작 의미가 현재 확정 조건과 맞을 때만 공식 TC를 선택하며, `execute`는 Snapshot과 현재 승인 Python 해시가 같을 때 그 파일을 임시 Workspace에서 실행합니다. `TC-V2-001`을 Registry에서 로딩해 현재 V2 HTML에 실제 Playwright 재실행했고 PASS·Screenshot·Trace를 확인했습니다.

같은 날 SRS 승인 결정: MODIFIED·UPDATE_REQUIRED Requirement는 Agent 2가 `SRS_개정_제안`에 현재 인수 기준, 제안 인수 기준, 근거 Condition과 사유를 구조화해야 하며 CP2가 누락·중복·현재 원문·근거를 검사합니다. 제안은 검증 실행, Agent 4 최종 보고, 사람 최종 검토 문서와 중앙제어 승인 화면까지 그대로 전달합니다. 적용할 변경이 있으면 별도 SRS 동의 없이는 공식 TC 승인을 차단하고, 동의 시 해당 Requirement 인수 기준만 현재 원문 일치 조건으로 교체하며 Run 결정 기록·공식 개정 기록·전후 SHA-256을 남깁니다. 기존 `TC-V2-001` 승인 당시 확인했던 `REQ-CONTROL-001`, `REQ-FAN-001`, `REQ-STATE-001` 기준 문구는 `SRS-REV-001` 기록과 Registry 해시로 연결했습니다. 이 보완은 기존 역사적 Run에 신규 계약을 소급 적용하거나 Checkpoint PASS를 사람 승인으로 바꾸지 않습니다. 자동 테스트 167건 중 Registry 로딩·공식 자동화 실제 재실행·SRS 제안 CP2·SRS 적용/충돌·UI 동시 승인 검증이 포함됩니다.

2026-08-31 승인 자산 재사용 Live 감사 결정: 공식 TC ID는 `TC-V2-001`처럼 가운데 구간에 숫자를 포함할 수 있습니다. Agent 2의 Expected Result는 적용 뒤 표시·내부 값처럼 독립 판정 가능한 관찰값이어야 하며 `선택하고 적용할 수 있다` 같은 실행 행동 성공 문장은 CP2가 재작성을 요구합니다. 변경 후 문구에 기존 동작이 반복돼도 전·후 사실 토큰을 대조해 유지 조건으로 분리하고, 관련 기존 TC가 담당하는 유지 조건만으로 신규 Expected Result를 만들 수 없습니다. CP4는 V1 기준 회귀의 기준 테스트 해시와 승인 공식 TC의 Registry 자동화 해시를 서로 다른 정상 출처로 검증합니다.

실제 MED Live `RUN-20260831-122323-0F0460`은 Agent 1~3, 신규 MED 후보 Trial, 환경 점검, `TC-MODE-001`과 승인 `TC-V2-001` 실행까지 모두 PASS했습니다. 신규 후보는 카드 `중풍`과 내부 `fanSpeed=MED` 두 결과만 포함하고, HIGH 유지 동작은 승인 TC로 분리됐으며 SRS 개정 제안도 생성됐습니다. 최초 Agent 4는 승인 자동화 해시를 기준 테스트 해시와 비교해 CP4 FAIL을 남겼고 이 원본은 덮어쓰지 않았습니다. 보완 뒤 동일 검증 입력을 임시 작업공간에서 규칙 기반으로 재분석해 CP4·최종 권고 PASS와 Slack·Notion PREVIEW를 확인했습니다. 외부 전송과 공식 자산 승인은 수행하지 않았으며, 자동 테스트는 171건입니다.

2026-09-01 로직 감사에서는 두 과도한 자동 판단을 철회했습니다. 첫째, 변경 전·후 단어 집합이 비슷하다는 이유로 Condition을 유지로 판정하면 값 매핑이나 순서 교체를 놓칠 수 있으므로 Agent 1의 구조화된 `변경`·`유지`·`보조_근거` 역할을 인계하고, 역할이 없는 변경 요청 Condition은 안전하게 `변경`으로 취급합니다. 둘째, Agent 1의 `VERIFY` Requirement ID만으로 카탈로그 TC를 자동 보충하면 검증 동작이 다른 회귀가 섞일 수 있으므로 제거하고, Agent 2가 Condition과 검증 동작을 대조해 명시한 선택만 CP2가 허용합니다. 기존 TC가 변경 후 조건을 모두 검증하면 신규 후보 없이 실행할 수 있고, 신규 후보가 모두 자동화 제외돼도 중단하지 않고 기존 회귀와 Agent 4 보고를 계속합니다. 제외만 남은 경우 최종 권고는 `HUMAN_REVIEW`입니다. 같은 감사에서 Agent 3 복원 시 검사 대상 내부 장비 필드의 실행 전·후 비교, 승인 공식 TC의 카탈로그 Snapshot 해시 필수 확인, 후보별 SRS 제안 범위 제한, 공식 승인 중 실패한 SRS·TC·자동화·Registry·임시 파일 원상복구를 추가했습니다. 자동 테스트 180건과 수집 180건은 통과했으며 새 실제 API Live는 아직 수행하지 않았습니다.

2026-09-03 최종 Live `RUN-20260903-121213-4CCC7A`에서 위 9월 1일 계약을 실제 모델과 브라우저로 다시 확인했습니다. Agent 1 첫 응답은 실행 대상 인수 조건 하나를 누락해 CP1-008이 차단했고 1회 재작업 뒤 PASS·CONTINUE가 됐습니다. Agent 2는 MED의 카드 `중풍`·내부 `fanSpeed=MED`만 신규 후보로 만들고, 변경되지 않은 HIGH의 `강풍`·내부 `fanSpeed=HIGH`는 승인 자산 `TC-V2-001`로 분리했습니다. Agent 3 첫 계획·CP3·후보 Trial, 환경 점검, 승인 기존 회귀, CP4가 모두 PASS했으며 제품 2건·환경 1건의 최종 권고도 PASS였습니다. Agent 1~3 사용량은 11,561 + 10,622 + 19,028 = 41,211 tokens이고 Slack·Notion은 PREVIEW만 생성했습니다. SRS 개정과 신규 후보 공식 등록은 사람 승인 전 상태로 유지하며, 최소 공개 증거는 `examples/results/agent1-agent2-agent3-agent4-medium-fan/`에 둡니다.

같은 실행의 공개 전 검사에서 JSON·Markdown·stdout에는 비밀정보와 로컬 절대경로가 없었고 대상 HTML과 승인 자산도 변경되지 않았습니다. 신규 Candidate Trace에는 사용자 경로가 남지 않았지만, 승인 기존 TC가 만든 로컬 원본 Trace에는 임시 `file://` 실행 경로가 남는 제한을 확인했습니다. Trace는 Git 공개 묶음에서 제외되므로 공개 노출은 없으며, 기존 회귀 Trace 정제는 코드·테스트 구조 분리 전에 다룰 후속 기술 부채로 기록합니다.

같은 날 위 기술 부채를 먼저 해결해 기존 회귀 Trace에도 Candidate와 같은 ZIP 내부 경로 정제를 적용했습니다. 이어 공개 `qa_pipeline_v2` import·CLI를 유지하면서 공통 계약, Agent 1, Agent 2, Agent 3, 실행, Agent 4 보고, Orchestrator와 공통 I/O·Trace 모듈로 소스를 분리하고 180건 테스트를 역할별 7개 파일로 옮겼습니다. 분리 전후 테스트 함수 176개와 Pytest 수집 180건은 동일하며 전체 회귀가 통과했습니다. 후속 Live `RUN-20260903-125732-ECE88F`은 CP1·CP3 재작성 각 1회 뒤 신규 후보·환경 점검·승인 회귀, CP4·최종 권고 PASS와 Slack·Notion PREVIEW를 기록했습니다. 누적 사용량은 63,022 tokens이고 Candidate·승인 회귀 Trace의 로컬 사용자 경로·임시 작업폴더명·키 패턴은 0건입니다. 역사적 Run과 공개 묶음은 덮어쓰지 않습니다.

2026-09-03 구조 정리 결정: 승인 기존 회귀도 Candidate Trial과 같은 ZIP 내부 경로 정제를 증거 해시 계산 전에 적용하고, 손상돼 정제를 보장할 수 없는 Trace는 증거에서 제외합니다. 기존 공개 import와 CLI는 `qa_pipeline_v2.py`에서 유지하면서 공통 계약·Agent 1·Agent 2·Agent 3·실행·Agent 4 보고·Orchestrator와 공통 I/O·Trace 모듈로 구현을 분리했습니다. 단일 테스트 파일도 같은 역할 기준 7개 파일과 공통 지원 파일로 나눴습니다. 분리 전후 테스트 함수 176개의 이름이 같고 매개변수 조합을 포함한 Pytest 180건이 통과했으며, 이 변경은 QA 판정 규칙이나 역사적 Run 산출물을 수정하지 않습니다.

2026-09-04 검수 후 최소 수정 결정: 검수에서 나온 모든 우려를 필수 수정으로 취급하지 않습니다. 실제 화면·보고 연결 문제 세 가지만 보완했습니다. 중앙제어 요약은 최초 외부 보고뿐 아니라 후속 시도 중 최신 상태와 이전 SEND 기록을 표시합니다. `execute`가 환경 점검 실패로 종료 코드 2를 반환해도 유효한 BLOCKED 결과가 저장됐으면 Agent 4로 넘겨 기존 CP4 검사를 거쳐 HOLD 보고를 만들며, 결과 누락·손상은 계속 중단합니다. 후보 승인 화면의 SRS 제안은 실제 적용과 같은 Requirement·Condition 필터를 사용합니다. 해시 방어 확대·복원 분류 변경·일반 문구 개선은 이번 범위에서 제외했습니다. 자동 테스트 183건(UI 19건)이 통과했고, 저장된 잠금 Run의 실제 전송과 최신 MED Run의 미리보기 상태를 읽기 전용으로 확인했습니다. 새 모델 API·외부 전송·공식 자산 승인 없이 기존 Run과 제품 HTML을 보존했습니다.

2026-09-06 V1 비교 후 최소 보완 결정: 감사 항목 중 수동 TC 누락, 기존 TC의 명시 값 불일치 재사용, 개별 기대 결과 PASS 추정, Notion 실행 간 덮어쓰기 네 항목을 우선 수정했습니다. 수동 후보는 모델 호출 없이 자동화 제외 사유를 남깁니다. 신규 Agent 2 계약만 CP2-019로 기존 TC 전용 변경 조건의 상태 코드·수치 포함을 검사하며 자연어 의미의 완전한 일치는 주장하지 않습니다. 실패 요약에 없는 ER는 판정 미확인으로 안내합니다. Notion 키는 Run ID+TC ID로 바꿔 다른 실행을 보존하고 같은 실행의 재전송만 갱신합니다. V1 로컬 project.html·Agent 4 코드와 캡처를 대조해 TC 유형과 실패 원인이 별개임을 확인했고, V2 실제 Run에 유형 필터·행 펼침 표를 연결했습니다. 신규/승인 TC의 전제조건·절차·기대 결과·복원과 관찰을 표시하되 기존 기준 회귀의 미기록 상세·유형과 미지정 우선순위는 추정하지 않습니다. 자동 테스트 187건, 저장된 MED 성공·잠금 실패 화면을 확인했으며 새 API·외부 전송·공식 승인·커밋·푸시는 수행하지 않았습니다. HTML은 보고 UI만 수정했지만 전체 해시가 달라지므로 과거 후보 공식 승인 전 모델 없는 재검증은 현행 정책대로 필요합니다. 다른 감사 항목은 후속 검토로 남기며 V1 원본·역사적 Run·공식 자산은 보존합니다.

같은 날 추가 검증 기록: 전체 187건 통과 뒤 반복 전체 실행에서는 186건 통과·1건 TIMEOUT(260.69초)이 발생했습니다. 기존 `test_agent3_trial_distinguishes_product_mismatch`의 내부 후보 시험 제한 20초를 초과했으며, 같은 테스트의 단독 재실행은 통과했습니다(테스트 전체 34.45초). 출력만으로 시간 초과 원인을 확정할 수 없으므로 환경 문제로 단정하거나 반복 안정성이 해결됐다고 설명하지 않습니다. 성공 이력으로 실패 이력을 지우지 않고 README·인계·검증 문서에 함께 남기며, 시간 제한이나 제품 판정 기준을 변경하지 않습니다.

2026-09-06 공개 문서 정합성 교정: GitHub main과 로컬 기준 커밋 `24357a8`의 일치를 확인하고 현재 코드·공개 증거와 설명을 대조했습니다. V1 원본과 V2 HTML의 바이트 동일 표현, 과거 Live를 최신 계약 검증으로 읽게 하는 표현, 모든 공개 예시에 같은 Manifest·Trace 제외 정책이 있다는 설명을 교정했습니다. V1 감사와 과거 테스트 수는 당시 기록임을 명시하고, 자동 검사와 자연어 의미 보장·사람 승인을 구분했습니다. 기존 TC 전용 요청의 준비·복원 메모 검사, 기존 TC 전용 SRS 단독 승인 부재, 제품 불일치와 복원 실패 동시 분류는 미해결 제한으로 공개했습니다. 설치 안내에 별도 pytest-playwright·Chromium 설치와 로컬 서버 필요 조건을 보충했지만 코드·패키지 정의는 변경하지 않았습니다. 제품 SRS는 소개·상태 설명만 교정하며 Requirement 행·공식 자산·과거 Run 증거는 보존합니다. 이번 문서 교정은 새 API Live 성공이나 기존 로직 문제 해결을 뜻하지 않습니다.

2026-09-07 두 문제 최소 수정 결정: 기존 TC만 선택한 요청에서 CP2-014가 비어 있는 신규 후보의 준비·복원 필드를 요구하지 않도록 했습니다. 절차를 실행했다고 추정하거나 기존 자동화를 변경하는 대신, 실행기는 해시로 확인한 요청 원문과 선택 TC를 최종 확인 사항으로 인계합니다. 신규 후보가 있는 요청의 절차 검사와 기존 TC 선택·값·추적성 규칙은 유지하고 `existing_procedure_review_contract=1.0`이 없는 과거 Run은 당시 CP2로 확인합니다. Agent 4는 해시가 일치하는 실제 복원 실패 로그가 있으면 자동화 실행 문제·HOLD로 분류하고 제품 불일치 관찰도 사람 검토서에 함께 보존합니다. 같은 TC를 중복 집계하지 않으며 Trial 원본 상태를 덮어쓰지 않습니다. 코드에 인용된 오류 표식과 변조 로그를 근거로 사용하지 않는 테스트를 포함했습니다. 관련 65건·실행 및 보고 46건, 최종 전체 191건(68.13초)이 통과했고 수집·카탈로그·컴파일·diff 검사도 확인했습니다. 중간 검증의 테스트 증거 경로 중복과 간소화 입력 호환 문제를 보완한 뒤 재실행한 결과입니다. 기존 TC 전용 SRS 단독 승인·간헐적 시간 초과 원인·새 API Live는 이번 범위가 아니며 제품 화면·SRS·공식 자산·과거 Run과 V1 원본은 변경하지 않았습니다.

2026-09-08 Live 후 최소 보완: 실제 TC가 UI·내부 관찰을 반복 조작으로 분리한 문제에 대해 작성 지침과 신규 CP2 판정 단계 대조를 보완했습니다. 과거 CP2에는 소급 적용하지 않습니다. Agent 4는 단일 CLI의 정확한 루트 파일·해시가 맞으면 요약 없이 인계하되 다중 실행의 요약 누락·변조는 계속 차단합니다. 전체 193건(68.07초)이 통과했고 저장된 실제 산출물에서 오설계 차단·정상 단일 인계를 읽기 전용으로 확인했습니다. 새 모델 생성 개선이나 전체 Live 성공을 확인한 것은 아닙니다. 제품·공식 자산·과거 Run을 보존했습니다.

2026-09-09 마지막 보완 범위: 기능 확장을 멈추고 합의한 세 항목만 구현했습니다. 일부 통과·일부 제외도 신규 최종 권고는 HUMAN_REVIEW로 표시하되 실행 자체·개별 TC 결과를 변경하지 않습니다. 생성 자동화는 같은 판정 단계의 결과·복원을 최대 2초 재관찰하며 조작은 재실행하지 않습니다. 실제 장비 응답 완료나 불변 지속까지 증명하는 계약은 아닙니다. 기존 TC 전용 Run은 현재 제품·인계·증거·제안과 SRS 원문 확인 후 사람 동의로 문구만 승인하며 TC를 새로 만들지 않습니다. 전체 201건(87.17초), 수집과 브라우저→로컬 HTTP→임시 SRS 승인 재검증을 확인했습니다. SRS 테스트의 Agent 1/2 로더는 고정 입력으로 대체했으므로 새 모델 전체 성공으로 표현하지 않습니다. 실제 SRS·공식 자산·과거 Run은 보존했습니다. 이후에는 사용자 승인하에 최종 Live·문서 확인·커밋·푸시·시연을 진행하고, 실제 거짓 통과·실행 불가·데이터 손상 외 편의 개선은 후속으로 남깁니다.

2026-09-10 최신 실제 실행 및 설명 문서 결정: `RUN-20260910-101352-9C9021`에서 MED 신규 후보, 환경 점검, 승인 HIGH 회귀 3건과 CP1~CP4·최종 권고 PASS를 확인했습니다. Agent 1 대상 사전조건 전달 누락 1회 재작성, Agent 2·3 첫 응답 통과이며 총 4회·43,419토큰입니다. 제품 HTML을 변경하거나 제품 기대값을 바꿔 통과시킨 실행이 아닙니다. Slack·Notion은 PREVIEW, SRS·MED 공식 등록은 승인 대기입니다. 포폴은 V1의 QA 기준과 설명을 기반으로 실제 성공·일부 누락·제품 불일치 사례 및 사람의 자산 등록을 보여주며 공개 실행 서버를 추가하지 않습니다. 일부 누락 시연과 최신 페이지 버튼 전체 실행은 아직 확인할 작업입니다. 사용자의 문서 작성·기존 변경 커밋·푸시 요청에 따라 `docs/08_SCENARIOS_AND_FLOW_GUIDE.md`를 추가하고 README·검증 현황을 갱신합니다. 미사용 함수 조사는 안내서에 포함하지 않고 후속 코드 검토 사항은 구현 완료로 바꾸지 않습니다.

2026-09-11 화면·실행 연결 최소 수정: 사용자 승인에 따라 상태 갱신 복구, 실행 ID와 조회 선택 분리, 설정 기준 경로 전달, 별도 브라우저 실행 안내만 보완합니다. 명시 Run ID를 CLI에 전달하고 기존 ID는 거부해 다른 실행 폴더가 혼입되지 않도록 합니다. 연결 실패를 정상 상태나 고정 데모로 숨기지 않습니다. 별도 작업 대기열·실시간 중계·새 CP 규칙은 추가하지 않습니다. 기존 순서 테스트를 확장하고 브라우저 연결·선택 및 Run ID 보호 테스트 2건을 추가했습니다. 과거 Run·공식 자산·제품 기대값은 보존하고 새 API 호출이나 외부 전송은 하지 않았습니다.

2026-09-12 내부 중복·분기 검수 후 수정: 기능 삭제 대신 승인 코드 일치와 보고 자산 참조 불일치, 화면 승인 동의 잔존·공통 10초 제한·설명 영역 겹침만 보완합니다. 다른 코드의 실행 증거로 원래 후보를 등록하지 않고 불일치를 차단합니다. 새 Run의 기존 승인 TC 원문은 카탈로그 Snapshot 안에 보관해 화면·Notion 상세가 현재 기본 폴더에 의존하지 않게 하며 원본 해시를 대조합니다. 과거 Run은 소급 변경하지 않습니다. 보조 함수 두 쌍과 CP4의 명백한 중복 비교는 정리하지만, 단일·다중 TC 및 과거 계약 호환, 단계 경계 재검사, 사람의 SRS 승인·공식 TC 재사용은 유지합니다. 같은 후보 코드의 연속 생성 최적화는 호출 계약 변경이 필요하고 API 비용과 무관하므로 이번에는 보류합니다. 새 관리 기능·승인 CLI·대규모 분리는 추가하지 않습니다.

2026-09-12 공개 데모·승인 근거 정합성 보완: 정상 변경 데모 Requirement ID를 현재 SRS의 `REQ-FAN-001`로 통일하고 문서 자동 테스트 수를 207건으로 바로잡습니다. 실행 당시 원문인 과거 SRS Snapshot은 수정하지 않고 예시 README에서 현재 문서로 연결합니다. 공식 자산 Registry가 Git 제외 Run 경로를 직접 가리키던 문제는 재검증의 공개 가능 요약을 `approved_assets/provenance/`에 보존하도록 수정합니다. Raw Screenshot·Trace는 로컬 Run에 유지하고 Registry에는 공개 요약 해시와 원본 재검증 기록 해시를 구분해 남깁니다. QA 판정·제품 동작·과거 Run은 변경하지 않습니다.

같은 날 공개 데모 결정: 포트폴리오 방문자가 `virtual-controller.html`을 직접 열거나 공개 웹 주소로 접속하면 V1 고정 모의 화면 대신 MED 풍량 정상 변경 1건의 V2 결과 패널을 보여줍니다. Agent 1 요구사항 정제, Agent 2 신규 후보 TC 1건, Agent 3 화면·내부 상태 이중 검증 PASS, Agent 4 정상 분류와 사람 승인 미리보기를 같은 시나리오로 연결합니다. 공개 데모는 API 요청·제품 자동화·외부 전송·SRS·공식 TC 파일 변경을 하지 않으며, 승인도 화면 상태만 바꿉니다. `localhost`·`127.0.0.1`의 로컬 브리지는 기존 실제 Run 조회와 선택적 Live 실행을 유지하고 연결 장애를 데모로 숨기지 않습니다. 브라우저 검증 1건을 추가해 전체 207건이 통과했습니다.

2026-09-12 동일 입력 Live 재검증: 일부 누락 입력에서 CP1-008이 색상 미정 원문까지 확정 조건으로 요구하고 CP1-009는 그 중복을 거부하는 충돌을 확인했습니다. 명시적 미정 표현이 있는 인수 조건 원문이 PARTIAL_PROCEED의 정보 부족·제외 정보 부족·제외 범위 세 곳에 보존된 경우만 CP1-008의 확정 조건 요구에서 제외합니다. 명확한 기준을 모델이 임의로 제외하는 것은 계속 실패 처리하며 모든 자연어 모호성을 해결했다고 주장하지 않습니다. 또한 최종 보고가 없으면 중간 단계 PASS를 전체 PASS로 표시하지 않고 후속 검증 대기로 표시합니다. 두 회귀 테스트를 추가해 전체 209건이 통과했습니다.

수정 전 입력 해시를 유지한 페이지 버튼 Live 재실행에서 정상 `RUN-20260912-100047-14E1B7`은 후보·환경 2건 PASSED, 최종 PASS(4회·41,090토큰), 일부 누락 `RUN-20260912-100402-5F1715`는 후보·환경·선택 HIGH 회귀 3건 PASSED, 색상 미정 1건 제외와 최종 HUMAN_REVIEW(4회·43,922토큰)를 확인했습니다. HIGH는 기존 TC 선택 결과이며 신규 TC 중복 생성이 아닙니다. 두 Run 모두 대상 조건 누락에 대한 Agent 1 재작성 1회와 변경 전 문구 확인 REVIEW를 보존합니다. 화면은 최종 보고 전 대기와 최종 사람 검토·등록 불가를 표시했습니다. 제품 기대값·HTML·SRS·공식 자산·기존 실패 Run은 변경하지 않았으며 외부 보고는 PREVIEW만 생성했습니다. 이번 수정은 자동 공식 승인이나 Slack·Notion 실제 전송을 포함하지 않습니다.

선택이 충돌하면 다음 순서로 판단합니다.

1. 실제 구현과 설명의 사실성
2. 입력이 결과를 바꾸는 인과관계
3. SRS·TC·실행 증거의 근거와 추적성
4. Agent가 승인 기준을 바꾸지 못하게 하는 통제
5. 사용자가 면접에서 직접 말할 수 있는 설명 가능성
6. 프로젝트가 끝날 수 있게 하는 범위 통제
7. 위 조건을 충족한 뒤의 시각적 완성도

## 11. 새 작업자가 기억해야 할 한 문장

> 세훈님이 만들고 싶은 것은 AI가 그럴듯한 TC와 코드를 보여주는 화면이 아니라, 기존 제품 기준과 QA 규칙을 근거로 실제 산출물을 만들고, 각 결과를 Checkpoint와 실행 증거로 통제하며, 본인이 면접에서 설명할 수 있는 변경관리 QA 파이프라인이다.

## 12. 설명 문서 통합 결정

2026-09-12 사용자 요청으로 설명 문서를 README(소개·실행), docs/PROJECT_GUIDE.md(시나리오·상세 로직), PROJECT_HANDOFF.md(현재 검증·결과 위치·다음 작업) 세 개로 통합했습니다. 기존 02~06·08 설명 파일을 통합하고 현재 계약은 프로젝트 안내에서, 날짜별 최신 수치는 인계 문서에서 관리합니다. SRS·테스트 카탈로그·기존 의사결정·실행 증거는 보존합니다. 문서 이동에 따른 SRS 안내 링크만 교정하며 제품 Requirement·인수 기준은 변경하지 않습니다. 기존 미커밋 CP1·UI 수정과 실제 재검증 결과도 통합 문서에 반영했습니다. 0바이트 로그는 실행 증거일 수 있어 유지하고 비어 있는 루트 reports·scripts 폴더만 제거합니다. 코드 로직 변경·새 API 실행·외부 전송·공식 승인을 수행한 작업이 아닙니다.

### 코드 감사 후 최소 보완 결정

2026-09-12 사용자 승인으로 실제 재현한 의미 반전·근거 밖 값·시험 절차 누락·결과 오분류·로컬 쓰기 요청 보호를 보완합니다. 요구사항→TC→계획→시험→분류→사람 승인 흐름과 기존 모델/추론 강도는 바꾸지 않습니다. 일반적인 의미 분석 엔진·새 서비스·문서 형식 전환은 추가하지 않습니다. 새 CP1/CP2 의미 규칙은 Manifest의 meaning_guard_contract로 구분해 과거 저장 판정과 해시를 소급 변경하지 않습니다. 범용 절차 검사에서 마지막 읽기 전용 확인은 연결된 Assertion으로 인정하고 불필요한 클릭을 강제하지 않습니다. 내부 프로그램 오류는 정상적인 자동화 제외와 구분해 전체 ERROR/비정상 종료로 기록하되 다른 후보의 독립 처리는 유지합니다. 출처 검사는 로컬 브라우저 요청 보호이며 인증·OS 격리로 표현하지 않습니다. 자연어 복합 조건의 완전한 의미 일치나 모든 오류 제거를 주장하지 않고, 재현한 오류 차단과 확인한 정상 입력 범위만 기록합니다. 제품·공식 자산·과거 Run과 기존 미커밋 문서 통합은 보존하며 최신 검증 수치는 PROJECT_HANDOFF에 둡니다.

<a id="v1-baseline-audit"></a>

## 13. V1 기준 자산 감사 보존본

기준일 2026-08-12. 이전 docs/05_PROJECT1_BASELINE_AUDIT.md의 제품·TC 대응·Coverage·GAP·표현 감사표를 보존합니다. 대상은 V1 portfolio_export의 virtual-controller.html, tests/test_controller.py, tests/conftest.py, scripts/agent4_reporting.py입니다. 아래 LOCAL·현장·미구현 표현은 당시 V1 상태이며 현재 V2 지원 범위가 아닙니다. 현재 V2 계약은 [프로젝트 안내](docs/PROJECT_GUIDE.md)를 따릅니다.

### 3. 제품 구현 확인

##### 확인된 제품 동작

- IDU-00~IDU-15 카드 16개와 내부 숫자 ID 1~16
- 단일·복수 장비 선택
- 전원, 5개 모드, 설정 온도, 4개 풍량과 잠금 대기값
- 적용 버튼 이후 허용 대상 상태 반영
- 오프라인·오류·잠금 상태의 제어 제한
- 코드로 주입된 VIEWER 역할 상태의 중앙 제어 차단
- 장비별 부분 적용
- °C·°F 표시 전환
- 상태 저장과 새로고침 후 복원
- 사용자 Toast
- HTML 기반 내부 상태 및 Register 시뮬레이션

##### 화면에 보이지만 제품 기능으로 확정하지 않은 UI

- 장치 필터: 모든 장치/상태 한 항목만 있고 필터 처리 이벤트가 없습니다.
- 장치정보 탭: 탭 모양은 있으나 전환 이벤트와 별도 정보 화면이 없습니다.
- 도움말 버튼: 클릭 동작이 연결되어 있지 않습니다.
- 역할 전환: VIEWER 차단 함수와 안내 Overlay는 있으나 ADMIN·VIEWER 전환 버튼이 HTML에 없습니다.

따라서 위 항목은 구현 완료 기능으로 주장하지 않고, 정식 요구사항으로 확정하려면 별도 UI와 동작을 먼저 구현해야 합니다.

##### 제품 기준으로 확정하지 않은 항목

- 숨김 공기청정 기능: Workflow Demo에서 표시되는 확장 사례
- 실제 Modbus·MQTT 통신
- 오프라인 장비의 현장 조작 성공
- 실제 운영 장비 알람으로서의 CH05·CH21 의미
- 실제 서버 저장과 다중 사용자 상태 동기화
- 수치화된 성능·응답시간 SLA

### 4. 기존 13개 TC 분류

기존 결과의 8 Pass·3 Fail·2 Skipped는 제품 회귀 성공률이 아니라 제품 기능 TC와 분류용 고정 사례가 섞인 데모 Dataset입니다.

| 분류 | 수 | TC |
|---|---:|---|
| 환경 사전 점검 사례 | 1 | TC-ENV-000 |
| 제품 기능 후보 | 7 | TC-MODE-001~003, TC-LOCK-001, TC-ERR-001, TC-INT-002, TC-TEMP-001 |
| 제품 결함 분류 Fixture | 1 | TC-TEMP-002 |
| Pipeline Control Fixture | 4 | TC-PIPE-001~004 |
| 합계 | 13 | 프로젝트 1 자산 |

##### TC와 제품 요구사항 연결

| TC ID | 실제 검증 목적 | 연결 요구사항 | V2 처리 |
|---|---|---|---|
| TC-ENV-000 | 페이지와 장비 16대, QA 패널 확인 | REQ-ENV-001 | 사전 점검 사례 |
| TC-MODE-001 | HEAT·24°C 적용과 화면·내부 상태 | REQ-CONTROL-001, REQ-MODE-001, REQ-STATE-001 | 회귀 후보 |
| TC-MODE-002 | FAN 온도 입력 비활성 | REQ-MODE-002 | 회귀 후보 |
| TC-MODE-003 | DRY 비활성 후 COOL 재활성 | REQ-MODE-001, REQ-MODE-002 | 회귀 후보 |
| TC-LOCK-001 | 16대 잠금과 중앙·현장 차단 | REQ-LOCK-001 | 회귀 후보 |
| TC-ERR-001 | CH05 오류와 중앙 제어 차단 | REQ-ERROR-001 | 회귀 후보 |
| TC-INT-002 | 선택 3대 HEAT 적용 | REQ-BATCH-001 | 비대상 불변 보완 전 회귀 제외 |
| TC-TEMP-001 | 30°C 상한 초과 차단 | REQ-TEMP-001 | 회귀 후보 |
| TC-TEMP-002 | AUTO 18°C 하한 위반 분류 | 기존 제품 SRS 근거 없음 | 제품 회귀 제외 |
| TC-PIPE-001 | 알람 UI 기준 부족 | 해당 없음 | REQUIREMENT_REVIEW Fixture |
| TC-PIPE-002 | 시뮬레이터 미응답 | 해당 없음 | ENVIRONMENT_ISSUE Fixture |
| TC-PIPE-003 | 존재하지 않는 Locator | 해당 없음 | AUTOMATION_EXECUTION_ERROR Fixture |
| TC-PIPE-004 | 지원 밖 17번째 장비 | 해당 없음 | NOT_EXECUTED Fixture |

### 5. 요구사항 Coverage

| Coverage | Requirement |
|---|---|
| EXISTING_TC | REQ-ENV-001, REQ-CONTROL-001, REQ-BATCH-001, REQ-MODE-001, REQ-MODE-002, REQ-TEMP-001, REQ-LOCK-001, REQ-ERROR-001, REQ-STATE-001 |
| PARTIAL | REQ-BATCH-002, REQ-NOTIFY-001 |
| CODE_OBSERVED_WITHOUT_DEDICATED_TC | REQ-SELECT-001, REQ-OVERVIEW-001, REQ-MONITOR-001, REQ-POWER-001, REQ-AUTH-001, REQ-TEMP-002, REQ-FAN-001, REQ-ERROR-002, REQ-GATEWAY-001, REQ-GATEWAY-002, REQ-LOCAL-001, REQ-LOCAL-002, REQ-PERSIST-001 |

코드가 존재한다고 검증 완료인 것은 아니며 전용 TC가 없다고 미구현인 것도 아닙니다.

### 6. 알려진 불일치와 한계

| ID | 확인 내용 | 영향 | V2 원칙 |
|---|---|---|---|
| GAP-TEMP-001 | 화면 정책은 16°C 하한이지만 중앙·현장 온도 코드가 15°C까지 허용 | SRS와 구현 불일치 | 정상 기대값은 16°C로 유지하고 결함으로 기록 |
| GAP-BATCH-001 | TC-INT-002가 선택 3대만 확인하고 비대상 13대 불변을 검증하지 않음 | REQ-BATCH-002 증거 부족 | 보완 전 회귀 세트 제외 |
| GAP-LOCAL-001 | 오프라인 현장 조작 로그는 작동을 표현하지만 실제 상태는 변경되지 않음 | 메시지와 실제 동작 불일치 | 제품 기대 결과 확정 보류 |
| GAP-GATE-001 | Project1 자체에서는 TC-ENV-000 실패가 후속 테스트를 자동 차단하지 않음 | Project1 단독 실행을 Gate로 주장할 수 없음 | V2 `execute`가 복사 Workspace에서 TC-ENV-000을 먼저 실행하고 미통과 시 관련 회귀를 차단 |
| GAP-EVIDENCE-001 | 기존 결과의 evidence_path가 빈 값 | Screenshot·Trace 완전성 근거 없음 | 존재하는 증거만 보고 |
| GAP-CLASS-001 | conftest가 예외 유형을 의미 라벨 failure_reason으로 먼저 작성하고 Agent 4가 이를 다시 분류 | 독립 분류 정확도 평가 불가 | V2는 중립 실행 신호를 규칙으로 분류. 독립 정답 기준과의 정확도·반복 평가는 별도 평가 프로젝트 범위 |
| GAP-CP4-001 | Project1 Agent 4는 검증 차단 여부 확인 전에 요약·포트폴리오 동기화를 수행 | Fail-closed 보장 부족 | V2 외부 전송은 CP4·인계 무결성 검사 통과 뒤 허용. 진단·검토 파일 저장까지 막는다는 뜻은 아님 |
| GAP-A1A2-001 | Agent 1·2는 저장된 Prompt·샘플·Demo이며 Live 모델 실행기가 없음 | 입력과 산출물의 동적 인과관계 없음 | V2 Agent 1부터 실제 호출 구현 |
| GAP-A3-001 | Agent 3는 기존 사람이 작성한 테스트를 실행하며 TC 기반 코드 생성은 하지 않음 | End-to-End 생성 파이프라인 아님 | V2에서 코드 후보 생성·검증을 별도 구현 |
| GAP-AUTH-001 | VIEWER 차단 로직은 있으나 화면에 역할 전환 버튼이 없음 | 일반 사용자 흐름으로 권한 전환을 검증할 수 없음 | 직접 함수 주입을 제품 UI 검증으로 포장하지 않고 UI 구현 전까지 미완료로 표시 |
| GAP-UI-SHELL-001 | 필터·장치정보 탭·도움말 버튼에 실제 동작이 연결되지 않음 | 화면만 보고 구현 기능으로 오해할 수 있음 | 요구사항·Coverage에서 제외하고 UI 껍데기로 기록 |

### 7. 사실성 표현 가이드

##### 사용 가능한 표현

- QA 판단 기준 기반 Agent Workflow Prototype을 설계했습니다.
- 가상 중앙제어기와 사람이 작성한 Playwright 회귀 테스트를 구현했습니다.
- 기존 실행 결과를 Python 규칙으로 집계하고 5개 원인 범주로 매핑했습니다.
- Agent 역할과 Checkpoint 검토 기준을 UI와 문서로 시연했습니다.
- Project1의 구현 간격을 확인하고 V2에서 Agent 1~3 실제 모델 호출·후보 시험과 규칙 기반 Agent 4 보고로 보완했습니다. 구체적인 확인 범위는 V2 Run별 증거로 구분합니다.

##### 피해야 할 표현

- 네 개 AI Agent가 새 요구사항을 실제로 End-to-End 처리했습니다.
- AI가 새로운 TC와 Playwright 코드를 자동 생성해 실행했습니다.
- Checkpoint 3가 환경 실패 시 모든 후속 실행을 차단했습니다.
- Agent 4의 분류 정확도를 검증했습니다.
- False Positive를 0%로 만들었습니다.
- 기존 13건 결과가 제품 회귀 품질을 나타냅니다.

### 8. 공개 포트폴리오 표현 재점검

Project1 원본은 이번 문서 개편에서 수정하지 않았습니다. 다음은 V1 감사 당시 구현보다 강했던 표현과 수정 제안이며, 현재 V2의 미구현 목록이나 외부 포트폴리오의 최신 게시 상태를 뜻하지 않습니다.

| 우선순위 | 위치·표현 | 실제 구현 | 권장 표현 |
|---|---|---|---|
| P0 | project.html의 Checkpoint 3가 사전 환경 점검 후 실행 증거를 승인·인계했다는 Before·After 사례 | TC-ENV-000은 일반 테스트이며 후속 차단 Gate와 독립 증거 승인 로직이 없음 | 환경 사전 점검과 실행 결과 검토 기준을 설계한 사례로 표시 |
| P0 | project.html의 Agent 4 오분류를 Checkpoint 4가 재분류했다는 Before·After 사례 | Agent 4는 conftest의 의미 라벨을 규칙으로 매핑하며 CP4에 의미 재분류 루프가 없음 | 규칙 기반 분류와 JSON·HTML 집계 정합성 검사로 표시 |
| P0 | interview 문서의 False Positive 0% 표현 | 독립 Ground Truth와 반복 평가가 없음 | 오분류 위험을 줄이기 위한 통제 기준을 설계했다고 표현 |
| P1 | Agent 1·2가 AI 산출물을 생성·검토해 다음 단계로 전달했다는 완료형 표현 | Prompt·샘플·Workflow Demo이며 실제 Live 인계가 아님 | Fixture 기반 역할·Checkpoint 시연이라고 표시 |
| P1 | Agent 1~4 QA 파이프라인 구동 영상 표현 | Agent 1~3 화면 일부는 Workflow Demo, Playwright와 Agent 4 규칙 코드는 실제 실행 | Demo와 실제 실행 구간을 분리 표기 |
| P1 | Agent 4 자동 분류를 품질 정확도 성과로 해석할 수 있는 표현 | 5개 범주 매핑은 구현됐지만 label leakage로 분류 정확도는 평가 불가 | 규칙 기반 원인 범주 매핑으로 한정 |
| P0 | `checkpoint2_review_after.md`의 모든 TC가 코드와 완벽히 일치하고 독립 실행이 보장된다는 표현 | TC-INT-002 비대상 불변 검증이 빠져 있고 일부 TC 계약·근거도 불완전함 | 검토 예시 문서이며 발견된 공백은 미해결 상태라고 표시 |
| P0 | `traceability_matrix.md`에서 전 TC의 Checkpoint 3 Pre-check 통과와 실행 증거를 완료형으로 표시 | 실제 독립 CP3 Gate가 없고 `evidence_path`도 비어 있음 | 기존 Test Run 매핑과 Report·JSON 기록만 사실로 표시 |
| P1 | project.html Hero의 AI가 동일한 품질 기준으로 테스트를 수행하도록 구조화했다는 표현 | Agent 1·2는 Fixture, Agent 3은 사람이 작성한 코드 실행임 | AI 역할·검토 기준을 시연했고 실제 실행은 Playwright·규칙 엔진이라고 분리 |
| P1 | README의 4-Agent·4-Checkpoint 및 Agent 1~4 흐름 설명 | 역할 구조는 있으나 네 단계 Live 인계가 구현된 것은 아님 | Fixture 기반 역할 시연과 실제 실행 구간을 같은 문단에서 구분 |

V2가 End-to-End로 구현되기 전에는 Project1 페이지에 Live Agent Pipeline 완료 표현을 추가하지 않습니다.
