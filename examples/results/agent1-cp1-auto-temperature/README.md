# Agent 1 → CP1 실제 실행 결과

실제 OpenAI API 연결 후 생성된 `RUN-20260811-135333-763DEB`의 공개용 증거입니다.

- 입력: AUTO 모드 온도 정책 변경 요청
- Agent 1 판정: `PROCEED`
- CP1 판정: 10개 규칙 모두 `PASS`
- 모델·토큰 사용량·실행 시각은 `run_manifest.json`에 보존
- API 키는 실행 환경변수로만 사용하며 파일에 저장하지 않음
- OpenAI `response_id`는 실행 증거에 필요하지 않아 공개본에서 제외

런타임 원본은 Git에서 제외되는 `runs/`에 저장하고, 검토가 끝난 결과만 이 폴더에 선별하여 공개합니다.
